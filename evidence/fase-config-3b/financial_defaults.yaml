# config/financial_defaults.yaml — FEATURE-CONFIG-EXTRACTION (FASE-CONFIG-3B)
# Valores financieros por defecto cuando plan_maestro_data.json no tiene
# datos regionales específicos. Cargado en runtime con cache por mtime.
#
# Extrae hardcodes: N-11 (SUPERPOSITION_FACTOR), N-11b (DEFAULTS dict)

version: "1.0.0"
description: "Valores financieros por defecto — fallback cuando no hay datos regionales"

superposition_factor: 0.7

factor_captura_aila: 0.70

comision_ota:
  min: 0.18
  base: 0.20
  max: 0.22

penalizacion_invisibilidad_ia: 0.05

exclusion_rating_bajo: 0.40

factor_perdida_base: 0.09
factor_perdida_min: 0.077
factor_perdida_max: 0.103

revpar_cop: 197120

reservas_ota_proporcion: 0.65
reservas_directo_proporcion: 0.35

uso_ia_proporcion_min: 0.10
uso_ia_proporcion_max: 0.20
