# Matriz de la conciliación final — ORDEN-CAMBIO-CALIDAD-PROCESO-2026-09-22

- **Sesión**: conciliación final previa al cierre, **2026-09-25**. Un solo escritor (la sesión principal);
  tres subagentes de **solo lectura** aportaron hallazgos, todos re-verificados en disco antes de escribir.
- **HEAD de partida medido**: `5817edd` · rama `master` · `git diff --cached` **vacío** (nada staged).
- **Fuera de esta sesión**: FASE-RELEASE, código, tests, `AGENTS.md`, `.cursorrules`, `.agents/**`,
  configuración, `VERSION.yaml`, `REGISTRY.md` y sus trackers, red, QMind, archivado, commit, tag, push.
- **Ninguna fase A/B/C/D se repitió.** Se distinguieron siempre **bloque C de la orden** y
  **piloto FASE-C del plan CONTEXTO**.

## 1. Requisito → evidencia → estado → remanente / dueño

| # | Requisito de la conciliación | Evidencia | Estado | Remanente y dueño |
|---|---|---|---|---|
| R1 | Conciliar piloto C ejecutado, D ejecutada, RELEASE pendiente | Orden: encabezado (tabla de cuatro momentos), §3, §4.C, §5 (filas `Alcance` y `Piloto`), §6 motivo 1, §7. Plan: `dependencias-fases.md` filas 3-5 (C y D **CERRADAS**, RELEASE **PENDIENTE**), `README.md` tabla y cabecera | **CONCILIADO** (documental) | FASE-RELEASE sin ejecutar — **dueño: el operador, con mandato propio** |
| R2 | Separar implementación / evidencia local / commit / publicación | Orden: bloque nuevo «Conciliación final 2026-09-25 — cuatro momentos que no se funden» + tabla con el comando de cada momento | **CONCILIADO** | Publicación: se **mide**, no se cita (`git fetch` + `rev-list`); el antecedente `da382b1..5817edd` queda como fechado |
| R3 | Conservar antecedentes fechados, retirarles vigencia de orden | Todos los retiros usan `⟦antecedente vencido …⟧` sobre el texto original: orden líneas del encabezado, §4.C `CONTEXTO/C`, §5 `Piloto`, §5-bis (cita del 2026-09-23), §5-ter (E5 y «autorizaciones separadas»), §7 | **CONCILIADO** | Nada que borrar: ningún antecedente se reescribió ni se eliminó |
| R4 | Eliminar la cuota residual de tres observaciones en WHATSAPP, admitiendo «sin novedades» | `REFACTOR-WHATSAPP-ENTREGA-2026-09-18/10-analisis-post-implementacion.md` §Lecciones nuevas (línea 72): «al menos tres observaciones» → sin cuota, «sin novedades» válido, y prohibido fabricar **confirmaciones** para no quedar en cero | **CONCILIADO en lo autorizado** | **`05-prompt-inicio-sesion-fase-G.md:52`** sigue mandando «al menos tres observaciones medidas» y **no estaba en el permiso** → dueño: el operador (es prompt de fase cerrada; el bloque C protege los prompts de fases cerradas como evidencia histórica) |
| R5 | Rectificar con atribución `whatsapp_html_detected` | Mismo archivo, §Seguimientos: fila marcada **CERRADO**, con el cableado real (`v4_asset_orchestrator.py` ~292-295/303/329/468, anotado `# FASE-B (AC1)`), con las tres excepciones tipadas ya retiradas de `validate_wiring.py`, y con **medición propia de esta sesión**: `scripts/validate_wiring.py --ignore-known` → `183 llamadas · gobernadas 75 (conformes 21, omisiones 0) · amparadas por excepción 0 · violaciones 0 · exit 0` (`08-validaciones.txt` §9 y `18-limites-y-cierre.md` §1) | **CONCILIADO**, atribuido a **FASE-B del propio plan** (no a esta sesión) | Anclas muertas 286/309/447 sustituidas por las corrientes; `docs/GUIA_TECNICA.md` y la evidencia de FASE-B no se tocaron |
| R6 | Rectificar con atribución la fecha de REGISTRY | Mismo archivo, §Seguimientos: la fila de «los dos escritores» queda **cerrada por el bloque B de la orden**. Verificado en disco: `scripts/sync_config.yaml` sección «REGISTRY.md — SIN REGLA DE FECHA (a proposito)»; `tests/test_registry_fecha_documental.py` aserta `registry_rules == []` y `"registry_last_update" not in ids`; cabecera real `docs/contributing/REGISTRY.md:3` = `2026-09-24` | **CONCILIADO** con atribución externa | La acción que la fila ordenaba (`sync_versions.py --rule registry_last_update`) quedó **sin objeto por diseño**; no se ejecutó ningún writer |
| R7 | JEV: consumidor C entregado, sin dar por autorizada su ejecución ni resueltas muestra humana ni decisiones de interfaz | `EVALUACION-JEV-TYPESAFE-2026-09-21/README.md` (filas FASE-B, tabla de interfaces, viñeta «Dependencia técnica», P2, P3, premisa P2) y `05-prompt-inicio-sesion-fase-B.md` (estado). Verificado: `git ls-files` → `scripts/triage_lesson_relevance.py` **rastreado**, añadido en `5817edd`; `scripts/build_phase_briefing.py` **sin rastrear** | **CONCILIADO** | **P1 revisión humana de la muestra** (sigue `BORRADOR`, `etiquetas.review_status = sin_revisar`), **decisión del *gap* de interfaz** (pedido vs efectivo, `usage_normalized`), **mandato de FASE-B** y **presupuesto de inferencias**: los cuatro **sin resolver y sin tocar** |
| R8 | Recalcular el porcentaje de carga desde `FASE-D/carga.json` | `carga.json`: `total.before 1.648.109`, `total.after 1.431.388`, `delta_bytes 216.721` → **216.721 ÷ 1.648.109 = 13,15 %** (calculado con `python -c`; el contraste con la re-medición de hoy queda en `12-huellas-post.txt` §F) | **CONCILIADO** | Las tres cifras que publicitaba el plan (`13,08`, `12,8`, «12 %») **no reproducían** su artefacto |
| R9 | Corregir consumidores y preferir referencias a una fuente | `README.md` fila «Carga de lectura A7» = **única fuente documental** (bytes + tokens + porcentaje + comando + límite); `README.md` §AC y tabla de fases, `09` §D (AC20), `10` decisión 1, `06` casilla de FASE-D: ahora **referencian** en vez de copiar | **CONCILIADO** | **L-VCF-19** respetada: `10` y `06` están dentro del corpus que el generador mide, así que ninguna cifra vive ahí |
| R10 | Sin alterar el expediente cerrado ni afirmar ahorro general | `evidence/…/FASE-D/carga.json` y `carga-pre-post.md` **no se re-escribieron** (sha256 en `12-huellas-post.txt` §F; la corrida usó `--carga -` / `--informe -`, que avisa «no se escribio ningun archivo»). La re-medición de hoy vive **en este expediente** (`11-carga-corrida-3.txt`, contraste en `12-huellas-post.txt` §F) | **CONCILIADO** | El porcentaje de `carga.json` (**13,15 %** recalculado de sus propias cifras) es la medida **del árbol que FASE-D midió**. La re-medición de hoy **no se publica en los documentos del plan**: cada edición los vuelve a mover (A6 sobre un corpus autorreferente). Y ninguno de los dos es un ahorro general del proceso, ni por fase, ni tiempo activo |
| R11 | Prompt RELEASE: cuatro fases nombradas y regeneración/verificación del índice **después** de las últimas escrituras | `05-prompt-inicio-sesion-fase-RELEASE.md`: lista de lectura con `05-prompt-inicio-sesion-fase-A/B/C/D.md` nombrados; cola fija reordenada (writers de corpus → packs → índice → dos `--check` → `--quick`) + **paso 7** de Post-ejecución que re-corre la cola tras las últimas escrituras | **CONCILIADO y MEDIDO**: el pack de RELEASE pasó de `SECCION-NO-RESUELTA` (`fuentes_ausentes: ["los cuatro prompts de fase"]`) a **COMPLETO con 10 fuentes** | Orden fijado leyendo las entradas/salidas reales: `build_phase_briefing.py` escribe `briefing/*.md` **dentro** del corpus de `build_lesson_index.py`; `--fix` reescribe `.md` |
| R12 | Permisos y bloqueantes explícitos en el prompt | **Primera pasada:** `--fix` y `--update-baseline` marcados **PERMISO APARTE** y trampa del `--carga evidence/…/FASE-D/carga.json` declarada. **Segunda pasada (rectificación pedida el 2026-09-25): retiro de la autorización implícita** — el prompt metía `sync` en «OFFLINE (tu mandato)» y a la vez reservaba permisos sobre sus destinos. Rige **C0**, añadido al prompt canónico, a su contrato (§Dos momentos + tabla de prohibiciones) y a la copia pegable, con los cinco destinos reales de `sync_config.yaml`, `VERSION.yaml` como **entrada** y el par `REGISTRY.md` + `.last_doc_phase.json` del escritor de registro | **CONCILIADO y medido** (ver `24-rectificacion-permisos.md`, `26-validacion-permisos.txt`, `27-frontera-permisos.json`) | C0 de una futura RELEASE **sin autorizar**: la rectificación describe operaciones, no concede permisos. Consumidores del recuento barridos y corregidos (README ×2, `dependencias` fila `CONTEXTO/RELEASE`, checklist de `10`); **dos menciones a «tres momentos» que hablaban de otra cosa** (cierre original / corrección / aceptación de FASE-B, en `09:109` y `dependencias:279`) **se dejaron intactas** |

## 2. Lo que esta conciliación **no** afirma

- No cierra la orden: sigue **abierta** y su cierre tiene criterio explícito en su §6 (conciliación 2026-09-25).
- No afirma terminados los cuatro planes ni la calidad semántica: **AC15 `NO-EJERCITADO`**, **D6 dormida**,
  **D7 sin activar**, y las deudas **D2, D3, D6, D7, S10, S14, S15, S16** siguen abiertas con su dueño.
- No ejecutó FASE-RELEASE, ni ninguna operación remota, ni commit, ni push, ni archivado, ni staging.
- No produjo métricas de tiempo activo ni de iteraciones: el instrumento canónico (`find . -name "*.jsonl"`)
  **no se corrió** aquí y no se inventó ninguna cifra en su lugar.

## 3. Cambios preexistentes conservados

`01-huellas-pre.txt` guarda el numstat contra HEAD de las 13 rutas tocadas **antes** de editar: son trabajo
ajeno (bloques B y C documentales, FASE-C, FASE-D, higiene) y **no** se atribuyen a esta sesión. Esta
conciliación es **aditiva sobre ese contenido**: ninguna línea ajena se restauró, reescribió ni revirtió.
