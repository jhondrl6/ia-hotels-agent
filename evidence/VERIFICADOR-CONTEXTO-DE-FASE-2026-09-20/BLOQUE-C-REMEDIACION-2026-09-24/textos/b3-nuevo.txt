8 de los 11 campos del piloto no están en la costura⟦**Cifra vencida, re-medida el 2026-09-24 y conservada aquí como lo que se dijo, no como estado vigente**: el «8» no reproduce ninguno de los dos métodos con que se cuenta el gap. Por **nombre literal** faltan **10 de los 11**, porque de los once nombres exigidos solo `request_id` aparece en `scripts/decision_client.py`. Por **identidad semántica declarada** —`proveedor`≈`provider_effective`, `modelo`≈`model_effective`, `respuestas`≈`answers`, `usage`≈`usage_raw`, más `request_id` por su propio nombre— faltan **6 de los 11**, y esa es la cifra vigente. No la fija esta anotación: la publica desde antes el consumidor real de este contrato, `EVALUACION-JEV-TYPESAFE-2026-09-21/README.md` en su párrafo «Gap de contrato medido», que nombra exactamente esos seis campos, y por eso **ese README no se edita**. Lo que sí era brecha, y es lo que se rectifica aquí con atribución y sin reconstruir el pasado, es que el resumen único de C y el plano al que C sirvió no dijeran el mismo número. Dos datos de la re-medición que este renglón no tenía: la costura es `class ResultadoEvaluacion` en `scripts/decision_client.py`, y los once campos se leen en `EVALUACION-JEV-TYPESAFE-2026-09-21/01-plan-maestro.md`, párrafo «Contrato requerido para el piloto». Comando que re-mide los dos métodos leyendo el código por AST: `./venv/Scripts/python.exe evidence/VERIFICADOR-CONTEXTO-DE-FASE-2026-09-20/BLOQUE-C-REMEDIACION-2026-09-24/instrumentos/campos_piloto.py` (su salida, en `02-barrido-reparado.txt`).⟧,
