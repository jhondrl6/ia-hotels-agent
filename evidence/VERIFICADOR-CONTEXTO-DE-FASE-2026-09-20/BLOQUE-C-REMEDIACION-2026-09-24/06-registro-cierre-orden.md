# 06 — Registro de cierre de la orden `ORDEN-CAMBIO-CALIDAD-PROCESO-2026-09-22` (sin ejecutar nada)

Estado de la orden al medir (2026-09-24, esta sesión): §6 tiene **6 casillas**, **5 `[x]`** y **1 `[ ]`**;
la destildada es el piloto, y su *lead-in* ya declara que cerrar cuatro por A/B y una por C «**no** cierra
esta orden: falta el piloto». Lo de abajo es lo que separa ese estado de la conclusión, renglón por
renglón. **Nada de esto se ejecutó ni se autorizó en esta sesión.**

| # | Qué falta (medido en disco) | Quién decide | Decisión mínima literal | Qué AC / casilla cierra | Qué evidencia lo probaría |
|---|---|---|---|---|---|
| 1 | **Piloto FASE-C de CONTEXTO** — única `[ ]` de §6. Su contrato está reconciliado (E1–E5 más carga total y frescura no circular), `scripts/triage_lesson_relevance.py` **no existe** porque nadie lo escribió: es lo que produce esa fase | El operador, con mandato propio (prompt canónico en `05-prompt-inicio-sesion-fase-C.md`, que esta sesión **no** copió ni editó) | «Autorizo la ejecución del piloto FASE-C de `VERIFICADOR-CONTEXTO-DE-FASE-2026-09-20` según su prompt, con este presupuesto.» | AC10–AC15 del plan CONTEXTO; casilla 6 de §6 de la orden; y con ella la demanda que originó el bloque C | `evidence/…/FASE-C/` con `ac10_delta.json`, `index_status`, `threshold`, `r26.txt`, `mutation/` y `coverage.json`; AC16 y AC17 en delta 0 sobre el árbol final |
| 2 | **Deudas con dueño intacto**: **D2** (promover `validate_governance_numbers.py` a check del `--quick` con renumeración 11→12; su rama literal se cumplió en parte), **D3 parcial** («Plan propio, posterior»), **D6** (lint de contradicciones semánticas; **dormida**, su disparador está ligado a D7), **D7** (proveedor real), **S10** (dónde vivirá el `import` del SDK), **D8/D9** (operaciones remotas QMind), **D10** (re-leer la interfaz del write-back al cerrar) | Cada una con el dueño que ya tiene en `01-plan-maestro.md` §6 y `dependencias-fases.md` §Deudas; D2 y D7 son decisión del operador, D10 es del propio plan en su RELEASE | «Se abre D2 / se mantiene D2 cerrada a esta orden», dicha **una por una**; para D6 no hay decisión posible sin D7 | D2 toca AC16 (delta 0 del `--quick`) y los pins de `tests/`; D6 dispara AC15; D7 es precondición de la aceptabilidad semántica; D8/D9 cierran el write-back; D10 se verifica en el RELEASE de CONTEXTO | Que cada deuda siga **nombrada con su dueño** en el maestro y en `dependencias-fases.md` (así está hoy) y que su cierre lleve el instrumento que lo afirma: `--quick` renumerado para D2, `validate_plan_semantics.py` para D6, proveedor real para D7/D6, `--upload` verificado por descarga+sha para D9 |
| 3 | **Momento remoto** (subidas y consultas QMind): falta **autorización literal y presupuesto**. Verificado en esta sesión: AC6 **sí** está partido en **AC6-entrega** (offline, cierra en la fase) y **AC6-aceptación** (Momento B, con dueño y disparador propios en maestro §6), y el arranque de `05-prompt-inicio-sesion.md` **no** contiene operación remota: `fetch_source_titles()` está movido a la tabla «Re-medir **en el momento B**» y marcado «es una llamada remota y no corre en esta sesión» | El operador (permiso) y el presupuesto que él fije | «Autorizo el momento B de `VERIFICADOR-ESCRITURA-QMIND-2026-09-20` con este presupuesto y este notebook.» | AC6-aceptación; `[15/15]` del check de QMind sobre el plan padre; D8 y D9 | Descarga byte a byte + `sha256` contra la instantánea versionada del repo (no el título, no el `exit 0` por CLI ausente); y que `_check_qmind_writeback()` distinga el tercer estado |
| 4 | **Tramos de fase que siguen abiertos**, por tres causas **distintas** que la orden exige no confundir: **FASE-D y su RELEASE** (CONTEXTO); **FASE-C de WHATSAPP con AC5**; **FASE-A→B→C de JEV** | Depende de la causa: el operador (autorización), el plan (dependencia técnica), o el revisor humano (aceptabilidad semántica) | Ver el desglose de abajo: cada renglón nombra **cuál** de las tres causas lo frena y con qué frase se desbloquea | CONTEXTO D: AC19–AC23; WHATSAPP C: AC5 y su dueño; JEV A: congelado de la muestra, B: AC1/AC2 contra la costura real, C: comparativa autorizada | El artefacto que cada fase declara en su `06-checklist` (packs, `carga.json`, `preflight.json` por proveedor, matriz de ACs), no un estado escrito |

**Desglose del renglón 4, porque las tres causas no se desbloquean con la misma decisión:**

| Tramo | **Dependencia técnica** (qué falta en código o en el árbol) | **Orden de gobernanza** (regla de serialización, no imposibilidad) | **Aceptabilidad semántica** (juicio que un verde no puede dar) |
|---|---|---|---|
| CONTEXTO **FASE-D** + **RELEASE** | El `build_phase_briefing.py` del pack no existe, y FASE-C no produjo los artefactos que D consume | D y RELEASE van después de C por mandato de una fase por sesión; el RELEASE además es **tercer momento** (offline / traslado / remoto) con permiso propio para el `git mv` | Ninguna: D es medición y empaquetado. Lo semántico quedó en AC15 `NO-EJERCITADO`, declarado, con D6 dormida |
| WHATSAPP **FASE-C** con **AC5** | Ninguna nueva: su punto de reanudación está reconciliado y publicado | AC5 es deuda declarada con dueño; la orden prohíbe cerrarla por arrastre de otra fase | Lo que AC5 mide es calidad de entrega: su verde con proveedor falso **no** vale como aceptabilidad |
| JEV **A→B→C** | **A**: falta el congelado humano de la muestra (4 pares `sin_revisar`, umbrales nulos). **B**: necesita el consumidor C del hermano **implementado** (`triage_lesson_relevance.py` no existe). **C**: preflight de autenticación y cuota | El orden de cierre hermano-primero (`JEV/dependencias-fases.md`, archivo **protegido**, `sha256 b55670d7…`, numstat `18 0`) serializa índice y write-back: es **gobernanza**, no imposibilidad técnica, y `D`/`RELEASE` del hermano **no** son precondición de B | Con proveedor real: `acceptance = NO-EJERCITADO` y AC15 parcial del hermano son **la entrada esperada**, no un bloqueo circular. Falta además la **decisión** sobre el gap de contrato (6 campos): extender la costura de forma aditiva, o producirlos en el runner de JEV |

| # | Qué falta | Quién decide | Decisión mínima literal | Qué cierra | Qué evidencia lo probaría |
|---|---|---|---|---|---|
| 5 | **Árbol sin commitear.** B y C siguen sin commit sobre `da382b1`: `git status --porcelain` da **62 líneas** = **54 modificadas** + **8 sin trackear** (4 rutas de `tests/` y 4 expedientes de `evidence/`, uno de los cuales es el de esta sesión). De las 54 modificadas, **2** son las cabeceras que esta sesión anotó; el expediente de C está sin trackear, así que git no cuenta sus cambios | El operador, y solo él | «Hace commit de este árbol» — y, si quiere publicado, «haz push», **por separado** | **Ninguna.** El `git commit` no es el quinto corte ni condición de ninguno de los cinco: es una acción posterior, separada y opcional | `git status --porcelain` vacío y `git rev-parse --short HEAD` distinto; el pre-commit con sus 7 checks en verde al hacerlo |
| 6 | **Textos centrales desincronizados que se declaran y no se tocan**: `AGENTS.md` (su cabecera sigue diciendo `v4.77.3` y publica cifras que el propio repo imprime distinta) y la tabla de `docs/CONTRIBUTING.md` sobre **cuándo** se regenera `DOMAIN_PRIMER` | El operador, porque tocar `AGENTS.md`/`.cursorrules` exige instrucción explícita y el clasificador lo bloquea sin ella | «Alinea `AGENTS.md` y la tabla de `docs/CONTRIBUTING.md` con el contrato del executor» — frase aparte, no por arrastre | **Ninguna casilla de §6.** Es coherencia de los textos de gobierno, y la orden la lista como decisión del operador en su §2 «Decisión humana» | `python scripts/validate_document_integration.py` y `python scripts/validate_agents_md.py` en verde sobre el texto ya alineado, no sobre el actual |

**Línea explícita que la orden exige, y que este registro no difumina:** ejecutar todo lo anterior
**tampoco cierra las deudas externas**. Aun con el piloto corrido, el momento remoto autorizado y los
tres tramos cerrados, siguen abiertas **D2** (pide decisión propia y toca pins de `tests/`), **D3** en su
parte completa, **D6** (dormida hasta que exista proveedor real), **D7** (el proveedor real en sí) y
**S10** (la geometría del `import` del SDK). Ninguna de las cinco se cierra por documento: se cierran con
hechos que esta sesión no pudo producir sin violar sus propias fronteras.
