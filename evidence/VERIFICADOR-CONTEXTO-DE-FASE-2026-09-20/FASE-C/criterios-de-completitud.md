# FASE-C — criterios de completitud, verificados contra el artefacto

Cada línea responde la pregunta práctica del contrato (§Reglas de forma, R2.4): *«¿dónde lo vería un
humano que solo tiene el artefacto?»*. Ningún criterio se marcó cumplido por haberlo intentado, y los
que no lo están dicen por qué.

| Criterio del prompt | Veredicto | Dónde se ve |
|---|---|---|
| `ac10_delta.json` muestra `removed: []` y hay un test que lo afirma (AC10) | **CUMPLIDO** | `ac10_delta.json` (`anchored_before` 14 = `anchored_after` 14, `removed: []`, `filas_cuestionadas_sin_borrar` con **11** IDs, `escrita_por_este_script: false`) + `test_triage_no_elimina_fila_anclada.py` (3 casos, todos verdes en `run_tests.txt`) |
| Los **tres** estados del índice tienen su test nombrado por la causa; ninguno cubre dos (⟦E2⟧) | **CUMPLIDO, con dos sub-causas añadidas por medición** | `test_triage_indices_ausente.py` (`exit 2`, ruta + comando), `test_triage_indices_vencido.py` (`exit 4`, `VENCIDO` producido por el check **propio** de C, con el ID que lo venció y la referencia al `[6/7]` del hook), `test_triage_indices_lector_fallido.py` (`exit 3`, tres artefactos rotos + sub-causa `calculo-en-memoria`). Los tres tests comprueban además la **no-colapsabilidad** (aserción explícita de que el estado no es el de los otros dos). Las dos sub-causas extra son la lección **L-VCF-14**: *parseable no es conforme* |
| `mutation/` tiene rojo y verde del guard de no-filtrado (AC14). Sin rojo, ⚠️ | **CUMPLIDO — con rojo** | `mutation/verde_baseline.json` (guard activo: 14→14, `removed: []`), `mutation/mutante_M-AC10-guard-aditividad.json` (guard apagado: 14→**3**, `removed` = los 11 IDs cuestionados), `mutation/resumen.txt` (símbolo mutado, alcance declarado del observador, destino declarado por el cierre), `mutation/corrida.txt` (`exit 0`). En pytest: `test_rojo_con_el_guard_apagado` + `test_el_rojo_es_el_que_quiebra_la_afirmacion_de_ac10` |
| `r26.txt` declara si el test contra corpus real **corrió** o se saltó, con el motivo (AC13) | **CUMPLIDO — corrió** | `r26.txt`: 4 casos PASSED, `SKIPPED = 0` con `-v -rs`, corpus medido (27 archivados, 2 con `00-`) y la **desviación de ruta registrada con su medición**, no aplicada en silencio. Un §2 del archivado sin filas leídas **falla** en lugar de saltar: el skipif solo cubre la ausencia del corpus |
| `coverage.json` tiene población, términos con sus conteos (**ceros incluidos**), familias no juzgadas y `acceptance` (AC15) | **PARCIAL por diseño — ⚠️ y no ✅** | La parte **no semántica** está: población leída de la corrida, `35 de 334`, 10 términos con **5 ceros** (incluido `verificador mec = 0`, A5 reproducido), `familias_no_juzgadas: ["S"]`, `ids_numericos` con su método y el `pool_no_sometido_a_juicio` declarado. La parte **semántica**: `acceptance = NO-EJERCITADO`, `valor: null`, motivo literal, y **sin ratio simulada** (E4). Es el techo que el plan previó con D7 sin activar, así que AC15 **no** se certifica verde |
| ⟦E1⟧ El umbral está aplicado a `confidence` de una pregunta `choice` de dos opciones, su `basis` nombra el campo, y hay prueba de que `probabilidad_si` no gobierna | **CUMPLIDO** | `informe.json` → `umbral.campo: confidence`, `umbral.basis` (nombra el campo y niega `por_si`), `forma_pregunta.opciones` = 2; `test_triage_pregunta_es_choice_y_no_noul.py` (6 casos) con el caso invertido (`por_si` 0.90 / `confidence` 0.41 → `a-revisar-humano`) y la prueba de que dos candidatos con el **mismo** `por_si` caen en buckets distintos |
| ⟦E3⟧ Ninguna propuesta del falso entró en §2 sin revisión humana; cada aceptación y cada rechazo registrados con quién decidió, y el script no editó §2 | **CUMPLIDO en su mecanismo — con 0 decisiones humanas, declarado** | `informe.json` → `revision_humana: {n_pendientes: 5, registros: [], seccion_dos_editada_por_este_script: false}`; sha256 del `00-` idéntico antes y después de la corrida completa y **cero operaciones de escritura observadas** dentro del plan. El mecanismo de registro está ejercitado por test (aceptada → `ACEPTADA-PARA-QUE-ESCRIBA-UN-HUMANO` con la fila para que la aplique una mano; rechazada → `RECHAZADA-PUBLICADA` con `fila_no_se_borra: true`; incompleta → `exit 6`; fuera de lista → `FUERA-DE-LISTA`). **Ninguna propuesta se revisó en esta sesión**, así que §2 quedó con sus 14 filas y §4 del `00-` lo dice con su conteo |
| Con proveedor falso, AC15 en ⚠️, `acceptance = NO-EJERCITADO` con motivo **y sin cifra simulada** (⟦E4⟧); **D6 declarada dormida** en `dependencias-fases.md` | **CUMPLIDO** | `coverage.json` → `aceptacion` (con `prohibido` explícito) y el test que busca la ratio mecánica y falla si aparece publicada; `dependencias-fases.md` fila D6 con la nota datada «DORMIDA con causa medida» y su re-evaluación atada a D7 |
| ⟦E5⟧ C cerró **sin** aplicar las mejoras generales de la orden de calidad: sigue leyendo el workflow canónico, no renumeró checks y no tocó el proceso común | **CUMPLIDO** | Delta 0 en los dos conteos (§1 arriba, resta comprobada); `git status --porcelain .agents/` sin ninguna ruta creada ni cambiada por C (las 3 sucias son preexistentes y se declaran); ningún comando del proceso común fue reescrito; la orden de calidad se tocó **solo** en su §6 y **por referencia** a esta evidencia |
| `validate_lesson_capitalization.py` sigue verde sobre `00-lecciones-capitalizadas.md` **después** de aplicar solo los candidatos **aceptados** (AC18) | **CUMPLIDO por vía vacua y declarada** | Cero candidatos aceptados ⇒ cero escrituras en §2 ⇒ la verde del verificador de forma se re-midió sobre el `00-` con sus 14 filas originales (salida en `run_all_validations_quick_post.txt`, check `[10/11]`). La fila dice explícitamente que el caso «aplicar aceptados» **no** se ejercitó en esta sesión: queda para cuando haya revisión humana |
| `--quick` verde sin haber alterado su composición (AC16) | **CUMPLIDO** | `run_all_validations_quick_post.txt` (11/11, `exit 0`) contra el par PRE medido al abrir (`faseC_quick_pre.txt`) |
| Post-ejecución completa e índice regenerado y comprobado sobre el mismo árbol final verificado | **CUMPLIDO en el cierre** | Los seis pasos del contrato: `dependencias-fases.md` (fila de C + D6), `README.md` (estado y contador de ACs), `06-checklist-implementacion.md` (tabla y casillas), `09` §A/§B/§D/§E, `10` (fila de la fase, lecciones L-VCF-13/14, decisiones con su alternativa rechazada, deuda S14), `00-` §4 (auto-triaje con conteo y carácter de falso) + Q12 re-ejecutable. Índice: `build_lesson_index.py` regenerado tras la última escritura de `.md` y `--check` en verde sobre ese árbol |
| El commit es opcional, posterior y requiere autorización explícita; no condiciona ninguno de los cinco cortes | **RESPETADO — sin commit ni push** | HEAD sigue siendo el de la apertura y el árbol conserva el trabajo de C **sin commitear**, mezclado con el ajeno (declarado en §2 y en `cero-red.txt`). La casilla de §6 de la orden se cerró **por referencia**, sin necesidad de commit |

## Lo que esta fase **no** cierra, dicho en su propio nombre

- **AC15 en su tramo semántico** (`acceptance`) y, con él, **D6** (dormida) — disparador atado a **D7**.
- **S10** (dónde vivirá el `import` del SDK cuando D7 se active) — del tramo que consume un proveedor real.
- **S14** (nueva, dueña: FASE-D o RELEASE de este plan) — `--plans-dir` sin `--context-dir` simétrico.
- **FASE-D** y **FASE-RELEASE** del plan, y por tanto el plan completo: tres de sus cuatro fases de
  implementación están cerradas, ninguna de las dos sesiones restantes lo está.
- **La orden de calidad `ORDEN-CAMBIO-CALIDAD-PROCESO-2026-09-22.md`**: sus seis criterios quedan
  satisfechos y la orden **sigue abierta**; el por qué está escrito en su propio §6, no aquí.
