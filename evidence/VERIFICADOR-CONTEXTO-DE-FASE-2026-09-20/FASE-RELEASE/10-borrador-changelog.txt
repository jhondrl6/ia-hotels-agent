# 10 · Borrador de la entrada `[4.78.0]` para CHANGELOG.md

Borrador **sin aplicar**: `CHANGELOG.md` está intacto (sha256 verificado en `08-sync-ejecutado.txt`) y la
entrada solo se escribe cuando la fuente única esté en 4.78.0 y `[6/11]` vuelva a verde. Se inserta
**encima** de `## [Sin publicar] - Remediación bloque A …` (línea 3), que es la convención del archivo.

---

## [4.78.0] - Gobernanza, costura, pertinencia y carga medida — 2026-09-25

### Objetivo

`VERIFICADOR-CONTEXTO-DE-FASE-2026-09-20` ataca tres formas en que un plan de este repo se lee caro, se
juzga mal y se desfasa solo: la **coherencia** de las aserciones sobre conteos en documentos de gobierno, la
**pertinencia** de las lecciones que el Paso 0 capitaliza, y la **carga de lectura** de una sesión de fase.
Cuatro fases de implementación, cada una con mandato propio y corte «hasta listo para revisión», y su
FASE-RELEASE ejecutada offline. El techo declarado del lote es `VERIFICADO OFFLINE`: el plan tiene
prohibidas la pipeline y la red, así que ningún AC puede llegar a `SUPERADO EN E2E`.

### Cambios Implementados

- **A — `scripts/validate_governance_numbers.py`** (AC1–AC5): aserción contra fuente dinámica con
  denominador publicado y tres estados; es el guard de las ediciones futuras sobre `.agents/`.
- **B — `scripts/decision_client.py`** (AC6–AC9): costura neutra de decisión con contract test de forma,
  tri-estado de proveedor y extensión probada a un segundo proveedor falso. AC9 certifica la **costura**, no
  al proveedor: activar uno real es deuda D7, sigue sin hacer.
- **C — `scripts/triage_lesson_relevance.py`** (AC10–AC15): capa de pertinencia **aditiva** sobre
  `lecciones_index.json` — propone y jamás descarta. **AC15 queda `NO-EJERCITADO`**: el emisor fue un
  proveedor falso, así que la mecánica está verificada y la aceptabilidad semántica no medida.
- **D — `scripts/build_phase_briefing.py`** (AC19–AC23): pack por fase dentro del plan, frescura por
  `sha256` de las fuentes gobernadas con HEAD como procedencia que **no** vence, negativa a truncar en
  silencio y **carga total de lectura medida** con tres sumandos por lado y la resta entre cargas totales.
- **Orden de calidad `ORDEN-CAMBIO-CALIDAD-PROCESO-2026-09-22.md`**: bloque A (remediación técnica
  focalizada), bloque B (proceso común e instrumentos, con su matriz vigente en §13), bloque C (enmiendas
  prospectivas a los cuatro planes) y su conciliación final, que separó implementación · evidencia en árbol ·
  commit · publicación y convirtió el prefight de permisos de FASE-RELEASE (C0) en contrato: *offline
  significa sin red, no con permiso de escribir*.

### Archivos Nuevos

Los que dejó cada fase, con sus pruebas y expedientes: `scripts/validate_governance_numbers.py`,
`scripts/decision_client.py`, `scripts/triage_lesson_relevance.py`, `scripts/build_phase_briefing.py`,
`tests/quality_gates/{decision_client,governance_numbers,lesson_relevance,phase_briefing}/`,
`tests/support_observador_escrituras.py`, `tests/test_registry_fecha_documental.py`,
`tests/test_validate_document_integration.py` y el directorio generado
`.opencode/plans/VERIFICADOR-CONTEXTO-DE-FASE-2026-09-20/briefing/` (5 packs).

### Frontera de versionado (declarada, no ocultada)

Commit y push **no** forman parte de esta release hasta que el operador los ordene: `5817edd` trae las 37
rutas propias de FASE-C y `7f2e9f9` el helper compartido, pero el producto de FASE-D
(`build_phase_briefing.py`, sus 49 casos y los packs), los documentos de cierre de C y D, el par del índice
y el expediente de esta conciliación **existen solo en el árbol de trabajo**. Esta entrada documenta el
trabajo; no afirma que esté publicado.

### Lo que queda abierto

D2, D3 completa, D6 (dormida con causa: su disparador es un `acceptance` no ejercitado), D7 + S10, D8/D9
(momento remoto sin autorización ni presupuesto), S14, S15 (`[6/7]` no reproducible entre checkouts), S16
(la convención `Lee …` no está en la plantilla) y las cinco propuestas de revisión humana del piloto
(L-VCF-10…14). El archivado del plan conserva su propio permiso.

### Tests

**No se corrió ninguna suite en la sesión de cierre**: el mandato de la conciliación y de este cierre
excluye tests. Las cifras por fase viven en su propio expediente
(`evidence/VERIFICADOR-CONTEXTO-DE-FASE-2026-09-20/FASE-<A..D>/run_tests.txt`) y no se re-transcriben aquí.
Lo que sí imprimió este cierre es su batería de verificadores, con exit por comando:
`evidence/…/FASE-RELEASE/07-cierre-verificacion.txt`.

---

## Comprobaciones que faltan antes de escribir esta entrada

| # | Pendiente | Motivo medido |
|---|---|---|
| 1 | Autorización para `doctor.py --regenerate-domain-primer` | `validate_document_integration.py:435-472` exige que `DOMAIN_PRIMER.md:6-7` cuadre con `VERSION.yaml`; sin regenerar, el quick cae a 10/11 en cuanto VERSION.yaml diga 4.78.0 (`09-codename-y-bloqueador.txt`) |
| 2 | Escribir `VERSION.yaml` (4.78.0 · codename · 2026-09-25 en `release_date` y `date`) | autorizado en cuanto 1 esté resuelto: hoy escribirlo deja rojo a reposo |
| 3 | `log_phase_completion.py` con `--release 4.78.0` (primero `--dry-run`) | su escritor es el único admitido de la cabecera de REGISTRY, y estampa la versión ya fijada: va después del sync |
| 4 | Decidir si el bloque A de la orden sube a esta release o sigue «Sin publicar» | su entrada actual lo declara explícitamente no-release y `VERSION.yaml` tiene el mismo comentario; con 4.78.0 eso cambia de estado y hay que reescribirlo con atribución |
