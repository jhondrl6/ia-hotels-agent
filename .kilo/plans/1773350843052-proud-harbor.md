# Plan for Executing README.md Line 133 with Data Consumption Guarantees

## Objective
Execute `python main.py v4complete --url https://hotelvisperas.com --nombre "Hotel Vísperas"` while ensuring:
1. Every data point produced is consumed in the pipeline
2. Every problem identified in diagnosis has a corresponding asset
3. Assets use real hotel data (not placeholders/ESTIMATED where avoidable)
4. No disconnections exist between data production and consumption
5. All publication gates pass (coherence ≥ 0.8, etc.)

## Prerequisites
- System dependencies installed (Python 3.8+, required packages)
- API keys configured (DeepSeek, Google Maps, etc.)
- Hotel Vísperas accessible at https://hotelvisperas.com

## Step-by-Step Execution Plan

### Phase 1: Onboard Real Hotel Data
**Purpose**: Capture confirmed operational data to replace estimations
```bash
python main.py onboard --url https://hotelvisperas.com --nombre "Hotel Vísperas"
```
**Expected Inputs** (provide when prompted):
- Número de habitaciones: 12 (example - should be real value)
- Reservas mensuales promedio: 30 (example - should be real value)
- Valor promedio de reserva (COP): 150000 (example - should be real value)
- % Canal directo: 70.0 (example - should be real value)
- Additional fields as prompted (occupancy rate, etc.)

**Validation**:
- Check that data was saved to: `output\clientes\hotel-vsperas_onboarding.yaml`
- Verify file contains real values, not defaults/estimations

### Phase 2: Execute Analysis with Onboarded Data
**Purpose**: Run v4complete using the confirmed hotel data
```bash
python main.py v4complete --url https://hotelvisperas.com --nombre "Hotel Vísperas"
```
**Note**: The v4complete command should automatically detect and use onboarded data if properly integrated.

### Phase 3: Validate Data Consumption & Asset Generation
**Verification Points**:

#### A. Diagnostic Document Consumption
- Review `output/v4_complete/01_DIAGNOSTICO_Y_OPORTUNIDAD_*.md`
- Confirm all validation summary fields show REAL data (not ESTIMATED):
  - rooms: should show actual count from onboarding
  - adr_cop: should show actual ADR from onboarding
  - occupancy_rate: should show actual rate
  - direct_channel_percentage: should show actual percentage
- Verify coherence score calculation incorporates real data

#### B. Problem-to-Asset Mapping Validation
- In diagnostic document, identify all listed problems (Sections [ALERT], [STOP])
- Cross-reference with commercial proposal document (`02_PROPUESTA_COMERCIAL_*.md`) Section [CLIP]
- Verify each problem has:
  - A corresponding "Asset Generado" listed
  - Asset type that was actually generated in delivery_assets/
  - Non-empty confidence level (not UNKNOWN where avoidable)

#### C. Asset Data Source Verification
- Check asset directories in `output/v4_complete/hotel_vísperas/`
- For each asset type, verify:
  - File naming does NOT use "ESTIMATED_" prefix where real data was available
  - Metadata files show source as onboarded data or verified sources
  - Content reflects actual hotel characteristics (not generic placeholders)

#### D. Evidence Ledger Validation
- Review `output/v4_complete/v4_complete_report.json`
- Confirm evidence_coverage gate passed (≥ 95%)
- Validate that all claims in diagnostic/proposal have corresponding evidence entries

### Phase 4: Publication Gates Validation
**Requirements** (all must pass):
1. **hard_contradictions**: count = 0
2. **evidence_coverage**: ≥ 95%
3. **financial_validity**: no default values in money fields
4. **coherence**: score ≥ 0.8
5. **critical_recall**: ≥ 90%

**If coherence gate fails** (score < 0.8):
- Examine coherence gap analysis in report
- Verify every diagnostic problem has a justified asset solution
- Check that asset generation confidence meets requirements
- Document specific misalignments requiring attention

### Phase 5: Disconnection Audit
**Systematic Verification**:

#### Data Flow Check:
1. **Onboarded Data → Validation Summary**
   - Confirm onboarded YAML file is read and parsed
   - Verify validation summary reflects real values (not defaults)

2. **Validation Summary → Problem Detection**
   - Confirm PainSolutionMapper receives accurate validation data
   - Verify detected problems match actual hotel deficiencies

3. **Problem Detection → Asset Planning**
   - Validate each detected problem maps to appropriate asset types
   - Check confidence thresholds are properly evaluated

4. **Asset Planning → Asset Generation**
   - Confirm planned assets match generated assets
   - Verify generation conditions (confidence requirements) are met

5. **Asset Generation → Commercial Proposal**
   - Ensure proposal accurately reflects generated assets
   - Validate asset descriptions match actual generated content

**Disconnection Reporting**:
If any disconnections are found, document:
- Where the disconnection occurred (which phase transition)
- What data was expected vs. what was actually used
- Impact on downstream processes
- Recommended fix

## Success Criteria
- [ ] Onboarded data successfully captured and saved
- [ ] v4complete execution completed without errors
- [ ] Diagnostic document shows real data (not ESTIMATED) for all validation fields
- [ ] Every problem in diagnosis has a corresponding generated asset
- [ ] All generated assets use real hotel data where possible (minimize ESTIMATED assets)
- [ ] Coherence score ≥ 0.8 (publication gate passes)
- [ ] All publication gates pass (hard_contradictions=0, evidence_coverage≥95%, financial_validity=true, coherence≥0.8, critical_recall≥90%)
- [ ] No unverified disconnections identified in data flow audit

## Contingency Plans
### If Onboarding Data Not Used by v4complete:
1. Examine data flow between onboard module and v4complete orchestrator
2. Consider manually specifying input data if supported
3. Report issue as potential disconnection in data consumption

### If Coherence Score Below Threshold:
1. Review detailed coherence validation report
2. Identify specific problem-asset mismatches
3. Determine if additional assets needed or confidence thresholds need adjustment
4. Consider manual asset creation for critical gaps

### If Asset Generation Fails:
1. Check asset catalog for implementation status
2. Verify required validation fields have sufficient confidence
3. Review error logs for generation failures

## Execution Notes
This plan emphasizes verification over assumption. Each step includes explicit validation checkpoints to ensure the fundamental requirement of "no disconnections" is met. The focus is on establishing a closed-loop system where:
- Input data flows through all processing stages
- Each stage's output is consumed by the next
- Problems identified trigger appropriate solution generation
- Solutions are validated against real hotel characteristics
- Final output reflects accurate, consumable intelligence
