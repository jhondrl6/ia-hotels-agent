# 09 — Documentación Post-Proyecto

> **Plan**: ESTABILIZACION-PRE-TRIBUNAL-2026-09-03
> **Estado**: 🟡 EN CURSO — archivo creado **desde la concepción** del plan (executor §4). Se actualiza
> **incrementalmente al cierre de cada fase** (Post-Ejecución punto 3), NO al final.
> **Consumidor**: FASE-RELEASE lo lee para generar CHANGELOG y GUIA_TECNICA. Si este archivo está vacío,
> RELEASE produce documentación vacía.
> **Regla anti-reproceso**: cada fase vuela su propia fila; RELEASE solo consolida.

---

## Sección A — Módulos nuevos

> Módulos/archivos **creados** por el plan que no existían antes. Fuente: Post-Ejecución de cada fase.

| Fase | Módulo / archivo nuevo | Propósito | Estado |
|------|------------------------|-----------|--------|
| A | `modules/common/service_identity.py` *(el plan predecía `modules/asset_generation/service_asset_registry.py` — nombre y ubicación corregidos al cierre)* | Fuente canónica única de identidad servicio↔asset↔pain (Capa 2). `modules/common/` no importa nada del proyecto ⟹ lo consumen `asset_generation`, `commercial_documents` y `financial_engine` sin ciclo | ✅ 2026-09-03 |
| A | `tests/common/test_service_identity_registry.py` *(el plan predecía `tests/asset_generation/test_canonical_registry_contract.py`)* | **21 funciones test / 37 casos parametrizados**, en 6 secciones: AC1 integridad del canónico, AC1/V2 cero IDs fantasma, V3 biyección asset↔pain, guardián AST «derivar no copiar», AC2/V14 narrativa↔fuente, validación de registros no derivados contra Capa 1 | ✅ 2026-09-03 |
| A | `evidence/FASE-A/` (`censo-registros.md`, `tdd-contract-tests-ROJO.txt`, `tdd-contract-tests-post-canonico.txt`) | Censo de los 14 registros + la curva TDD completa (27 fallados → 37 pasados) | ✅ 2026-09-03 |
| B | `tests/commercial_documents/test_pain_map_bijection.py` | Guardián AST **tridireccional** de la biyección mapa↔emisión↔narrativa (patrón SR-A, no regex). **11 funciones / 28 casos**. Fija: capa narrativa **total** sobre Capa 1 (sonda conductual que llama a `_pain_to_brecha`), emisiones ⊆ Capa 1, y la partición **del lado de la emisión** `Capa 1 = emitidos ⊎ PAINS_DIFERIDOS` (6 diferidos con motivo + seguimiento). Incluye el guardián L-NC4 («la narrativa derivada sale de Capa 1, no de una tabla paralela») y el de S14 («el peso de impacto no vive de un default mudo») | ✅ 2026-09-03 |
| B | `tests/commercial_documents/test_detect_pains_emisiones_faseB.py` *(no previsto en el plan)* | **18 tests** (9 negativos) sobre las 3 ramas emisoras nuevas + el retiro de `no_ga4_enhanced`. Los negativos fijan «vacío vs ausente»: clave `llms_txt` presente y == 0, `images_without_alt > 0`, y `confidence == "high"` para excluir la ruta de excepción del detector | ✅ 2026-09-03 |
| B | `evidence/FASE-B/` — **13 archivos nuevos**: `decision-pains-muertos.md`, `faseB_narratives_audit.py` **re-ejecutable**, `faseB_bijeccion_audit.txt`, `tdd-candado-ROJO.txt`, `faseB_iteraciones.txt` (re-medición de las iteraciones de B con el instrumento de D: **345** al corte del commit `e6d28b8`, corrige los 151 publicados) + 8 logs de tests/validaciones | B1 (las 11 decisiones motivadas) + la medición conductual del delta. El script importa los helpers del candado ⟹ evidencia y candado miden **igual**. ⚠️ **El directorio ya existía** con 7 archivos de la FASE-B de **otro plan** (commit `d2a9700`, «tabla de servicios dinámica desde opportunity_scores»): `fase_b_*.txt` y `verify_breach_consistency_static*`. Los de este plan usan el prefijo `faseB_*` (camelCase). Colisión de nombres por fase-letra → VERIFY debe distinguirlos | ✅ 2026-09-03 |
| C | `tests/asset_generation/test_fase_c_propuesta_dinamica.py` | Candado del punto 8: **14 funciones / 17 casos** en 4 secciones — complementos siempre-activos fuera del denominador de justificación (derivados del registro, no listados a mano; y el caso con dientes: un asset sin `pain_id` que **no** sea complemento **sigue** restando), la matriz solo promete con brecha o presencia (`no_breach == 0`, `actionable_total == promised_services_total`, `not_promised` visible y serializado), «vacío ≠ ausente», y **anti-A5**: los dos builders producen los **mismos** pares `(servicio, status)` en 4 variantes + el skip de servicio desconocido observable por `caplog`. Escrito **antes** de tocar producción (rojo capturado) | ✅ 2026-09-03 |
| C | `evidence/FASE-C/faseC_contrafactual.py` (+ `.json`) | **Re-ejecutable**: reprocesa el corpus **real** de Salento Real (`output/FASE-D_salentoreal_post_guard/`, run `2026-08-31T12:28:03`) con el código vivo, llamando a las funciones de producción (`PainSolutionMapper.map_to_solutions`, `V4AssetOrchestrator._solutions_to_asset_specs`, `CoherenceValidator._check_assets_are_justified`, `AssetAlignmentMatrix.build`, `AlignmentResult.from_asset_alignment_matrix`). No es fixture: es el delta AC5/AC6 medido | ✅ 2026-09-03 |
| C | `evidence/FASE-C/contrato-propuesta-dinamica.md` | C1: el contrato escrito antes de implementar — las 3 superficies de promesa, la semántica de la partición, «vacío ≠ ausente», y la frontera explícita `C1 DEFINE, G4 IMPLEMENTA` para la escotilla V9 | ✅ 2026-09-03 |
| C | `evidence/FASE-C/` — **20 archivos** (`delta-medido.md`, `contrato-propuesta-dinamica.md`, `faseC_contrafactual.py/.json`, `faseC_antes.txt`, `faseC_despues.txt`, `tdd-colecta-ROJO.txt`, `tdd-comportamiento-ROJO.txt`, `tdd-run1.txt`, `tdd-run2.txt`, `suite1-3.txt`, `suite_final.txt`, `suite_resto.txt`, `commercial_selectivo.txt`, `faseC_baseline_antes.txt`, `faseC_alignment7_antes.txt`, `validaciones_quick.txt`, `faseC_iteraciones.txt`, `faseC_dynamic_verde.txt`, `faseC_confidence_disclosure_SB12.txt`) | Curva TDD completa + baselines + el delta medido + las **142 iteraciones** al corte del commit de código. ✅ **Sin colisión de nombres**: a diferencia de `evidence/FASE-B/` y `evidence/FASE-D/`, este directorio **no existía** antes de este plan — `git log --all -- evidence/FASE-C/` devuelve solo `c1bf5e2` y `552c190`. Verificado, no asumido | ✅ 2026-09-03 |
| E | `modules/asset_generation/site_presence_adapter.py::save_site_presence_snapshot` *(el plan predecía un módulo nuevo `site_presence_writer.py` — corregido al cierre: el writer vive en el adaptador existente, no se creó archivo)* | Mitad disco de DT4-N2: persiste `site_presence_snapshot` **tal cual** ya propagado (passthrough puro — sin `normalize_site_presence`, verificado por test sonda de campos de probe). Envoltorio `{"snapshot_version": "1.0", "snapshot": ...}`, UTF-8 explícito, un solo archivo por corrida: `v4_audit/site_presence_snapshot.json` | ✅ 2026-09-03 |
| E | `tests/test_site_presence_persistence.py` *(no previsto en el plan)* | **5 tests**: writer escribe con versión; **no-reconstrucción** (campos de probe sobreviven top-level y dentro de `results` — una normalización no puede colarse en silencio); UTF-8 sin escapes `\u`; `normalize_site_presence(None)` camino de snapshot vacío; el snapshot persistido alimenta `_presence_exists`/`_presence_resolved`/`committed_services_from_entries` | ✅ 2026-09-03 |
| E | `tests/quality_gates/test_delivery_asset_path.py` *(no previsto en el plan)* | **5 tests** sobre A6: LINKED con `asset_path` poblado vía `AssetAlignmentMatrix.build` (dicts con forma del caller real, ledger `[{"pain_id": "ai_crawler_blocked", ...}]` → `llms_txt`); `path=None` se queda null; MISSING_ASSET null; **forma pre-E (sin clave `path`) null** (retrocompatibilidad); extremo E2E — `DeliveryQualityReportGenerator().generate(...)` → `proposal_asset_gate.passed=True` con la ruta real persistida en el JSON | ✅ 2026-09-03 |
| E | `evidence/FASE-E/` — 6 archivos (`consumidores-snapshot.md`, `faseE_persist.txt`, `faseE_baseline_antes.txt`, `faseE_baseline_despues.txt`, `faseE_validaciones.txt`, `faseE_iteraciones.txt`) | Censo E4 de consumidores del snapshot (6 activos + 1 ruta viva de re-verificación + 3 puntos muertos heredados → insumo F1) + curva de tests + baselines 892→897 + validaciones 7/7 + **72 iteraciones medidas** al corte del commit de código `b0ab27a`. ✅ **Sin colisión de nombres**: el directorio **no existía** antes de este plan (`git log --all -- evidence/FASE-E/` = solo `b0ab27a`), verificado como se hizo con C | ✅ 2026-09-03 |
| D | `tests/quality_gates/test_gate_severity_lists.py` *(el plan predecía `test_gate_severity.py` — nombre corregido al cierre)* | **8 funciones** que fijan la estructura 11 blocking + 2 advisory: `asset_confidence` no es advisory, los advisory son **exactamente** 2, las listas suman 13 y son disjuntas, el registro `self.gates` coincide con la unión, **guardián AST** contra el `not r.passed` plano en `check_publication_readiness`, contra una segunda copia del criterio y contra un **cuarto régimen** de severidad en `modules/quality_gates/*.py` (exonera `delivery_quality_report.py` y `commercial_gate.py`), y que los docstrings no prometan el régimen antiguo | ✅ 2026-09-03 |
| D | `evidence/FASE-D/faseD_contrafactual.py` (+ `.txt`) | **Re-ejecutable**: reconstruye los 13 veredictos de la corrida real de SalentoReal y aplica el criterio viejo vs el nuevo ⟹ **0 flips de `ready`**, 2 advisories antes silenciosos ahora divulgados. Es la prueba de que D **no relajó** ningún veredicto | ✅ 2026-09-03 |
| D | `evidence/FASE-D/measure_iterations.py` | Cierra en parte la brecha que señaló B («no hay contador instrumentado de iteraciones, cada fase se auto-reporta»). Cuenta ids de mensaje de asistente únicos + `tool_use` sobre un transcript `.jsonl`, con corte opcional por timestamp ⟹ la cifra de iteraciones de una fase es **auditable y comparable**. Misma unidad con la que B y D publicaron sus números | ✅ 2026-09-03 |
| D | `evidence/FASE-D/` — 8 archivos de salida (`faseD_baseline_{pre,post}.txt`, `faseD_severity_lists.txt`, `faseD_publication_gates.txt`, `faseD_validations.txt`, `validate_agents_md.txt`, `faseD_contrafactual.txt`) | Baseline 848 → **872** con delta explicado (24 tests propios), `-v` del candado de severidad, 69+1 del archivo de gates, `run_all_validations.py` 7/7 y `validate_agents_md.py` 6 PASS / 0 FAIL. ⚠️ **Un artefacto llegó fósil al commit de código**: `faseD_severity_lists.txt` se capturó a mitad de fase con **18** tests y el archivo final tiene **8** — el log **no reproducía** el candado que decía probar. Regenerado en el cierre documental. Lección: capturar los logs **después** de estabilizar los tests, no antes. ⚠️ El directorio **ya existía** con 6 archivos `fase_d_*` (snake_case) de la FASE-D de **otro plan** (commit `04fe193`) — misma colisión por fase-letra que registró B; VERIFY debe distinguirlos | ✅ 2026-09-03 |
| G | `tests/auditors/test_v4_comprehensive.py` *(no previsto en el plan — el plan asignaba G2 a `pain_solution_mapper.py`, predicción falsa)* | **16 funciones** en 3 clases sobre el detector real: `TestPageSpeedErrorCriterion` (PageSpeed `status=ERROR` no cubierto → issue; cubierto → sin issue), `TestGeoBandCriticalCriterion` (banda `critical` con fixture SalentoReal 29/100; dict u objeto `to_dict`; umbral `GEO_CRITICAL_MAX_SCORE = 35`; issues duplicados no se añaden dos veces), `TestLegacyCriteriaIntact` (los criterios pre-G no retroceden) | ✅ 2026-09-04 |
| G | `tests/quality_gates/test_coverage_gate_v5.py` *(no previsto en el plan)* | **14 funciones** en 2 clases sobre el gate REAL: `TestV5Escotilla` — generado+mencionado cubre, **generado silencioso FAILED**, huérfanos SalentoReal (`indirect_traffic_optimization`, `analytics_setup_guide`) → value 0.0, **anti-reversión BUG-6** (`ASSET_GENERATED` ∈ `_JUSTIFIED_STATUSES`), VERIFIED_IN_SITE silencioso pasa, otros estatus justifican, caso mixto; `TestV9LedgerVacio` — 4 combinaciones {fallback,resolved}×{vacío,ausente} + dict con `entries` vacías → BLOCKED | ✅ 2026-09-04 |
| G | `evidence/FASE-G/` — **9 archivos** (`faseG_v5.txt`, `faseG_v5b.txt`, `faseG_gates.txt`, `faseG_baseline.txt`, `faseG_integration.txt`, `faseG_validations.txt`, `faseG_iteraciones.txt`, `faseG_anti_reveresion_fixture.py` + `.json`) | Curva de tests + baseline 920→944 + validaciones 7/7 + **154 iteraciones medidas** al corte del commit de código `c317cda`. El **fixture anti-reversión es re-ejecutable y corre el gate REAL** (no un mock): 4 aserciones en verde (passed, justified=1, uncovered vacío, `ASSET_GENERATED` presente en la lista). ✅ **Sin colisión de nombres**: prefijo `faseG_*` camelCase; el directorio no existía antes de este plan | ✅ 2026-09-04 |
| H | `modules/common/performance_status.py` *(no previsto en el plan)* | **Archivo nuevo de V11 — única definición del criterio «¿el eje PageSpeed quedó medido?»**: `PERFORMANCE_OK_STATUSES = {OK, SUCCESS, VERIFIED, LAB_DATA_ONLY}` + `is_performance_api_unavailable()`. Existe porque el **documento** (`v4_diagnostic_generator.py`) y el **audit** (`v4_comprehensive.py`) decidían ese criterio cada uno por su cuenta — la deriva de doble fuente que denuncia el dossier (familia H10/A1). Vive en `modules/common/` por la misma razón que el canónico de FASE-A: **cero imports del proyecto ⟹ consumible sin ciclo**. Lista **blanca** a propósito: vacío, `None` y cualquier valor desconocido caen del lado «no evaluable» (vacío ≠ ausente, lección SR-H2) — que es justo el error que cometía el texto de D6 al asumir «sitio nuevo». 27 líneas | ✅ 2026-09-04 |
| H | `evidence/FASE-H/` — 9 archivos (`README.md`, `faseH_baseline_pre.txt`, `faseH_baseline.txt`, `faseH_units.txt`, `faseH_contracts.txt`, `faseH_validations.txt`, `faseH_conteos.txt`, `faseH_code.diff`, `faseH_v11_textos.diff`) **+ `faseH_iteraciones.txt`, que añade el parent al commitear** | Baseline **antes y después** (944/2 → 944/2, con la anotación de que el «848/2» del prompt estaba desactualizado), la batería de units de la fase (**188 passed / 0 failed**), los contratos A/B (**83 passed**), `run_all_validations.py --quick` (**7/7**), los conteos por archivo, **el diff completo de código y tests** y una **captura enfocada de los textos V11** en los dos archivos (la prueba de que se barrió hasta la fuente, no solo el documento). `.env` verificado **sin cambios** (`git diff --stat` vacío) | ✅ 2026-09-04 |
| … | *(agregar filas si una fase crea archivos no previstos)* | | |

**Nota**: los nombres exactos los fija cada fase en su implementación; esta tabla se corrige al cierre de
fase con el nombre real. Lo importante para RELEASE es la **lista consolidada de archivos nuevos**.

---

## Sección B — Funcionalidades nuevas

> Comportamiento **nuevo o modificado** que el sistema gana con el plan.

| Fase | Funcionalidad | Hallazgo del dossier que cura | Estado |
|------|---------------|-------------------------------|--------|
| A | Fuente única de identidad servicio↔asset↔pain en `modules/common/service_identity.py`, en **dos capas** (Capa 1 = `PAIN_SOLUTION_MAP` 27 pains como universo de pain_id, intacto; Capa 2 = `SERVICE_IDENTITIES` 8 entradas). De los **14** registros censados (el dossier decía ≥9): **6 derivados** del canónico, **6 validados** contra Capa 1 con razón registrada, 2 fuera de alcance. Drift «8 vs 7» disuelto **eliminando sus 3 copias**, no comparándolas. Perla `monthly_report → no_faq_schema` eliminada. 6 IDs fantasma + 1 asset fantasma corregidos sin cambio de comportamiento (contrafactual medido) | V2, V3, V14 (§12.3); causa raíz §12.5 (≥9 registros); deuda P10 | ✅ 2026-09-03 |
| B | Biyección **triple** mapa↔emisión↔narrativa. Lo que gana el sistema: (1) **`_pain_to_brecha` ya no descarta en silencio** — el `return None` se reemplazó por una **derivación de Capa 1**, así que la capa narrativa es **total (26/26)** y no se escribió ni una entrada a mano (L-NC4: el dict literal sigue en 16); (2) **3 pains ganan señal verificable** (`missing_llmstxt` vía sonda HTTP de `ia_readiness`, `missing_alt_text` y `no_social_links` vía `seo_elements`), cada uno con guard «vacío vs ausente» para no emitir falsos positivos cuando el detector falló; (3) **`no_ga4_enhanced` se retira** de Capa 1 y de `asset_catalog` (guardia insatisfacible: `is_enhanced` no existe en el repo) ⟹ Capa 1 **27 → 26**; (4) **6 pains se difieren con decisión registrada** (`PAINS_DIFERIDOS` en el candado, con motivo y seguimiento S-B1…S-B6). Delta neto: emisiones 18 → 20, **DESCARTE REAL 2 → 0** | V1 (9 pains muertos) + N-A1 (2 que se emitían y se descartaban) = **11**; §3 candado de biyección. ⚠️ La premisa de N-A1 resultó **falsa** (S-B7) | ✅ 2026-09-03 |
| C | **Punto 8 — propuesta dinámica**. Lo que gana el sistema: (1) **una sola función decide qué se promete** — `classify_promised_services()` en `proposal_asset_alignment.py`, consumida por los **dos** builders de la matriz, así que el drift A5 (dos rutas de skip silencioso idénticas) deja de ser posible en vez de quedar parcheado dos veces; (2) **un servicio sin brecha mapeada y sin presencia verificada ya no se promete**: pasa a `not_promised`, en vez de emitirse como `NO_BREACH` e inflar el denominador ⟹ `no_breach = 0` **por construcción** y `total == actionable` como **identidad estructural**, no como una resta que se auto-anula; (3) **el skip dejó de ser silencioso**: `not_promised` y `unknown_services` se publican en `proposal_asset_matrix.json` (`summary = {promised, not_promised, unknown}`) y el servicio fuera del registro canónico emite `logger.warning`; (4) **la presencia entra en la partición** (`site_presence_report` es parámetro de ambos builders), así que un servicio comprometido por presencia **nace** `PRESENT_IN_PRODUCTION` en la matriz en vez de nacer `NO_BREACH` y ser re-clasificado después por el DTO; (5) **los complementos siempre-activos salen del denominador de justificación**: `ALWAYS_ACTIVE_COMPLEMENT_ASSETS` se **deriva** del registro (`counts_in_alignment=False`) y `assets_are_justified` deja de contar `monthly_report` como promesa por brecha — el complemento **sigue generándose**, sólo deja de contarse; (6) **«vacío ≠ ausente»** queda como contrato en 3 sitios: `None` → catálogo estático legacy, `[]` → 0 comprometidos. **Delta medido** (corrida real de Salento Real): `no_breach` **6→0**, `promised_services_total` **7→1**, `assets_are_justified` **0.75/error → 1.0/info**, `overall_score` **0.88 → 0.9133**, `errors` **[1] → []**, **`is_coherent` False → True** con el **umbral 0.8 intacto** y `_coherence_gate` sin tocar. ⚠️ **La premisa B5 del dossier era falsa**: la causa del `is_coherent=false` estructural **no** era `promised_assets_exist` (pasa en 1.0 antes y después) sino `assets_are_justified = 0.75` por un asset con `pain_ids=[]` cuyo `any()` es **siempre** False ⟹ fallaba en **toda** corrida, cualquier hotel | §9.2 B1-B5; tautología de coverage; `is_coherent = false` estructural; A5 (skip silencioso); A3/P12 (documentado, no usado como apoyo); V15/V16 (premisa modificada) | ✅ 2026-09-03 |
| D | **Severidad explícita de gates**: el régimen de publicación pasa de «los 13 bloquean» a **11 blocking + 2 advisory** decidido por **un único predicado** `gate_blocks_publication()`. Lo que gana el sistema: (1) **una fuente** (`BLOCKING_GATE_NAMES`/`ADVISORY_GATE_NAMES`) con **fail-fast en `__init__`** — añadir un gate sin declarar su severidad ahora revienta al instantiar, no en producción; (2) **piso por naturaleza del fallo**: un advisory degrada a blocking bajo su corte estructural (`content_quality` con `details["blockers"]`; `proposal_asset_alignment` con `value < PROPOSAL_ASSET_ALIGNMENT_FLOOR = 0.8`) y **un gate que no se ejecutó siempre bloquea** (`GATE_EXECUTION_FAILED_KEY`), así que «advisory» no puede volverse «inocuo por fallo de infraestructura»; (3) **divulgación**: `summary["advisory_issues"]` llega al `human_checklist.md` vía el nuevo parámetro `advisory_issues` de `HumanChecklistGenerator.generate()`, antes los fallos advisory eran invisibles para el humano; (4) `content_quality` con solo warnings pasa de `PASSED` a `WARNING`, el estado que ya existía y nadie producía. **`asset_confidence` sigue bloqueando** (dossier §8.2: es el único mecanismo que vuelve no-entregable un paquete Tier C con 100 % assets ESTIMATED — ~37 % del corpus). ⚠️ **Medido**: contrafactual sobre las 2 corridas reales disponibles ⟹ **0 flips de `ready`** y **2 advisories nuevos divulgados** ⟹ la fase **no relajó veredictos**, corrigió el mecanismo y la visibilidad | H10; §8.1 (cuatro regímenes), §8.2 (asset_confidence medido), §8.4; docstrings 10+3 vs código 13 | ✅ 2026-09-03 |
| E | **A2 — oráculo de presencia auditable**: `site_presence_snapshot` se persiste en `v4_audit/site_presence_snapshot.json` (writer passthrough en `site_presence_adapter.py`, versionado, llamado desde `main.py` tras crear `v4_audit_dir`). Lo que gana el sistema: el dato que decide `present_in_production` → `no_breach`/`unresolved`/`coverage_ratio`/G9 deja de ser **inauditable post-hoc** — cualquier revisor (tribunal §10#2) puede abrir el JSON del run y contrastar lo que los gates afirmaron contra lo que el sitio respondió. **Sin reconstrucción**: el writer serializa el snapshot ya propagado por DT4-R2 (test sonda de campos de probe impide que una normalización se cuele). **A6 — `asset_path` deja de ser `null` para LINKED**: causa raíz en el **caller** de `main.py` (`assets_for_quality` construía dicts sin clave `path`; la matriz ya la poblaba bien) — 1 línea, con la rama fallback de `asset_plan` deliberadamente sin rutas (no se inventan). ⚠️ **Desviación de §4**: E1/E2 se ejecutaron **secuenciales**, no en paralelo — ambos tracks editan `main.py` (L-D1/S-B15). **Censo E4** (`consumidores-snapshot.md`): 6 consumidores activos del snapshot, 1 ruta viva de re-verificación (`conditional_generator.py:64/:111`), 3 puntos muertos heredados (bloques `presence_lookup` de `v4_proposal_generator.py:1389-1396`/`:1618-1625`/`:1686-1693`, instanciación muerta `v4_asset_orchestrator.py:240`) + **NameError latente** `main.py` (`site_presence_report` solo existe si `generate_proposal=True`) → insumo duro para F1/A4. Baseline 892→**897** (10 tests), validaciones 7/7 | A2, A6 (§9.1); H7 (criterio canónico preservado, sin tocar); DT4-N2 mitad disco | ✅ 2026-09-03 |
| F | **Tres cierres de doble-fuente-de-verdad**. (1) **A4 — oráculo único de presencia**: `verify_proposal_asset_alignment` ya no clasifica con su propio `== "exists"` — narra con el **mismo criterio canónico** `is_present_in_production()` (`PRODUCTION_PRESENT_STATUSES`, FASE-SR-E H7/L-SR3 **intacto**: cambió quién lo consulta, no el criterio) con el que deciden `AlignmentResult`/`AssetAlignmentMatrix`/`committed_services_from_entries`; el veto FASE-12B se extiende a `exists_with_issues`; `details.missing_count` == conteo de la matriz (igualdad oráculo↔narrador fijada por test con presencia `exists_with_issues`). El cross-reference de `AlignmentResult` (V15) **se conserva** para artefactos pre-C (decisión registrada DA-F2). (2) **A1 — `skipped ≠ passed`**: los **2 defaults G9 independientes** de `delivery_quality_report.py` unificados en `_not_evaluated_g9()` — estado `NOT_EVALUATED` distinto de passed/failed, `summary` distingue `passed/failed/not_evaluated`, y el skip deja de ser invisible: «G9: NOT_EVALUATED» entra a `human_review_items`. No bloquea (régimen delivery intacto, H10). (3) **N11/P9 — el gate respeta el veredicto**: `coherence_verdict_passes()` (`coherence_gate.py`) es la **única definición** del veredicto — `is_coherent=None` es vacío ≠ ausente (legacy por score intacto), solo `False` explícito bloquea; consumida por `publication_gates._coherence_gate` **y** por `CoherenceGate` (legacy + validator, donde un veredicto negativo baja `CERTIFIED→REVIEW` con `can_certify=False`). **Umbral 0.8 intacto.** `assessment_builder` propaga `is_coherent` desde la **misma fuente** del score (`final_coherence_report`, DT4-N4). **H8**: `publication_state.py` (675 líneas) **eliminado** — 0 importers verificados; `coherence_gate.py` es el conectado. **F4 — corpus re-evaluado sin correr v4complete** (barrido de `output/` + `archives/outputs/`): 28 corridas primarias + 4 copias de delivery; **0 corridas liberadas**; **4 veredictos de paquete READY→NOT_READY** — todas con `final_coherence_report.is_coherent=False` persistido, incl. la repro del dossier (SalenteReal FASE-D, 0.88/False); las ~11 ESTIMATED (con `coherence_score_final=None`) **siguen bloqueadas** en ambos mundos (`asset_confidence` intacto) ⟹ el único volteador es F3 y su dirección es segura. 23 tests nuevos (5 anti-A4 + 7 veredicto + 6 delivery + 5 gate) + 2 reescritos al contrato nuevo; baseline 897→**920**, validaciones 7/7 | A4, A1 (§9.1); V15; V16; N11/P9 (deuda más grave); H8, H9-parcial | ✅ 2026-09-03 |
| G | **Ceguera de gates cerrada (NR1-NR4)**. (1) **`doc_audit_consistency` llega con datos**: `AssessmentPayload.audit_data` (crudo, poblado por `with_audit_data(audit_result.to_dict())` en `main.py`) — el gate ya no cae en «No audit data available» PASSED con value=null; datos ausentes → **`NOT_EVALUATED`** con `state_reason` (coherente con A1: no bloquea, no cuenta como pasado, visible en `summary["not_evaluated"]`), contradicciones reales → **FAILED** (patrones `og_missing_vs_present`, `performance_error_vs_new_site`), Check 2 acepta `gbp.reviews` como `int` (986) o `float`, además del dict legacy. (2) **`critical_recall` deja de ser vacuo**: `_identify_critical_issues` califica PageSpeed `status=ERROR` no cubierto y banda GEO `critical` (29/100, helper módulo-level `geo_band_critical_issue` compartido con `AssessmentBuilder.with_geo_flow()` que carga `v4_audit/geo_flow_result.json`); SR-H2 queda **condicionado** — el atajo favorable (lista vacía + audit ejecutado → 1.0) solo opera si `performance.status != "ERROR"`; con el eje caído y nada reportado, recall = 0.0 y el gate BLOCKEA. (3) **Escotilla V5 sin revertir BUG-6**: `ASSET_GENERATED` SIGUE en `_JUSTIFIED_STATUSES`; el estrechamiento vive en la **regla de mención** del loop de coverage (generado + mencionado ⟹ covered; generado silencioso ⟹ `is_justified=False`) y el caso «existe en producción y basta» es `VERIFIED_IN_SITE` (primera clase FASE-P1-D), que justifica sin mención — fixture anti-reversión que corre el gate REAL en `evidence/FASE-G/`. **El gate NO re-detecta brechas** (DT4-N2). (4) **Escotilla V9**: `_coverage_gate` decide sobre el ledger **normalizado** — `resolved` vacío + `original` no vacío → BLOCKED (`coverage_basis=reconciler_dropped_entries`), vacío legítimo → PASSED con traza `ledger_present_zero_entries` + `reconciler_ran`, `None` → fallback (vacío ≠ ausente, DA-C3 intacto). ⚠️ **`pain_solution_mapper.py` NO se tocó** (predicción de §3 falsa — G2 vive en `v4_comprehensive.py`) ⟹ H1/H3 sin orden forzoso. Baseline 920→**944**, validaciones 7/7 | §12.5 Nivel 3.7; V5, V9, V10 (confirmado); NR1-NR4 | ✅ 2026-09-04 |
| H | **Seis quirúrgicos del Nivel 3.8 — el sistema gana que tres formas de «no lo evaluamos» dejen de leerse como «no hay nada que decir»**. (1) **V7**: `low_ota_divergence` (high, priority 1) **puede disparar** — el guard `hasattr(direct_field.value, '__iter__')`, que era insatisfacible con un número, se reemplazó por validación numérica con **normalización de unidades** (`_normalize_to_fraction`: `0.2`, `20`, `"0.2"`, `"20"` y `"20 %"` significan todos 20 %; desempate por valor > 1 ⟹ porcentaje) más `_describe_ota_presence`. `ota_presence` entró como **evidencia no bloqueante** del description y **no** como guard, porque `main.py` no lo registra en el `ValidationSummary` y como guard el pain habría vuelto a ser código muerto; el `isinstance(...)` muerto desapareció como rama inalcanzable y `int(direct_pct*100)` → `round(...)`. Como la capa narrativa ya era total (FASE-B), la brecha **aparece en el documento** sin editar una segunda tabla. (2) **V8**: `low_organic_visibility` **ya no se emite dos veces** — se calculan primero las dos señales (`not ga4_available`, `organic_measured_low`) y hay **un solo punto de emisión** que, cuando ambas aplican, **conserva el dato medido** (sesiones/umbral) además del motivo «sin analytics»; severidades, nombres y `detected_by` intactos. (3) **V6**: el `except Exception` de `_identify_brechas` deja de devolver una lista vacía **en silencio**: loguea **traceback**, fija `BRECHAS_STATE_NOT_EVALUATED` (estado **por corrida**, reseteado en `generate()`) y **deja de escribir el caché**; el render lo dice — `_FUGAS_NOT_EVALUATED_NOTICE` y el título `FUGAS PRINCIPALES (NO EVALUADAS)` — y el helper `_brechas_detection_failed()` se aplica también a `_build_brechas_section` y `_build_brechas_resumen_section`, que seguían afirmando «No se detectaron brechas criticas» con el detector caído. (4) **V11** en **tres** frentes: la tabla de atención manual **renderiza como tabla** (`MANUAL_ATTENTION_TABLE_HEADER`, construida por `_build_manual_attention_table`, no por el template, para que el conteo de columnas no se desincronice — el fallback de GBP pasó de 4 a 3), la rama de performance distingue «la API no respondió» de «el sitio no tiene datos» (`is_performance_api_unavailable`: `API_NOT_CONFIGURED`/vacío/`None`/desconocido → 🔴 Alta con el `message` real; `OK`/`SUCCESS`/`VERIFIED`/`LAB_DATA_ONLY` → 🟡 Media), la **fuente** deja de mandar al cliente la causa mal atribuida (`sanitize_pagespeed_message` + `_generate_recommendations` leyendo `status`/`message` reales — porque `extract_top_problems` vierte esas recomendaciones al diagnóstico) y `disjoint_executed_validators()` hace `executed` ∩ `skipped` = ∅ **por construcción**. El criterio compartido queda en **un solo archivo**: `modules/common/performance_status.py`. (5) **V13**: ya no hay dos `MetadataValidator` — el gemelo fue **borrado físicamente** (`git rm`, 219 líneas) con autorización del usuario en la misma sesión, sus dos consumidores apuntan al vivo y la guardia fija la **ausencia** de la segunda implementación. (6) **V12**: **medido y documentado, sin editar `.env`** → ver la **nota OPS** al final de la Sección E. ⚠️ Lo que H **no** hace: no propaga el fallo de detección a gates ni al `human_checklist`, no cierra el punto (a) de la capa de pain → **S-H2/S-H4…S-H16** en `10-analisis` §5. Baseline **944/2 intacto**, contratos A/B **83 passed**, validaciones **7/7**, **188 passed / 0 failed** en la batería de la fase | §12.5 Nivel 3.8; V6, V7, V8, V11, V12, V13; caídas §4 #1 (parcial), #6, #7, #8 | ✅ 2026-09-04 |

---

## Sección C — Ejecución E2E (FASE-I)

> Resultado certificado de la **única** corrida `v4complete` del plan, sobre Hotel Salento Real.
> La llena FASE-I y la certifica FASE-VERIFY. **Valores reales, no de fixture.**

| Campo | Baseline (FASE-D 2026-08-31 12:28) | Corrida FASE-I | Delta | AC relacionado |
|-------|------------------------------------|----------------|-------|----------------|
| `no_breach` (proposal_asset_matrix) | 6 | *(pendiente)* | *(pendiente)* | AC5 |
| `coverage_ratio` | 1.000 (tautológico) | *(pendiente)* | *(pendiente)* | AC6 |
| `is_coherent` (3 artefactos / 6 copias) | `false` | *(pendiente)* | *(pendiente)* | AC6, AC12 |
| `coherence` score | 0.88 | *(pendiente)* | *(pendiente)* | NR2 |
| Gates ejecutados | 13 | *(pendiente: 11+2)* | *(pendiente)* | NR3 |
| `site_presence_snapshot` en disco | **ausente** | *(pendiente: presente)* | *(pendiente)* | AC9 |
| `asset_path` en entradas LINKED | `null` | *(pendiente)* | *(pendiente)* | AC9 |
| G9 (delivery_quality_report) | `skipped` contaba como `passed` | *(pendiente: NOT_EVALUATED)* | *(pendiente)* | AC11 |
| ZIP de delivery | generado | *(pendiente)* | *(pendiente)* | NR4 |

**Anomalías preexistentes** (no cuentan como regresión): *(FASE-I las clasifica; VERIFY las confirma)*.

---

## Sección D — Métricas acumulativas

> Se actualiza al cierre de **cada** fase. RELEASE cierra con el total final.

| Métrica | Valor inicial | Valor actual | Última fase que actualizó |
|---------|---------------|--------------|---------------------------|
| Tests totales (`def test_`) | 3,689 | **3,899** atribuibles al plan (**A 21 + B 29 + C 14 + D 24 + E 10 + F 23 = 121 funciones** + 3 netos de C + **G 40 netas** + **H 46 netas** = 210 funciones netas). Medición 2026-09-04 al cierre de H (árbol de trabajo): `grep -rE "^\s*def test_" tests --include=*.py \| wc -l` = **3,899** vs **3,853** en `HEAD` (post-G). **Delta H = +46 funciones / +74 casos** por archivo: `test_pain_solution_mapper.py` 5→**21** func (5→46 casos), `test_diagnostic_brechas.py` 42→**52**, `test_diagnostic_generator.py` 27→**36**, `test_metadata_validator.py` 22→**23**, `tests/auditors/test_v4_comprehensive.py` 16→**26** func (+13 casos), `test_hotel_visperas_regression.py` **intacto**. Aritmética verificada: 3,853 (post-G) + 46 = **3,899**. Archivos: **293** `.py` en `tests/` (**H no creó archivos de test nuevos**). ⚠️ `AGENTS.md`/README documentan **3,689** ⟹ desfase de **+210** pendiente de **S11/RELEASE** | H |
| Tests quality_gates + asset_generation | 848 passed / 2 skipped | **944 passed / 2 skipped — SIN cambio tras H**. H no tocó ninguno de los dos directorios (sus tests viven en `tests/commercial_documents/`, `tests/data_validation/`, `tests/auditors/` y `tests/` raíz), así que el contador quedó idéntico al cierre de G ⟹ **0 regresiones** y delta de la fase = **0** en esta métrica (`evidence/FASE-H/faseH_baseline_pre.txt` = `faseH_baseline.txt` = 944/2). ⚠️ **El «848/2» que citaba el prompt de FASE-H estaba desactualizado**: es el baseline **pre-plan**, no el vigente — la fase lo re-verificó en su propio árbol antes de aceptar nada (S26: un conteo literal no puede ser invariante de un plan que agrega tests; lo único que funciona es medir el pre en el árbol propio). Árbol ancho: sigue el **1 fallo preexistente y ajeno** `tests/test_diagnostic_geo_metrics.py::test_diagnostic_includes_geo_metrics` → **S-H16** | H |
| Contract tests agregados | 0 | **170 funciones** (sin cambio por H): A = 21/37 (`tests/common/test_service_identity_registry.py`) + B = 29/46 (`test_pain_map_bijection.py` 11/28 + `test_detect_pains_emisiones_faseB.py` 18/18) + C = 14/17 (`test_fase_c_propuesta_dinamica.py`) + D = 24/24 (`test_gate_severity_lists.py` 8 + `TestFASEDGateSeverity` 12 + `TestAdvisoryDisclosureFASED` 4; sin parametrizar) + E = 10/10 (`test_site_presence_persistence.py` 5 + `test_delivery_asset_path.py` 5) + F = 23/23 (anti-A4 en `test_alignment_result.py` 5 — fixture canónico `normalize_site_presence` + igualdad oráculo↔matriz; `TestFaseFCoherenceVerdictPasses` 7 en `test_coherence_gate.py` — única definición del veredicto; `test_delivery_quality_report.py` 6 — `NOT_EVALUATED` visible y único default; `TestFaseFCoherenceRespetaIsCoherent` 5 en `test_publication_gates.py` — repro 0.88/False) + G = **49** (16 detector + 14 V5/V9 + 12 doc_audit reescrito + 7 recall/summary en `test_publication_gates.py`; sin parametrizar salvo casos heredados). ⚠️ **H no sumó contract tests entre módulos**: sus 46 funciones son **units** de los seis quirúrgicos **más 2 guardianes anti-deriva** — `test_v6_estado_not_evaluated_reutiliza_literal_canonico_del_gate` (el literal replicado por el ciclo de imports no puede divergir del del gate) y `test_v13_no_existe_segunda_implementacion_del_validador` (fija la **identidad** de la clase, no su texto) — que van en la fila de arriba. Los **contratos de A y B se re-verificaron en verde: 83 passed** | H |
| Fases completadas | 0 / 11 | **8 / 11** (A, B, C, D, E, F, G, **H**) — B y D en **sesiones paralelas** sobre el mismo working tree (L-D1); C en sesión propia sobre el árbol ya combinado; E en sesión propia, con sus 2 tracks **secuenciales** (ambos editan `main.py` — desviación justificada de §4); F en sesión propia (DIRECTO), commit de código `23d0978`; **G** en sesión propia (DIRECTO), commit de código `c317cda`; **H** en sesión propia (**DELEGADA con 2 subagentes + parent**), con el reparto **re-emitido por archivos disjuntos** porque dos tracks compartían `pain_solution_mapper.py` y `v4_diagnostic_generator.py` (L-H2, mismo defecto de §4 que S-E1); su código queda en el árbol de trabajo de la sesión y el parent lo commitea al cerrar | H |
| Versión | 4.74.1 | 4.74.1 *(solo RELEASE la mueve)* | — |
| Registros de identidad consolidados | ≥9 dispersos (dossier) → **15** reales tras el censo (C-5 añadió el #15) | **1 canónico** (`SERVICE_IDENTITIES`) + **Capa 1** (`PAIN_SOLUTION_MAP`) + **6 derivados** + **4 validados contra Capa 1** + **3 fuera de alcance** — censo §8.2. **B retiró 1 pain_id de Capa 1** (`no_ga4_enhanced`, guardia insatisfacible) ⟹ Capa 1 = **26**, no 27: cualquier cita de «27» en docs/prompts posteriores quedó desactualizada | B |
| Peso de impacto de narratives (S14/C-5) | 16 valores × 4 copias YAML + 16 fallbacks Python = **80 literales** | **20 valores × 4 regiones** (los 4 pains nuevos declarados en `regional_benchmarks.yaml`) + fallback Python **derivado del `estimated_impact` de Capa 1** (nunca un default mudo). La costura de regionalización se preservó a propósito (S-B8 pide el lint, no el colapso) | B |
| Gates blocking / advisory | 10 / 3 (declarado) · 13 plano (código) | **11 / 2 declarado = 11 / 2 en código**, decidido por `gate_blocks_publication()`. El `RuntimeError` de `__init__` hace imposible que las dos vistas diverjan. **Tres regímenes cerrados, el cuarto (delivery) intacto a propósito** (H10). Verificado por contrafactual: **0 flips de `ready`** sobre 2 corridas reales | D |

> **Conteos de tests** (memoria `conteos-tests-documentados-metodo-def_test`): documentar por
> `grep "def test_"`, no por `--collect-only` (3,631 vs 3,520). Actualizar README + AGENTS **juntos**.

---

## Sección E — Archivos afiliados actualizados

> Archivos de producción y documentación **modificados** por el plan. RELEASE los consolida en el
> CHANGELOG (`### Archivos Modificados`).

| Fase | Archivo modificado | Región / cambio | Estado |
|------|--------------------|-----------------|--------|
| A | `modules/asset_generation/proposal_asset_alignment.py` | **DERIVADO**. `:1-45` import de `..common.service_identity` + `PROPOSAL_SERVICE_TO_ASSET` como dict-comprehension sobre `SERVICE_IDENTITIES` filtrado por `counts_in_alignment`; `ALL_PROMISED_SERVICES` deriva de él; comentario del drift (copia #1) reescrito preservando las 2 NOTEs históricas. `:219` y `:993` no requirieron cambio. **`:609-612` / `:792-794` intactos (trampa A5 → FASE-C)** | ✅ 2026-09-03 |
| A | `modules/asset_generation/conditional_generator.py` | **VALIDADO, no derivado** (+ comentario en `:230-241`). `PAIN_TO_ASSET` (atributo de clase, 11 entradas) responde otra pregunta — enruta qué asset *generar*, no qué servicio *vender*: derivarlo haría que `poor_performance` generara `optimization_guide` en vez de `performance_audit`. `:314-326` sin cambio | ✅ 2026-09-03 |
| A | `modules/asset_generation/pain_ledger.py` | **PARCIAL**. `NORMALIZATION_RULES` **derivado** de `PainSolutionMapper.PAIN_SOLUTION_MAP` (corrige N-A2: faltaban 2 entradas y había 1 clave obsoleta). `PAIN_TO_PRESENCE_ASSET` **no derivado** a propósito: la derivación completa produce 13 vs sus 6 y cambia la semántica de `apply_site_verification` → insumo de **FASE-F** (A4/V15) | ✅ 2026-09-03 |
| A | `modules/commercial_documents/v4_diagnostic_generator.py` | `:126-166` — los **6 IDs fantasma + 1 asset fantasma** corregidos a `None`/asset real; falso encabezado «ÚNICA FUENTE DE VERDAD» y el «Sincronizar con» manual reemplazados por la regla. `ELEMENTO_KB_TO_PAIN_ID` **validado, no derivado** (responde «qué elemento del KB dispara qué pain», no identidad de servicio). `:3067-3086` sin cambio (solo itera `.keys()`) | ✅ 2026-09-03 |
| A | `modules/commercial_documents/v4_proposal_generator.py` | **DERIVADO** (2 registros locales de método). `:1281-1289` `service_brecha_candidates` ← `identidad.brecha_candidates` (**solo la fuente de identidad; la lógica intacta, como exigía A4** — C2 la reescribe); `:1365-1372` `ASSET_TO_PAIN_ID` ← canónico (**copia #3 del drift eliminada**). Único lector confirmado por grep: `:1410` | ✅ 2026-09-03 |
| A | `modules/commercial_documents/service_catalog.py` *(no previsto en el plan)* | **DERIVADO**. `SERVICE_CATALOG` construido desde `SERVICE_IDENTITIES`; **eliminada** la mutación post-hoc `SERVICE_CATALOG["optimizacion_ia_generativa"] = ServiceEntry(...)` (**copia #2 del drift**). `SERVICE_TO_ASSET_LOOKUP = dict(PROPOSAL_SERVICE_TO_ASSET)` ya derivaba — sin cambio | ✅ 2026-09-03 |
| A | `tests/commercial_documents/test_proposal_dynamic.py` *(no previsto en el plan)* | **6 aserciones fosilizadas desfossilizadas**, 3 tests renombrados, 1 docstring corregido. El renombrado clave: `test_all_service_catalog_services_have_lookup_entry` codificaba el invariante **invertido** (que la demanda sea el drift) → `test_solo_servicios_alineables_tienen_lookup_entry`. Ahora deriva sus expectativas de `SERVICE_IDENTITIES` | ✅ 2026-09-03 |
| B | `modules/commercial_documents/pain_solution_mapper.py` | **3 cambios**. (1) `PAIN_SOLUTION_MAP` `:60` — **retirada** la entrada `no_ga4_enhanced` (comentario de retiro en `:188`), Capa 1 27 → 26; (2) `_detect_analytics_pains` `:720` — **eliminada** la rama muerta `elif status and hasattr(status, "is_enhanced")`; (3) `detect_pains` `:333` — **3 ramas emisoras nuevas** con guard, insertadas antes de `# Sort by severity` (+85 líneas en el archivo). ⚠️ **Desplazó las citas de H**: el guard `__iter__` de V7 está ahora en **`:447`** (era `:453`) y la emisión `low_ota_divergence` en **`:452`** (era `:457`) | ✅ 2026-09-03 |
| B | `modules/commercial_documents/v4_diagnostic_generator.py` | **Confinado a `_pain_to_brecha`**, como exigía el prompt. `:3346-3369`: el `return None` del descarte silencioso se reemplazó por una **derivación de Capa 1** (nombre y detalle del registro canónico; peso de `pain_narratives` con fallback derivado de `estimated_impact`). El dict literal `narratives` `:3263-3344` quedó **byte-idéntico** (16 entradas) — prueba de que no se creó tabla paralela (L-NC4). **V6 `:3197-3202` y V11 `:1953` intactos y en su posición** | ✅ 2026-09-03 |
| B | `config/regional_benchmarks.yaml` *(no previsto en la Sección E; asignado a B por S14)* | `pain_narratives` de las **4 regiones**: 16 → **20 entradas** cada una (+32 líneas). Los 4 pains que dejaron de descartarse reciben peso explícito (`low_ota_divergence 0.20`, `missing_alt_text 0.10`, `missing_llmstxt 0.08`, `no_social_links 0.08`). Se preservó la costura de regionalización a propósito; el lint de sincronía queda como S-B8 | ✅ 2026-09-03 |
| B | `modules/asset_generation/asset_catalog.py` *(no previsto en el plan)* | `:298` — `promised_by` del asset huérfano: se retiró `"no_ga4_enhanced"` junto con el pain, para no dejar un asset prometido por un pain_id inexistente | ✅ 2026-09-03 |
| B | `README.md` | `:222` — `Pain narratives (16)` → `(20)`. Lo exige `run_all_validations.py --quick` (check «README numerical counts vs code»), que falló 6/7 hasta corregirlo. Se preservó el número desnudo porque el validador lo busca por regex | ✅ 2026-09-03 |
| C | `modules/asset_generation/proposal_asset_alignment.py` | **1005 → 1062 líneas** (208+/151−). La predicción de A5 (`:575-659` y `:748-789`, «curar **uno** solo») era correcta en las posiciones pero **subestimó el remedio**: se extrajo **`classify_promised_services()` `:511-641`**, la **única** partición propuesta→brecha→asset, y los **dos** builders delegan en ella (`ProposalAssetMatrix.build` `:717-749`, antes 87 líneas → 33; `AssetAlignmentMatrix.build` `:842-889`, antes 98 → 48). Duplicar el arreglo en gemelos era exactamente la derivación que A5 midió. Nuevo `ALWAYS_ACTIVE_COMPLEMENT_ASSETS` **`:54`** derivado de `counts_in_alignment=False` (nunca un literal). Sin brecha **y** sin presencia el servicio **no se promete** y queda declarado en `not_promised`; el servicio fuera del registro deja de saltarse en silencio (`unknown_services` + `logger.warning`). `to_dict()` `:945-974` publica `summary = {promised, not_promised, unknown}` ⟹ la exclusión es **auditable en el JSON**, no solo en el log. `derive_committed_services` `:1005` re-expone el complemento | ✅ 2026-09-03 |
| C | `modules/commercial_documents/coherence_validator.py` *(declarado **solo-lectura** para C en `dependencias-fases.md` §3 — desviación registrada)* | **732 → 761** (32+/3−), **un solo hunk** `:278-319` en `_check_assets_are_justified` (def en `:255`): el complemento siempre-activo sale del **denominador** (`promised_assets` filtra `ALWAYS_ACTIVE_COMPLEMENT_ASSETS`, `complementos` se divulga en el detalle) y `if not promised_assets:` devuelve `passed=True, score=1.0, severity="info"` — **0.75 → 1.0** estructural, no por relajar nada. Los dientes quedan intactos: un asset sin `pain_ids` que **no** sea complemento sigue restando. **Umbral 0.8 sin tocar.** ⚠️ Prueba de que la desviación no invadió FASE-F: las regiones que F3 tiene asignadas — `:185-188` y **`:670-700`** (`promised_assets_exist`, P12/A3) — **no aparecen en el diff** | ✅ 2026-09-03 |
| C | `modules/quality_gates/publication_gates.py` *(no previsto para C en la matriz de conflictos)* | **2181 → 2188** (9+/2−), 3 hunks en `_proposal_asset_alignment_gate` `:979-1017`. Los **3 sitios que colapsaban «vacío» con «ausente»**: caído el `or []` del argumento, `if pain_ledger is not None:` `:1003` (era `if pain_ledger:`), y `site_presence_report=site_presence_report` `:1014` entra a la matriz — sin esto el gate y el delivery report volvían a divergir (AC3). ⚠️ **Citas desplazadas +7** para todo símbolo posterior a `:979` (L-A6 se aplica otra vez): las posiciones que midió FASE-D siguen vigentes hasta `:979`, las de G (`:1328`, `:1335`, `:1555`) quedan **+7** | ✅ 2026-09-03 |
| C | `modules/commercial_documents/v4_proposal_generator.py` | **Predicción corregida**: `:1281-1289 service_brecha_candidates` **NO se tocó** (sigue derivando de Capa 2 desde FASE-A, intacto). Los 2 hunks reales son **6 líneas** (2540 → 2544): `:699` pasa `site_presence_report` al build de la matriz y **`:1205`** `if not pain_ledger: return None` → **`if pain_ledger is None: return None`** — un ledger resuelto **sin** brechas (`[]`) ya no cae al catálogo estático legacy. La divergencia de atribución que quedó **visible** al medir (matriz vía Capa 1 `ai_crawler_blocked → llms_txt` vs columna del documento vía Capa 2 `brecha_candidates=("missing_llmstxt",)`) es **V4 → S-C5**, no se cerró aquí | ✅ 2026-09-03 |
| C | `modules/commercial_documents/service_catalog.py` | **119 → 126** (+7). `TECHNICAL_ASSET_CATALOG` `:105-117`: entra **`indirect_traffic_optimization`** `:110` — se generaba y no aparecía en **ninguna** tabla técnica. **Cierra S-B10** (heredado de FASE-B) | ✅ 2026-09-03 |
| C | `modules/quality_gates/delivery_quality_report.py` | **498 → 503**, **solo comentario** `:230-242` (2 hunks de texto, 0 de lógica): documenta por qué el reporte de delivery y el gate deben leer la **misma** matriz ya particionada. Régimen de delivery **intacto** a propósito (H10, dueño E→F). Las filas que F tiene asignadas (`:250-257`, `:310-319`, `:325`) **no se tocaron** | ✅ 2026-09-03 |
| C | `modules/commercial_documents/templates/propuesta_v6_template.md` | **NO SE TOCÓ — predicción retirada.** `${dynamic_services_table}` (`:52`) **ya era dinámico** desde FASE-SR-B / D-PF1: la tabla de servicios nunca fue la superficie estática. Las dos superficies estáticas reales eran la **matriz + gate** y la **lista de assets que se entrega a coherencia**, y ambas se corrigieron. Queda **una cuarta** superficie estática sin tocar: `TECHNICAL_ASSET_CATALOG` como tabla del documento → **S-C4** | ➖ No aplica |
| C | `tests/asset_generation/test_fase_c_propuesta_dinamica.py` *(nuevo)* | **306 líneas · 14 funciones / 17 casos** en 4 clases: `TestComplementosFueraDeJustificacion` (el complemento no resta, un asset sin pain que no sea complemento **sí**), `TestMatrizSoloPrometeConBrecha`, `TestVacioNoEsAusente` (`[]` ≠ `None` en los 3 sitios), `TestAmbosBuildersIdenticos` (los gemelos no pueden derivar — **la prueba de que A5 quedó curada, no esquivada**). TDD: colectivo **ROJO** por `ImportError` y comportamiento **ROJO** antes de implementar (`evidence/FASE-C/tdd-*.txt`) | ✅ 2026-09-03 |
| C | `tests/asset_generation/test_proposal_asset_matrix.py` · `tests/quality_gates/test_alignment_contract.py` · `test_alignment_result.py` · `test_proposal_asset_alignment.py` | **+122/−32** entre los 4. Desfossilizadas las expectativas que codificaban el catálogo estático (7 prometidos siempre) y añadidos los contratos de `not_promised`/`unknown_services`/`summary`. `test_alignment_contract.py:133` **ya cerraba el cerrojo B7** — fue la prueba que permitió resolver S-B11 corrigiendo el **test**, no el código | ✅ 2026-09-03 |
| C | `tests/commercial_documents/test_proposal_dynamic.py` *(commit aparte `552c190`)* | **S-B11 cerrado** (28+/5−). `test_no_assets_no_presence_all_excluded` esperaba los **8** servicios en el footnote y fallaba: **dos premisas suyas eran falsas** (`PROPOSAL_SERVICE_TO_ASSET` deriva de Capa 2 y ya **no** contiene el complemento, así que su skip era código muerto; `validar_semantica_comercial` devuelve True para las 8 identidades — medido) y su expectativa **contradecía B7** (FASE-R0-D), ya cerrado en ambos sentidos por `TestFaseR0DServiciosAdicionalesWhatsApp`. Ahora deriva del registro y **afirma B7 explícitamente**: 34 passed. ⚠️ El diagnóstico de B ubicaba la regla en `proposal_asset_alignment.py`; vive en `v4_proposal_generator.py:1561-1565` | ✅ 2026-09-03 |
| D | `modules/quality_gates/publication_gates.py` | **2064 → 2181 líneas (+117)**. `:4` docstring de módulo y `:232` docstring de clase → «11 blocking + 2 advisory»; **`:130-190` bloque nuevo** = única fuente del régimen (`BLOCKING_GATE_NAMES` 11 en `:130`, `ADVISORY_GATE_NAMES` 2 en `:144`, `PROPOSAL_ASSET_ALIGNMENT_FLOOR` `:152`, `GATE_EXECUTION_FAILED_KEY` `:155`, `gate_severity()` `:158`, `advisory_degrades_to_blocking()` `:163`, `gate_blocks_publication()` `:179`); `__init__` **`:268-278`** fail-fast (registro ↔ severidad); `:307` el camino de excepción de `run_all` señala `GATE_EXECUTION_FAILED_KEY`; `:311` `is_ready_for_publication` y `:326` `get_blocking_gates` deciden por el predicado; `:827` `content_quality` con solo warnings emite `GateStatus.WARNING` (antes `PASSED`); `:2010/:2065` `check_publication_readiness` filtra por severidad y expone `summary["blocking_gate_names"]` + `summary["advisory_issues"]`; `__all__` +6 nombres. ⚠️ **Desplazó +91 líneas** todo símbolo posterior a `:130` ⟹ las citas de los prompts C, F y G de este archivo están **fosilizadas** (ver L-A6 en `dependencias-fases.md`) | ✅ 2026-09-03 |
| D | `modules/quality_gates/human_checklist_generator.py` | **Divulgación D2 (riesgo C)**. `generate(report, advisory_issues: Optional[List[Dict[str, Any]]] = None)` — nuevo parámetro con default ⟹ retrocompatible. Sección **5b** antes de los ítems de decisión comercial para que `items[:MAX_ITEMS]` no los corte: cada advisory no bloqueante se imprime como `ADVISORY <gate>: <message> — no bloquea la publicacion, decidir antes de entregar`. Sin esto un gate advisory que falla es **invisible para el humano**, que era el defecto de raíz de H10 | ✅ 2026-09-03 |
| D | `main.py` | 4 hunks: `:2915` importa `gate_blocks_publication`; `:2952` consola en 3 estados (✅ / ❌ / ⚠️) en vez de 2; `:2965-2966` imprime los advisories no bloqueantes; `:3181-3185` cablea `advisory_issues` desde `readiness_report["summary"]` al checklist. `delivery_quality_report.py` **no se tocó** (régimen delivery, dueño E→F) | ✅ 2026-09-03 |
| D | `tests/quality_gates/test_publication_gates.py` | `+127` líneas: helper `_gate()` + `class TestFASEDGateSeverity` (**12 tests**) conductuales — advisory que falla por encima del piso no bloquea, `content_quality` con blockers sí bloquea, `proposal_asset_alignment` bajo/sobre 0.8, `asset_confidence` sigue bloqueando, WARNING divulgado, advisory que **no se ejecutó** bloquea, `run_all` marca el fallo de ejecución, paridad con `get_blocking_gates`. 69 passed / 1 skipped en el archivo | ✅ 2026-09-03 |
| D | `tests/quality_gates/test_human_checklist_generator.py` | `+48` líneas: `class TestAdvisoryDisclosureFASED` (**4 tests**) — el checklist contiene el advisory, respeta `MAX_ITEMS`, y sigue funcionando sin el parámetro (retrocompatibilidad) | ✅ 2026-09-03 |
| D | `AGENTS.md` | Fila `quality_gates/` de «Módulos Activos» → **11 blocking (con `asset_confidence`) + 2 advisory** nombrados; bloque «FASE 4.5» del flujo reagrupado por severidad **y completado a 13** (faltaba `doc_audit_consistency`). Lo verifica `validate_agents_md.py` (6 PASS / 0 FAIL) + el candado de docstrings | ✅ 2026-09-03 |
| E | `main.py` | **2 hunks**. E2 — `assets_for_quality` (~`:2798`): los dicts que el caller le pasa al builder ganan `"path": a.path or None` — **causa raíz de A6 corregida aquí, no en la matriz** (`classify_promised_services` ya poblaba `asset_path` bien; la predicción de Sección A/E que apuntaba a `alignment_result.py` era falsa y se retiró). La rama fallback de `asset_plan` sigue sin rutas a propósito. E1 — FASE 0E (~`:3157`, tras crear `v4_audit_dir`): persistencia del snapshot en `try/except` con `[WARN]`, usando **`site_presence_snapshot`** (incondicional) y no `site_presence_report` (solo existe si `generate_proposal=True` — NameError latente preexistente, no introducido ni curado aquí → F/H) | ✅ 2026-09-03 |
| E | `modules/asset_generation/site_presence_adapter.py` | **+writer** `save_site_presence_snapshot()` (+ `SNAPSHOT_VERSION = "1.0"`, imports `json`/`Path`): serialización passthrough del snapshot ya propagado (DT4-N2 — no llama `normalize_site_presence`), envoltorio versionado, UTF-8 explícito, `mkdir` del padre. El resto del adaptador intacto | ✅ 2026-09-03 |
| E | `tests/test_site_presence_persistence.py` *(nuevo)* + `tests/quality_gates/test_delivery_asset_path.py` *(nuevo)* | **5 + 5 tests** — detalle en Sección A. El test de delivery clava el extremo E2E: `DeliveryQualityReportGenerator().generate(...)` sobre directorio temporal con matriz/ledger reales → `proposal_asset_gate.passed=True` y `asset_path` con la ruta real en el JSON persistido | ✅ 2026-09-03 |
| F | `modules/asset_generation/proposal_asset_alignment.py` | **A4 — la clasificación usa el criterio canónico**. `verify_proposal_asset_alignment` (~`:206-220` + rama de clasificación): el `== "exists"` del oráculo estricto se reemplaza por `is_present_in_production()` (import del checker canónico) — un solo oráculo decide y narra; el **veto FASE-12B** (audit dice `hotel_schema_detected=false` vs presencia que lo afirma) se extiende a `exists_with_issues`. ⚠️ `alignment_result._presence_resolved` **no se tocó** — tercera predicción de archivo falsa del plan: el segundo oráculo vivía en este módulo, no en `alignment_result.py`. La corrección del fixture del test fue de **test**, no de código | ✅ 2026-09-03 |
| F | `modules/quality_gates/delivery_quality_report.py` | **A1 — 503 → 547 líneas**. Los 2 defaults G9 independientes unificados en **`_not_evaluated_g9()`** (`:25`; exactamente 1 `"state": "NOT_EVALUATED"` en el source — lo escanea `test_unico_default_g9_sin_segundo_default`); `summary` distingue `passed/failed/not_evaluated`; el skip deja de ser invisible: «G9: NOT_EVALUATED» entra a `human_review_items` (`:287-291` — el test del PASS-all-gates ahora exige ese ítem). **No bloquea** — régimen delivery intacto (H10). ⚠️ Residual divulgado: G6-delivery sigue leyendo **solo score** (fallback post-gen) y no consume `coherence_verdict_passes` → **S-F2** | ✅ 2026-09-03 |
| F | `modules/quality_gates/coherence_gate.py` | **N11/P9 (b) — CONECTADO, no eliminado**. `coherence_verdict_passes(score, threshold, declared_is_coherent)` — única definición del veredicto (`None` = vacío ≠ ausente → legacy; solo `False` explícito bloquea) — en **ambos** caminos (`execute` y `execute_from_validator`): un veredicto negativo con score sobre umbral baja `CERTIFIED→REVIEW` con `can_certify=False` (estado y veredicto ya no pueden divergir). El test FASE-1-COH que consagraba el defecto (0.81/False → CERTIFIED) se corrigió al contrato nuevo (patrón S-B11: el test contradecía el cerrojo, no el código) | ✅ 2026-09-03 |
| F | `modules/quality_gates/publication_gates.py` | **N11/P9 en el orquestador — 2188 → 2231 líneas**. `:56` importa `coherence_verdict_passes`; `_coherence_gate` decide `passed` con el veredicto y su mensaje nombra `is_coherent=False` (el prompt de C citaba el gate en `:458`, hoy `:549` — caso (5) de S15, re-verificado con grep antes de editar); `check_publication_readiness` hereda el bloqueo vía `gate_blocks_publication` (FASE-D). **Umbral 0.8 intacto**; repro del dossier fijado por `TestFaseFCoherenceRespetaIsCoherent` (5) | ✅ 2026-09-03 |
| F | `modules/quality_gates/publication_state.py` *(eliminado)* | **H8 — eliminación, no conexión** (decisión (c) del prompt): **675 líneas → 0**, 0 importers verificados por grep antes de borrar. Era el huérfano del dossier (duplicaba publication readiness con otra semántica). El conectado es `coherence_gate.py` (fila arriba) | ✅ 2026-09-03 |
| F | `modules/assessment_builder.py` | **Propagación del veredicto** (precondición del (3)): `AssessmentPayload` gana `is_coherent: Optional[bool] = None` (vacío ≠ ausente, L-SR5) y `with_coherence` extrae score **y** veredicto de la **misma fuente** (`final_coherence_report` preferido, DT4-N4) — antes el gate solo veía el score | ✅ 2026-09-03 |
| F | `tests/quality_gates/test_alignment_result.py` · `test_coherence_gate.py` · `test_delivery_quality_report.py` · `test_publication_gates.py` | **+23 funciones nuevas + 2 reescritas al contrato nuevo** (5+7+6+5). Destacados: fixture PRESENCE corregido a forma canónica (`normalize_site_presence` — la forma que el fixture usaba pasaba `_presence_exists` pero no `_presence_resolved`, reproduciendo el defecto A4 dentro del propio test); `test_unico_default_g9_sin_segundo_default` endurecido a `count('"state": "NOT_EVALUATED"') == 1` (el comentario nuevo de F triplicaba el match — L-A1 aplicada a sí misma) | ✅ 2026-09-03 |
| F | `modules/commercial_documents/coherence_validator.py` | **NO SE TOCÓ — predicción retirada**. `:670` `if not generated_assets:`, `:689-700` (`score=1.0` hardcode) y el mensaje stale «7 servicios verificados» siguen intactos: es **A3/P12** (S-C3), fuera del alcance de A4/N11 → reasignado a G/H | ➖ No aplica |
| G | `modules/quality_gates/publication_gates.py` | **2231 → 2346 líneas (+115)**. **G1** — `_doc_audit_consistency_gate` reescrito (región ~`:1654-1837` post-F): datos ausentes → `NOT_EVALUATED` con `state_reason` (`missing_diagnostico_text`/`missing_audit_data`, coherente con A1 — no bloquea, no cuenta como pasado, visible en summary), Check 2 acepta `gbp.reviews` `int`/`float` además del dict, contradicciones reales → FAILED con patrones `_DOC_AUDIT_CONTRADICTION_PATTERNS` (`og_missing_vs_present`, `performance_error_vs_new_site`); `GateStatus.NOT_EVALUATED` añadido a `__init__`/`run_all`/summary (+`gate_blocks_publication` lo excluye). **G3** — loop de coverage ~`:1549-1553`: `ASSET_GENERATED` sin mención → `is_justified=False` (`_JUSTIFIED_STATUSES` **:1386 intacto**, BUG-6 no revertido). **G4** — `_coverage_gate` ~`:1487-1521`: normalización única dict/lista, `reconciler_dropped_entries` BLOCKED, vacío legítimo PASSED con `ledger_present_zero_entries`+`reconciler_ran`. **G2 consumidor** — `_evident_critical_missed` ~`:2073` + `_extract_critical_recall` ~`:2099` contrastan contra `audit_data.performance.status`. ⚠️ Citas del prompt G desfasadas (L-A6): usar símbolos | ✅ 2026-09-04 |
| G | `modules/auditors/v4_comprehensive.py` | **1885 → 1945 (+60)**. **G2 — G2 vive aquí, no en `pain_solution_mapper.py`** (cuarta predicción de archivo falsa del plan, corregida en §3): `GEO_CRITICAL_MAX_SCORE = 35` + helper módulo-level `geo_band_critical_issue()` (acepta dict u objeto con `to_dict`), `_identify_critical_issues(..., geo_flow_result=None)` con 2 criterios nuevos (PageSpeed `status=ERROR` no cubierto; banda GEO `critical`) — los criterios legacy intactos (TestLegacyCriteriaIntact). `to_dict()` ya serializa lo que el gate contrasta | ✅ 2026-09-04 |
| G | `modules/assessment_builder.py` | **320 → 346 (+26)**. `AssessmentPayload.audit_data: Dict[str, Any]` (crudo, para G1) + `with_audit_data()` + `with_geo_flow()` (carga el helper `geo_band_critical_issue`, añade el issue a `critical_issues` sin duplicar). `main.py` **3859 → 3871 (+12)**: `builder.with_audit_data(audit_result.to_dict())` tras `with_audit` + carga tolerante de `v4_audit/geo_flow_result.json` (WARN no bloqueante si falta) | ✅ 2026-09-04 |
| G | `tests/auditors/test_v4_comprehensive.py` *(nuevo)* · `tests/quality_gates/test_coverage_gate_v5.py` *(nuevo)* · `tests/quality_gates/test_doc_audit_consistency_gate.py` *(REESCRITO 9→12)* · `tests/quality_gates/test_publication_gates.py` (75→82) · `tests/quality_gates/test_coverage_gate_integration.py` (asserts actualizados al contrato post-G3/G4) | **+40 netas** — detalle en Sección A y en la fila NR de `06-checklist`. Los 3 archivos preexistentes codificaban el contrato pre-G (doc_audit WARNING-mode, reconciler-mención, mensaje de reconciliación) y se actualizaron con registro — patrón S-B11: el test contradecía el cerrojo nuevo, no el código | ✅ 2026-09-04 |
| H | `modules/commercial_documents/pain_solution_mapper.py` | **1202 → 1297 líneas (+95)**. **V7 (H1)** `:443-472`: el guard `hasattr(direct_field.value, '__iter__')` se **elimina** y se reemplaza por validación numérica con normalización de unidades — helpers nuevos **`_normalize_to_fraction`** (`:1235`, desempate: valor > 1 ⟹ porcentaje; `0.2`, `20`, `"0.2"`, `"20"`, `"20 %"` = todos 20 %) y **`_describe_ota_presence`** (`:1271`); `ota_field` (`ota_presence`) pasa a **evidencia no bloqueante** del description y **nunca** a guard (con `main.py` sin registrarlo en el `ValidationSummary`, usarlo de guard habría devuelto `low_ota_divergence` a código muerto); el `isinstance(...)` muerto desaparece como rama inalcanzable y `int(direct_pct*100)` → **`round(...)`**. **V8 (H3)** `:740-800`: `low_organic_visibility` **deja de emitirse dos veces** — primero se calculan las dos señales (`not ga4_available`, `organic_measured_low`) y hay **un solo punto de emisión**, que cuando ambas aplican conserva el dato medido (sesiones/umbral) además del motivo «sin analytics»; severidades, nombres y `detected_by` de cada caso **intactos**. ⚠️ Sustituye las posiciones que midió B (`:447`/`:452`/`:677-701`): **usar símbolos** (L-A6) | ✅ 2026-09-04 |
| H | `modules/commercial_documents/v4_diagnostic_generator.py` | **3715 → 3843 líneas (+128)**. **V6 (H2)**: estados `BRECHAS_STATE_EVALUATED` / `BRECHAS_STATE_NOT_EVALUATED` (`:65-66`), atributo `self._brechas_detection_state` en `__init__` (`:553`) y **reseteado por corrida** en `generate()` (`:623`); el `except Exception` de `_identify_brechas` (`:3296-3314`) hace `logging.getLogger(__name__).exception(...)` (traceback), fija NOT_EVALUATED y **deja de escribir** `_cached_brechas`/`_cached_brechas_key`; la ruta de éxito (`:3330`) y el cache-hit (`:3261`) restauran EVALUATED. **Render**: `_FUGAS_NOT_EVALUATED_NOTICE` (`:2786-2796`) en `_build_fugas_principales_section`, título `FUGAS PRINCIPALES (NO EVALUADAS)` (`:1063`) y —**cerrado por el parent**— el helper **`_brechas_detection_failed()`** (`:2739`) aplicado también a `_build_brechas_section` y `_build_brechas_resumen_section`, que de otro modo seguían afirmando «No se detectaron brechas criticas / Sin brechas detectadas» con el detector caído. **V11 (H3, lado documento)**: **`MANUAL_ATTENTION_TABLE_HEADER`** (`:77`) que `_build_manual_attention_table` antepone a las filas (cierra el punto **(c)** del dossier §1 — las filas de `${manual_attention_table}` no renderizaban como tabla; la cabecera la construye el código, no el template, porque las filas nacen ahí y el conteo de columnas no se desincroniza: el fallback de GBP pasó de 4 a **3** columnas para respetar el contrato) y la rama de performance, que deja de tratar «no ERROR» como sinónimo de «sitio nuevo» y consulta `is_performance_api_unavailable(status)` (`:2003`): `API_NOT_CONFIGURED`, status vacío/`None`/desconocido → 🔴 **Alta** con el `message` real de la fuente; solo `OK`/`SUCCESS`/`VERIFIED`/`LAB_DATA_ONLY` conservan el 🟡 Media «El sitio puede ser nuevo o tener tráfico bajo». ⚠️ **Decisión de diseño**: el literal `NOT_EVALUATED` **no** se importa de `GateStatus`, se **replica** — el import top-level de `modules.quality_gates.publication_gates` desde este módulo produce `ImportError` por ciclo (verificado empíricamente); la deriva la corta `test_v6_estado_not_evaluated_reutiliza_literal_canonico_del_gate` | ✅ 2026-09-04 |
| H | `modules/auditors/v4_comprehensive.py` | **1945 → 2025 líneas (+80)**. **V11 (H3, lado fuente)** `:47-79`: constantes `PAGESPEED_KEY_MESSAGE` = «API de PageSpeed no disponible (verificar clave)», `PAGESPEED_GENERIC_MESSAGE`, `PAGESPEED_NOT_CONFIGURED_MESSAGE` + **`sanitize_pagespeed_message()`**; `_audit_performance` publica el mensaje **ya sanitizado** (el crudo del `PageSpeedClient` queda solo en `logger`) y `_generate_recommendations` lee `status`/`message` reales en vez de afirmar «site may be new or low traffic» cuando la causa era nuestra. Importa porque **`extract_top_problems`** (`modules/commercial_documents/data_structures.py:532-536`) vierte esas recomendaciones textuales **en el diagnóstico del cliente**. **Punto (d)**: **`disjoint_executed_validators()`** (`:83`, consumida en `:760`) hace que `execution_trace.executed` y `.skipped` sean **excluyentes por construcción** — `pagespeed_api` figuraba en ambos. ⚠️ **Residual**: el predicado de skip sigue siendo `not has_field_data`, que confunde «la API no devolvió datos de campo» con «no se llamó a la API» → **S-H3** | ✅ 2026-09-04 |
| H | `modules/common/performance_status.py` *(nuevo — ver Sección A)* | **Criterio compartido de V11** (27 líneas): `PERFORMANCE_OK_STATUSES` + `is_performance_api_unavailable()`. Justificación: el documento y el audit decidían el mismo criterio por su cuenta — es la deriva que denuncia el dossier (H10/A1). Lista blanca a propósito: **vacío ≠ ausente** (SR-H2) | ✅ 2026-09-04 |
| H | `modules/data_validation/metadata_validator.py` + `data_validation/metadata_validator.py` | **V13 (H4) — unificación de gemelos.** El gemelo (`modules/…`) eran **219 líneas byte a byte idénticas** al vivo, verificado con `diff`/`md5sum`, y **fue borrado físicamente** (`git rm`, −219 líneas): el único que queda es `data_validation/metadata_validator.py` (el vivo: lo importa `modules/auditors/v4_comprehensive.py:33` y lo re-exporta `data_validation/__init__.py`). Los **dos únicos consumidores** del gemelo repuntados al vivo: `tests/data_validation/test_metadata_validator.py:15` (22 → 23 casos) y `tests/test_hotel_visperas_regression.py:22` (intacto) ⟹ los checks **CRITICAL `title`** / **HIGH `description`** siguen cubiertos. Guardia: `test_v13_no_existe_segunda_implementacion_del_validador` fija la **ausencia** de la segunda implementación. Secuencia: el primer `git rm` lo bloqueó la política de permisos y quedó un shim de delegación (opción que admite el AC); el usuario autorizó el borrado en la misma sesión → **borrado** | ✅ 2026-09-04 |
| H | `tests/commercial_documents/{test_pain_solution_mapper,test_diagnostic_brechas,test_diagnostic_generator}.py` · `tests/data_validation/test_metadata_validator.py` · `tests/auditors/test_v4_comprehensive.py` | **+46 funciones / +74 casos** (5→21 func, 42→52, 27→36, 22→23 y 16→26 func respectivamente; `tests/test_hotel_visperas_regression.py` solo cambió su import → al vivo). Batería conjunta de la fase: **188 passed / 0 failed** (`evidence/FASE-H/faseH_units.txt`) | ✅ 2026-09-04 |
| H | `.env` | ➖ **NO EDITADO — V12 es decisión OPS y así se respetó** (`git diff --stat .env` = vacío). Lo que H hizo fue **medir y documentar** → ver la **nota OPS** debajo de esta tabla | ✅ 2026-09-04 (documentado) |
| RELEASE | `VERSION.yaml`, `CHANGELOG.md`, `GUIA_TECNICA.md`, `REGISTRY.md`, `README.md`, `.cursorrules`, `docs/CONTRIBUTING.md`, `DOMAIN_PRIMER.md` | Cierre documental 4.75.0 | ⬜ Pendiente |

### Nota OPS — V12 (`GOOGLE_PAGESPEED_API_KEY`), medida y documentada en FASE-H (2026-09-04)

> **Esta nota no es trabajo de código y `.env` NO se editó en esta fase** (regla transversal del
> `README.md` del plan + DA5: V12 es decisión **OPS**). Es el insumo para que OPS decida.

**Medición empírica sobre `./.env` (solo longitudes, nunca valores — `evidence/FASE-H/README.md`)**:

| Variable | Longitud medida | Veredicto |
|----------|-----------------|-----------|
| `PAGESPEED_API_KEY` | **39 chars** | válida — es la que resuelve hoy |
| `GOOGLE_PAGESPEED_API_KEY` | **3 chars** = `xxx` | **placeholder inválido** (la trampa del dossier) |
| `GOOGLE_API_KEY` | **ausente** | no existe en el archivo |

**Cadena de fallback vigente** — `modules/data_validation/external_apis/pagespeed_client.py:25`:

```python
self.api_key = api_key or os.getenv("PAGESPEED_API_KEY") or os.getenv("GOOGLE_PAGESPEED_API_KEY") or os.getenv("GOOGLE_API_KEY")
```

**Por qué el orden importa (y por qué esto no es cosmético)**: hoy la cadena resuelve la clave de 39
chars y PageSpeed funciona. **Eliminar la canónica `PAGESPEED_API_KEY` reintroduce el síntoma**
porque el fallback cae en el placeholder de 3 caracteres y Google responde **«API key not valid»** —
es decir, la variable que parece «configurada» no lo está, y el fallo se lee como un problema del
sitio analizado (eso es exactamente lo que V11 acababa de verter al cliente, ahora con el mensaje
sanitizado).

**Recomendación OPS (dos acciones, ninguna de código de producción)**:
1. **Eliminar la línea `GOOGLE_PAGESPEED_API_KEY=*** del `.env`**, o poblarla con una clave válida.
   Dejar un placeholder es peor que no poner la variable: simula una configuración que no existe.
2. **Añadir una comprobación de longitud/formato al `onboard`/`setup`**, para que un valor de 3
   caracteres no se lea como «clave configurada». Sin esa comprobación, cualquier futuro orden de
   fallback (o un `.env` distinto por entorno) re-abre el síntoma en silencio.

**Lo que FASE-H sí cerró del hallazgo** (y no dependía de tocar `.env`): el mensaje que llega al
cliente ya no atribuye a «sitio nuevo o poco tráfico» una causa que es nuestra
(`sanitize_pagespeed_message`, `is_performance_api_unavailable`) y `execution_trace` ya no puede
listar `pagespeed_api` como ejecutado y saltado a la vez (`disjoint_executed_validators()`). Lo que
**no** cerró, porque no era su alcance: la capa de pain sigue descartando el ERROR sin pain ni
justificación → **S-H2** en `10-analisis` §5.

> **Archivos NO tocados** (decisión explícita del plan): `delivery_quality_report.py:289`
> `BLOCKING_GATE_NAMES` (rige el ZIP, régimen de delivery, no publicación — §8.4 punto 3); `.env`
> (V12 es decisión OPS, se documenta, no se edita — **FASE-H lo midió y lo documentó el 2026-09-04
> sin tocarlo**: ver la **Nota OPS — V12** encima de esta línea).
