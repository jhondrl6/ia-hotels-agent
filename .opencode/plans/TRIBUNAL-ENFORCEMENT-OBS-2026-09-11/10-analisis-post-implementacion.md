# Análisis Post-Implementación — TRIBUNAL-ENFORCEMENT-OBS-2026-09-11

> **Estado**: 🔄 en curso (**3 fases cerradas**: P1, P3-A, P3-B) *(la cabecera quedó en "0 fases cerradas" tras P1 — descuido que se corrigió en P3-A y se mantiene al día desde entonces)*
> **Plan**: TRIBUNAL-ENFORCEMENT-OBS-2026-09-11 · **Versión objetivo**: 4.77.0 (confirmar en P1)
> **Creado**: 2026-09-14 en la sesión de ajuste — el executor lo exige **desde la concepción** del plan (v1.4.0+ del template); este plan lo debió desde el 2026-09-11 y la deuda queda aquí registrada, no retrodatada. Se actualiza al cierre de **cada** fase.

---

## Resumen de Ejecución (llenar al cierre de cada fase)

| Fase | Sesión | Estado | Iteraciones (ids + tool_use, corte = commit) | delegate_task | Notas |
|------|--------|--------|----------------------------------------------|---------------|-------|
| Ajuste de preparación *(orquestación, fuera de presupuesto de fase)* | 2026-09-14 | ✅ | auto-reporte (D-V2.1: el instrumento no alcanza el transcript bajo el cliente actual; unidad declarada) | No | Etapa 1 completada: prompts P3-A/P3-B/P4/RELEASE + 09/10 creados; P2 diferido a Q1 con declaración; P3 dividida por R3; FASE-VERIFY condicionada; cierre sin P4 fijado; citas `3bdc14e`/origin/tag `v4.76.0` corregidas |
| FASE-P1 | 2026-09-14 | ✅ | **≈30 `ids` / ≈58 `tool_use`** (auto-reporte, **unidad declarada** — D-V2.1: `measure_iterations.py` no alcanza el transcript bajo el cliente actual), corte = commit de cierre | No | Q1=sí · Q1b=escalar · Q2=O1-cuarentena · Q2b=2 capas · Q3=P3→P2 · Q4=el hotel · Q5=a · Q6=4 estados · Q7=knob heredado · VERIFY no activa · prompt P2 creado. **Presupuesto de 30 superado** → causa y cura en L-P1.4 |
| FASE-P3-A | 2026-09-14 | ✅ | **106 `ids` / 126 `tool_use`** — medido con `measure_iterations.py`, corte = commit de código `0d4d072`. **El instrumento SÍ alcanzó el transcript → D-V2.1 NO se reprodujo** (cifra medida, no auto-reportada) | No | AC-F1 (dos capas ZIP-aware + stub estructural, 4 estados NR8) + AC-F2 (tier desde `financial_scenarios`, MANIFEST fallback) + AC-F4 (`FIRST_FLOOR_TIERS` += `"B+"`). R2.7 4.109→4.130 (+21 íntegro en `passed`, 0 regresiones); 4 pares NR7 verde/rojo; `--quick` 9/9; sonda real: AC-F1 1 CRITICAL (468 B), AC-F2 acta `"B"`==pipeline `"B"`, AC-F4 `B+`→CONDITIONAL. **Presupuesto de 20 superado** → causa y cura en L-P3A.1 |
| FASE-P3-B | 2026-09-14 | ✅ | **120 `ids` / 120 `tool_use`** — medido con `measure_iterations.py`, corte = commit de código `bad0a5e`. **El instrumento volvió a alcanzar el transcript → D-V2.1 no se reprodujo (segunda fase).** **Presupuesto de 25 superado = tercera fase seguida** → causa y cura en L-P3B.1 | No | AC-F5 **ejecutado** (Q5=a): hoist de `ga4_available`/`gsc_available` sobre el bloque FASE-K de `main.py`; **medido**: GSC no tenía valor real que hoistear (nadie lo computaba en `v4complete`) así que hubo que calcularlo, y `gsc_configured` del MANIFEST se apuntó a la misma variable para no divergir del tier. AC-F3 cierra **D-V.1** (whitelist barreda test-only, §5.1 del contrato, **dos** mutaciones) y AC-F6 lee `VERSION.yaml` en cada escritura. R2.7 **4.130→4.153 = +23** (17 funciones; `passed` +24 = 23 nuevos + el barreda que migró de rojo a verde), **3 fallos restantes todos ajenos → 0 regresiones**. **6 pares NR7** verde/rojo; `--quick` 9/9 y hooks 7/7. Sonda de techo de tier sobre el baseline real: `B` con las cuatro combinaciones de banderas → el techo lo pone el dato, no el cableado (entrada para AC-O0). **Cero cambios en `judge.py`/`asset_reviewer.py`/`delivery_packager.py`** (`git show --stat`) |
| FASE-P2 | — | ⬜ **(se ejecuta: Q1=sí; depende de P3-A y P3-B)** | — | No | O1-cuarentena; prompt creado por P1 |
| FASE-P4 | — | ⬜ (opcional: §Cierre válido sin P4) | — | Sí (corrida) | T3a = el hotel, por contacto del operador (DA-P1.9) |
| FASE-VERIFY | 2026-09-14 | ❌ **No activa** (cerrado en P1) | n/a | No | Criterio §4.6-2 no garantizable desde la ingeniería; AC-V1 en RELEASE |
| FASE-RELEASE-4.77.0 | — | ⬜ | — | Sí | Cierre + tag + **AC-V1** (patrón VERIFY embebido) + archivado R2.5 |

**Regla L-R.1 (vigente para este archivo)**: una fila en `⬜` con celda de iteraciones en `—` **no bloquea**; lo que bloquea el ✅ de fase cerrada es `—` **después** de cerrada. Una fase diferida (P2 por Q1=no, P4 por cierre sin P4) se marca `Diferida` con la decisión como causa, nunca `—`.

---

## Decisiones Arquitectónicas / de Proceso

### D-AJUST.1 — División de FASE-P3 en P3-A / P3-B (2026-09-14)
- **Decisión**: P3 empaquetaba 6 fixes (AC8, tier del acta, AC-F4, AC-F5, barreda, versión) y excedía el máximo de R3 (≤4 tareas). Se divide: **P3-A** = AC-F1 (AC8) + AC-F2 (tier acta) + AC-F4 (primer piso `B_PLUS`) sobre `judge.py`/`asset_reviewer.py`; **P3-B** = AC-F3 (barreda, test-only) + versión del acta + AC-F5 solo condicional a Q5=(a), sobre tests/`acta_writer.py`/`main.py`.
- **Rationale**: la división es por naturaleza de cambio (detección/fidelidad del acta vs cableado/test), no por tamaño: P3-A no toca `main.py` ni `acta_writer.py`; P3-B solo toca `main.py` bajo Q5=a. Cada fase nace dentro de R3 y con baseline NR1 propio.
- **Alternativas rechazadas**: (i) agrupar "tier acta + versión acta" en una sola tarea para caber en 4 — rechaza medir R3 con agrupaciones convenientes, que es como la regla muere; (ii) dejar P3 inteira "porque son fixes chicos" — R3 existe precisamente contra el agotamiento por acumulación (la regla lo dice: el orquestador que crea fases grandes es responsable del agotamiento de las sesiones siguientes).

### D-AJUST.2 — FASE-VERIFY condicionada, no decidida en la preparación (2026-09-14)
- **Decisión**: los 3 criterios de §4.6 no son evaluables hoy porque el conteo de fases de implementación depende de Q1 (P2), Q5 (AC-F5 en P3-B) y Q4/T3a (P4). Se registra la evaluación condicional en `dependencias-fases.md` y **FASE-P1 la cierra**.
- **Rationale**: activar o excluir VERIFY con una decisión no tomada sería el gesto que L-V.4 prohíbe (decidir en la fase equivocada); el plan ya tenía el patrón embebido en RELEASE (AC-V1) sin declararlo sustituto.
- **Alternativas rechazadas**: (i) fijar "no activa" (conteo P2+P3=2) — omitía que P4 es fase con ejecución E2E y que el conteo cambia con Q1; (ii) crear ya el prompt VERIFY — prompt condicional a una decisión pendiente es un entregable falso.

### D-AJUST.3 — Escenario de cierre sin P4 (2026-09-14)
- **Decisión**: si T3a no se cierra (con Q5≠c), P4 se difiere con la misma mecánica que Q5=(c) — §Cierre válido sin P4 en `dependencias-fases.md`. El fallo de T3b no difiere: AC-O0 (`B_PLUS` con límite). Si P1 eligió Q1=(c) y T3a falla después, la resolución por defecto es O4 documentada.
- **Rationale**: sin esto, RELEASE quedaba bloqueada por una precondición externa que quizá nunca llega; con esto, el diferimiento es una decisión con rastro, no un `—`.

### FASE-P1 (2026-09-14) — decisiones DA-P1.1 … DA-P1.10

Desarrollo completo con alternativas rechazadas: `evidence/FASE-P1/decision-enforcement.md` §1.

| ID | Decisión | Rationale (una línea) | Alternativa rechazada |
|----|----------|-----------------------|------------------------|
| DA-P1.1 | **Enforcement sí** | G0 (`ROADMAP.md` §9) exige acta `APROBADO-PARA-ENTREGA`, y esa rama está cerrada por un guard que hoy no puede recibir los hallazgos: auditoría-only = **G0 incerrable por diseño** | (b) O4 documentado · (c) decidir tras P4 (inobservable con el cableado actual) |
| DA-P1.2 | **Escalar, no ciclar** | Los defectos que bloquean son estructurales (stub, asset ausente): regenerar produce el mismo stub. El `suggestion` de L-PF3 no tiene forma de restricción aquí | reintento con guard anti-bucle · entrega parcial (contradice el acta) |
| DA-P1.3 | **Orden P3-A → P3-B → P2 → P4** | Cablear dientes sobre detecciones rotas produce bloqueos inevaluables y invita a apagar el tribunal | P2 primero |
| DA-P1.4 | **O1-cuarentena** | El packager ya escribe `.zip.tmp` y renombra de forma atómica con los bytes finalizados: la decisión se mueve del *write* al **publish** sin duplicar serialización ni cambiar el contrato ZIP-only | O1-staging (layout fantasma) · O2 (memoria) · O3 (abandona single-write) · O4 |
| DA-P1.5 | **AC8 en dos capas** | Arreglar solo la lectura deja pasar el stub; arreglar solo el heurístico no sirve porque el archivo nunca se lee. **AC8 y AC-F2 son el mismo bug con dos víctimas** | (a) sola · (b) sola · dejarlo caer con el reordenamiento |
| DA-P1.6 | **Cuatro estados, no tres** | El tri-estado omitía `NOT_RUN`, que es el estado que la corrida real ya exhibe; diseñar los estados alrededor del bug dejaría P2 sin palabra para nombrar su propio fallo | tri-estado del plan · colapsar `NOT_RUN` con `READER_FAILED` |
| DA-P1.7 | **Heredar `GATE_BLOCKING_ENABLED`** + `enforcement.suppressed_by_operator` en el acta | Medido: el tribunal **no** heredaba el knob. Un solo botón para todo lo que suprime entrega, y el escape queda declarado en el artefacto en vez de ser silencio | knob sin escape · knob propio `TRIBUNAL_BLOCKING_ENABLED` (dos switches confundibles) |
| DA-P1.8 | **Q5=(a) como par con AC-F2** | Propagar banderas sin arreglar la fuente del tier es invisible: `_read_evidence_tier` seguiría leyendo `"C"` | (b) `B_PLUS` como decisión de producto (deja `APROBADO-PARA-ENTREGA` como código muerto) · (c) diferir |
| DA-P1.9 | **T3a la da el hotel, por contacto del operador** | Es la única decisión del plan que no es de diseño sino comercial; sus líneas rojas (el agente jamás llena el YAML, nada de datos plausibles para destrabar, consentimiento y frescura) son la garantía de que el tier no vuelve a mentir | que el agente o el plan "estimen" el dato · correr con defaults |
| DA-P1.10 | **FASE-VERIFY no activa** | El criterio §4.6-2 depende de que un hotel responda: atar la certificación a eso deja el cierre en `—` indefinido. La certificación se ancla al par NR7 por fase, que sí depende de este plan | crear el prompt VERIFY con una condición abierta · dejarla "condicionada" otra sesión |

**Tensión registrada, no resuelta en P1** (para que P2 no la descubra tarde): la decisión Q2b asume que los revisores leen el ZIP, y la decisión Q2 los pone a leer el `.zip.tmp`. Son el mismo archivo en regímenes distintos del mismo nombre; el contrato lo cierra fijando que **el resolutor de entrega lee un ZIP sea `.tmp` o publicado**, y P2 debe verificar además **si el acta viaja dentro del ZIP** (Tarea 1 de su prompt).

---

## Lecciones Aprendidas (mínimo 3 por fase con aprendizaje; al cerrar cada fase)

| ID | Pasó / Qué | Por qué | Cura (y si es regla sin verificador, declarada — L-R.4) | Pertinencia |
|----|-----------|---------|----------------------------------------------------------|-------------|
| L-AJUST.1 | Un plan citaba como viva (`bd2bf57`, "local sin push", "sin tag") una verdad que un rebase y un push posteriores invalidaron | Las citas de commit se escribieron en presente en la concepción y ningún check las contra-verifica al re-verificar el plan | Higiene aplicada 2026-09-14: citar el commit **vivo en origin** y distinguir en el texto lo histórico ("existen desde v2.21.0") de lo presente. Sin verificador mecánico → límite declarado (L-R.4) | INCLUIR — aplicable a todo plan largo con predecesor |
| L-P1.1 | La opción barata de Q2 estaba **fuera de la lista**: O1/O2/O3/O4 se concibieron como alternativas mutuamente excluyentes y ninguna aprovechaba que `_create_zip_single_write` ya recibe `manifest_bytes`/`implementation_order_bytes` **finalizados** y que `package()` escribe `.zip.tmp` antes del rename atómico | El menú se escribió desde el diseño concepción ("¿dónde corren los revisores?") y no desde la medición del artefacto ("¿qué costura ya existe?"); la pregunta bien hecha — *¿dónde cae la decisión?* — no estaba en la tabla | Toda pregunta de arquitectura de este plan se formula después de re-leer el artefacto y **nombra la costura existente** si la hay. Sin verificador mecánico: lo disciplina la Tarea 1 del prompt de fase → límite declarado (L-R.4) | **INCLUIR** — la clase de error que produce refactor más caro del necesario |
| L-P1.2 | El docstring de `_compute_verdict` prometía "Finding CRITICAL de revisores → DEVOLVER-CORRECCIONES" y el cuerpo de la función no tenía ni el parámetro: contrato documentado y código divergían | Un docstring escrito en T1 describió la intención del plan, no lo que se implementó, y ningún check compara el texto con la firma | En P2, la matriz del contrato se refleja en la **firma** de `_compute_verdict` y su test; un docstring no es evidencia. La lectura del texto para juzgar el comportamiento es la misma falla que L-V.2 prohíbe sobre las notas | **INCLUIR** — leer el cuerpo, no el comentario |
| L-P1.3 | La pregunta Q2b estaba formulada como "(a) **o** (b)" y la medición mostró que eran **las dos mitades de un mismo fix**; además AC8 y AC-F2 resultaron ser el mismo defecto (un lector escrito contra un layout descomprimido) con dos víctimas | El plan ereditó la disyuntiva de la concepción sin medirla; al medir las dos capas, la disyunción se autodestruyó | Antes de fijar una pregunta de decisión, **verificar que las opciones son excluyentes**. Si la medición muestra que un "o" es un "y", se re-enuncia la pregunta, no se elige una rama | **INCLUIR** — aplicable a cualquier tanda de decisiones |
| L-P1.4 | **Presupuesto superado**: 30 iteraciones declaradas ≈ 30 `ids` / 58 `tool_use` (unidad auto-reportada, D-V2.1) | Causa medida: (i) el Paso 0 de fuentes obligatorias de esta fase son **11 documentos** y hubo que leerlos; (ii) la Tarea 1 exige re-leer 14 símbolos por evidencia propia (L-V.2) y no heredar; (iii) 8 preguntas de decisión con su tanda, dos de ellas corregidas en vivo | Un presupuesto de fase debe declararse **junto con su instrumento de medición**, y D-V2.1 (el instrumento no alcanza el transcript) debería forzar a revisar el presupuesto de las fases de decisión: 30 era un número de fase de código. Propuesta para el sucesor: fase de decisión con ≥6 preguntas → presupuesto en función de `fuentes obligatorias × símbolos a re-leer`, no del tamaño del entregable. Regla sin verificador → declarada (L-R.4) | **INCLUIR** — el nº va en la celda, no en la memoria |
| L-P1.5 | Un `except Exception` que solo imprime un warning envuelve **las tres** cadenas que este plan decide (FASE-K, el bloque del Juez y el de cada revisor): en las tres, el fallo se manifiesta como **ausencia silenciosa**, no como error | No es un defecto de la fase: es el patrón never-block del repo aplicado a código que además **declara valores por defecto** (`"C"`, `[]`, `financial_breakdown = None`) | NR8/Q6 extiende su regla a los gates: ningún camino puede producir "sin hallazgos" desde "no leí". En AC-F5 el test "sin `NameError` con `generate_proposal=False`" es obligatorio por esto mismo | **INCLUIR** — patrón transversal de `main.py` (familia L-T2C.2) |
| L-P3A.1 | **Presupuesto superado otra vez**: 20 declaradas, **106 `ids` / 126 `tool_use` medidas** (corte = `0d4d072`). A diferencia de P1, el instrumento **sí alcanzó el transcript** → D-V2.1 **no se reprodujo** y la cifra es medida | Causa medida: una fase de detección/fidelidad **paga su verificación, no su código** — el delta R2.7 exige **dos corridas de la suite completa** (4.130 tests, ~7-9 min cada una) y NR7 exige **4 pares verde/rojo** que a su vez relanzan subsets; el código editado son 2 archivos. 20 era un presupuesto de "tamaño del entregable", y el entregable chico esconde la verificación cara | Presupuestar la fase por **coste de verificación** (`corridas NR1 × duración + pares NR7`), no por líneas de código. Corolario: como D-V2.1 no se reprodujo, **no hay excusa para auto-reportar** — la celda de iteraciones se mide con el instrumento. Regla sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — segunda fase seguida que revienta el presupuesto por la misma causa raíz (nº de fase de código aplicado a trabajo de verificación) |
| L-P3A.2 | AC-F4 parecía un fix de una línea (añadir el tier a `FIRST_FLOOR_TIERS`) y la clave correcta es **`"B+"`**, no `"B_PLUS"`: `EvidenceTier.B_PLUS` **serializa como `"B+"`**. Con `"B_PLUS"` el set queda con un miembro fantasma, el primer piso sigue sin aplicarse y **un test que use la forma larga del enum daría verde igual** | El fix dependía de una constante string que espeja un enum, y el nombre del enum (`B_PLUS`) no es su valor serializado (`B+`). Es la misma trampa que L-T4A.5 (test vacuo): el verde no prueba el régimen real | Cuando un fix se ancla a un string que espeja un enum, el test **assertúa contra `Enum.MIEMBRO.value`**, nunca contra el nombre escrito a mano. Los 4 tests de AC-F4 usan `"B+"` literal + una comprobación de que `EvidenceTier.B_PLUS.value == "B+"` | **INCLUIR** — familia L-T4A.5; aplicable a todo gate que compare contra enums serializados |
| L-P3A.3 | El conteo canónico había **derivado**: 4.063 (v4.76.0) → 4.109 en el PRE de P3-A, porque el plan ajeno PASO0-VERIFICADOR-CAPITALIZACION añadió ~45 funciones test entre medias. Restar contra el ancla "pre-plan" documentada habría dado un **+66 falso** y se habría leído como baseline contaminado | El número "pre-plan" del `09`/AGENTS es un **ancla histórica** de v4.76.0, no el baseline R2.7 de la fase; entre una y otra entraron commits ajenos | R2.7 se resta contra el **snapshot PRE propio de la fase**, tomado al iniciar (4.130 − 4.109 = **21** = tests nuevos, íntegro en `passed`). El ancla pre-plan se reconcilia aparte (4.063 + 45 ajenos + 21 P3-A = 4.129 canónico). Es exactamente lo que la regla R2.7 ya dice; la lección es **no confundir el ancla con el baseline** | **INCLUIR** — aplicable a cualquier fase que arranque sobre un HEAD que ya no es el del plan |
| L-P3B.1 | **Presupuesto superado por tercera fase consecutiva**: 25 declaradas, **120 `ids` / 120 `tool_use` medidas** (corte = `bad0a5e`). P1: 30→≈30/58 (auto-reporte). P3-A: 20→106/126. P3-B: 25→120/120. La serie es el hallazgo, no el caso | El presupuesto se declara **por tamaño del entregable** y lo que domina el coste es la **verificación**: el par NR1 son dos corridas de la suite completa (4.130+ tests, 7:30 y 10:21 min), y NR7 son 6 mutaciones que cada una lanza el verde y el rojo. En P3-B el código editado son **2 archivos de producción**; la verificación costó ~6× más que el fix | **Cambio de regla propuesto, no nota de fase**: que el executor declare el presupuesto de una fase con AC de detección/bloqueo como `2 × corrida_completa + pares_NR7 × corrida_subset + edición`, y no como un número fijo por "complejidad". Las tres fases de este plan que llevaron la fórmula en el encabezado ("MEDIA/ALTA") fallaron igual. **D-V2.1 queda en observación, no confirmada**: el instrumento alcanzó el transcript en P3-A y P3-B, así que la limitación que justificaba auto-reportar **no se reprodujo** y la celda debe medirse. Regla sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — tercera ocurrencia de la misma causa raíz; ya es patrón, no excepción |
| L-P3B.2 | `test_barreda_un_solo_emisor_de_la_clave` tenía **dos** aserciones y solo una se ejecutaba: la de `emisores` fallaba desde v4.76.0, así que la de `consumidores` —escrita debajo— **nunca fue evaluada**. Al arreglar la primera resultó que la segunda también estaba rota (Bot 3 consume `entry.get("asset_path")` desde que existe) | Un test en rojo por su primera causa deja **sin vigilar todo lo que hay después de esa causa**, y el rojo visible ocultaba un segundo defecto. Es la cara opuesta de L-T4A.5: allí un verde no probaba nada; aquí un **rojo** no probaba nada de lo que venía después. NR7 mira si el test *puede* fallar; no mira si **llegó** a la aserción | (i) Al cerrar un test que llevaba rojo, **ejecutar y leer la salida completa**, no solo el cambio de color: el primer mensaje de error distinto al esperado es la señal de la aserción sucesiva rota. (ii) A una whitelist no le basta la mutación "retiro la autorización → rojo" (que es la que pedía el plan): se agregó **"aparece un tercero no contratado → rojo"** (`NR7-AC-F3-b.txt`), porque una contención que solo puede fallar por retirada es una barra decorativa. (iii) Candidato a regla: separar en dos tests las aserciones que hoy comparten un cuerpo, para que una no enmascare a la otra | **INCLUIR** — familia L-T4A.5/L-VUP-5, con un modo nuevo (rojo que oculta, no verde que miente) |
| L-P3B.3 | La promesa de Q5/AC-F5 decía "propagar la **disponibilidad real**", y medido en `main.py` solo existía la mitad: `ga4_available` se calculaba y se tiraba, pero `gsc_available` **no se calculaba en ninguna parte** del flujo — `AnalyticsStatus` lo declara en `False` y nadie lo asigna en `v4complete`. Propagar solo lo que había habría dejado el Tier A **exactamente igual de inalcanzable** (la regla exige `ga4_enabled and gsc_enabled`) y el AC se habría cerrado en verde sin arreglar el síntoma que lo justificó | El plan heredó del párrafo §2.1 del maestro una premisa formulada en plural ("la disponibilidad real se calcula más abajo") y la midió solo en su caso GA4. La restricción "NO re-decidir Q5" (L-VUP-6) **no** obliga a ejecutar literalmente un enunciado cuyo input resultó no existir | Antes de aplicar un fix, **verificar la premisa de cada una de sus mitades**: "¿existe ya un valor real que propagar, para cada fuente?" Si no existe, el fix es *calcularlo*, y eso hay que decirlo en la fase —no es re-decidir Q5, es ejecutarla de verdad. Y cuando calcularlo abre un segundo lector divergente (`gsc_configured` del MANIFEST leyendo el campo muerto), se cierra en la misma fase por **L-SR3**, no se deja para otra | **INCLUIR** — el fix que se habría cerrado falso de no medirse; aplicable a todo "propagar el valor real" |

---

## Métricas de Ejecución (al cerrar cada fase)

- Tests canonónicos: pre-plan 4.063 / 293 archivos (medido 2026-09-11, v4.76.0) — actualizar con delta R2.7 por fase. **P1: 4.063 / 293 sin cambio** (fase de decisión, cero código, cero tests nuevos ni tocados; verificado por `git status` en el commit de cierre).
- `--quick`: 9 checks desde `4ac139a` (eran 8 al cierre del predecesor). **P1: 9/9 al cierre (2026-09-14 14:00).** Además, el pre-commit aportó un dato propio: el hook `[6/7]` **rechazó el primer intento** de `fd8e4f4` porque editar los `.md` del plan vence el índice de lecciones generado — la regla existe, falla fuerte y obligó a regenerar el par `.md`+`.json`. Es el patrón que este repo prefiere (verificador que truena antes que disciplina manual).
- Coherence / publicación: sin cambio en P1 (no hay corrida).
- Archivos tocados por P1: `evidence/FASE-P1/` (2 nuevos), `05-prompt-inicio-sesion-fase-P2.md` (nuevo), y los **7** documentos de plan actualizados (`00`, `01`, `06`, `README`, `dependencias`, este `10`, más `09`).
- **FASE-P3-A (2026-09-14)**: canónico **4.129 funciones / 296 archivos**; R2.7 **PRE 4.109 → POST 4.130 = +21** propios, íntegro en `passed` (4.070→4.091), con los **4 fallos ajenos idénticos** pre/post (`test_faq_generator_output_is_jsonld`, `test_function_default_flags` flaky, `test_barreda_un_solo_emisor_de_la_clave` [deuda P3-B], `test_diagnostic_includes_geo_metrics`) → **0 regresiones**. `--quick` **9/9** al cierre (743 citas históricas, 0 nuevas, 0 crecimientos). Tests del tribunal **114 → 135**. Coherence/publicación **sin cambio** (no hay corrida en esta fase). Archivos tocados: **2 `.py` de producción** (`asset_reviewer.py`, `judge.py`), **1 test nuevo** (`test_p3a_zip_tier_firstfloor.py`, 21 tests), **1 fixture editada** (`test_judge.py`), `evidence/FASE-P3-A/` y los **6 docs de plan** del cierre (`06`, `dependencias`, `README`, `09`, este `10`, `00`). Commit de código `0d4d072` (14 archivos, +1.749/−75, hooks 7/7).
- **FASE-P3-B (2026-09-14)**: canónico **4.146 funciones / 298 archivos**; pytest recolecta **4.153** (6 de las 17 funciones nuevas están parametrizadas con 2 casos). R2.7 **PRE 4.130 → POST 4.153 = +23**, con `passed` 4.091→4.115 = **+24** porque además migró de rojo a verde `test_barreda_un_solo_emisor_de_la_clave`; **3 fallos en POST, los tres ajenos y ya presentes idénticos en el PRE** → **0 regresiones**. `--quick` **9/9** y pre-commit **7/7** en `bad0a5e`. Tests del tribunal **135 → 158** (recolectados). **Se cierra D-V.1**: la fila de Tests de `AGENTS.md` queda desactualizada por este plan y su actualización es de RELEASE con el bump. Coherence/publicación **sin cambio** (no hay corrida en esta fase; `v4complete` explícitamente fuera de alcance por la restricción del prompt). Archivos tocados: **2 de producción** (`main.py`, `acta_writer.py`), **4 de test** (2 nuevos, 1 ampliado, 1 edición test-only), `evidence/FASE-P3-B/` (13 archivos) y los **6 docs de plan** del cierre. Commit de código `bad0a5e` (19 archivos, +1.993/−18). **Verificación explícita de líneas rojas**: `git show --stat bad0a5e` confirma **cero cambios** en `judge.py`, `asset_reviewer.py` y `delivery_packager.py`. Iteraciones: **120 `ids` / 120 `tool_use`** con el instrumento (corte = el commit de código), presupuesto 25 superado → L-P3B.1.

---

## Seguimientos Abiertos

- ~~Empujar el tag `v4.76.0`~~ — **hecho 2026-09-14**: master `8cbcb05..3f026d2` y tag `v4.76.0` (`2df9dbc`→`3bdc14e`) en origin, tras L3 sin hallazgos. La convención sigue para los futuros: los tags no se empujan solos, se preguntan.
- La rama local `backup/pre-sanidad-evidence-20260912` conserva el objeto `bd2bf57`: candidata a podar cuando el usuario confirme que nada la referencia (no borrar desde un agente sin pedirlo).
- **T3a sin cerrar (Q4/DA-P1.9)** — dueño: **el operador**, con la jerarquía Salento Real → Don Alfonso/Luxor → Zi-One. Tres líneas rojas: el agente **jamás** llena el YAML de onboarding; **nada** de datos plausibles "para destrabar"; consentimiento y frescura registrados. Si al iniciar P4 no hay dato con fuente → §Cierre válido sin P4 **sin reabrir decisión**. No es una precondición "en espera": es un diferimiento con disparador y dueño escritos.
- ~~**`test_barreda_un_solo_emisor_de_la_clave` en rojo**~~ → **CERRADO por FASE-P3-B (AC-F3, `bad0a5e`)**. La whitelist quedó en el test (`EMISORES_LEGITIMOS`/`CONSUMIDORES_LEGITIMOS`) justificada por §5.1 del contrato, con la igualdad de conjuntos intacta y dos mutaciones (`NR7-AC-F3-a.txt` retira la autorización, `NR7-AC-F3-b.txt` hace aparecer un tercero no contratado). **El emisor no se tocó**, que era la promesa exacta de D-V.1. Medido al cerrarlo: la aserción de `consumidores` llevaba sin evaluarse desde que existe y también estaba rota → L-P3B.2
- **`analytics_status.gsc_available` sigue sin asignarse en `v4complete`** → **divergencia nueva registrada, no arreglada (L-V.4)**, con dueño propuesto. Tras AC-F5 el hecho "GSC está configurado" tiene una sola fuente para el tier y para el `MANIFEST.json`, pero el **diagnóstico** sigue leyendo `analytics_status.gsc_available` (siempre `False`) y su `summary_for_template()` es la que afirma "datos de búsqueda orgánica **incluidos**" — aunque el pipeline no los incluya. No se asignó aquí porque el pipeline **no consume** datos de GSC: poner la bandera sin consumir el dato convertiría un "no configurado" honesto en una afirmación falsa. **Dueño**: plan de analítica (T3b) — o FASE-RELEASE si se prefiere abrir un AC propio. Detalle en `evidence/FASE-P3-B/constancia-Q5.md` §4
- **El acta MD declara una fuente que ya no es la fuente** → `acta_writer.py` imprime en la sección del primer piso ``**Artefacto fuente**: `MANIFEST.json` (evidence_tier)``, pero **AC-F2 (P3-A)** cambió la fuente real a `financial_scenarios_*.json → breakdown.evidence_tier`, con MANIFEST solo de fallback. Es de la familia que este plan denuncia (el documento describe un mecanismo que el código ya no usa), y **no se arregló en P3-B porque ningún AC lo autoriza**: P3-B solo podía tocar la versión del acta (AC-F6). **Dueño natural: FASE-P2**, que ya va a editar `acta_writer.py` para quitar el guard `if reviewer_reports:` (DA-P1.6 regla 1) — que el valor salga del propio `first_floor_rule` del acta, no de un literal
- **El techo de tier de P4 ya no puede atribuirse al cableado** → AC-O0 (`evidence/FASE-P4/informe-observacion.md` §Techo de tier) pierde su mitad de excusa: con AC-F5 cerrado, un `B_PLUS` en la corrida de observación significa **una de dos cosas externas** y hay que nombrar cuál — (i) el hotel no tiene GA4+GSC configurados (T3b), o (ii) los tiene pero sus fuentes de breakdown no son verificadas (`adr`/`canal` no vienen de onboarding). La sonda `verify_probe_ac_f5_techo_tier.py` mide el segundo caso sobre el baseline real de FASE-I: `B` con las cuatro combinaciones de banderas
- **Cola de 78 adyacentes sin evaluar (Q7 del Paso 0)** → **declarada fuera de alcance en P1**, con razón escrita (§6.1 de `decision-enforcement.md`): ninguno toca los símbolos del plan y la pasada se hace por síntoma, no por volumen. Si una fase de código descubre un candidato que sí nombra uno de sus símbolos, se capitaliza **ahí** y se anota en el `00-`.
- **"Verificador de conteos declarados en §4"** (llegó del plan PASO0 con la premisa ya corregida) → **RETIRADO en P1 con medición**: el §4 de `00-lecciones-capitalizadas.md` declara 19 lecciones y el §2 tiene 19 filas. No se hereda el ítem ni se abre AC.
- **Tensión que hereda P2** (registrada arriba, en DA-P1.4/§final): Q2b asume ZIP publicado y Q2 pone los revisores sobre el `.zip.tmp`. El contrato lo resuelve con un resolutor que lee un ZIP en cualquiera de los dos estados — P2 no puede resolverlo improvisando.
- **`acta_writer.py` con la versión hardcodeada** → pasó de nota de fase a **AC-F6** con artefacto y mutation check, y **quedó cerrado en FASE-P3-B** (`bad0a5e`): `_read_project_version()` lee `VERSION.yaml` en cada escritura, el footer ya no lleva un literal, y el test lo verifica contra el YAML del disco y contra un YAML falso distinto (nunca contra un valor escrito a mano). `evidence/FASE-P3-B/NR7-AC-F6.txt` trae el par verde/rojo de la mutación que pide el contrato

---

## Lecciones capitalizadas de planes anteriores

Fuente canónica: `00-lecciones-capitalizadas.md` (Paso 0 horizontal del 2026-09-12 + capa fría del índice). Su tabla §2 (19 lecciones con dueño y efecto) **es** esta sección; no se duplica. Lo que aquí se registra es el contraste promesa↔realidad al cerrar cada fase:

| Fase | Lecciones que la gobernavan | Se aplicó de verdad (evidencia) | No aplicó / parcial (causa) |
|------|-----------------------------|----------------------------------|------------------------------|
| FASE-P1 | L-V.2 (re-leer, no heredar), L-V.1 (fixture ≠ régimen), L-SR5/L-PF3 (bloqueo con consecuencia), L-PF6/L-PF10 + DA-C3 (ausencia ≠ hallazgo), L-T2C.2 (`except` anchos), L-VUP-6 (delegar solo lo decidido), R2.2 (símbolos), R2.4 (AC en artefacto), §15.4.1 (contrato antes que implementadores) | **L-V.2**: los 14 símbolos se re-leyeron y **§2.1 quedó confirmado con evidencia propia**, no heredada — y de esa re-lectura salieron tres hechos que el plan no tenía (`research-estado.md` §3). **L-VUP-6**: Q1b/Q5/Q6/Q7 se cerraron **aquí**; el brief de P4 y el prompt de P2 reciben decisiones, no preguntas. **R2.4**: los 17 ACs del §6 llevan artefacto + clave. **§15.4.1**: el contrato se fijó antes de que exista una línea de P2 | **L-PF3 en su letra**: la cura "regenerar con el `suggestion` + 1 reintento" **no** se aplicó porque los defectos bloqueantes son estructurales (DA-P1.2 la rechaza con causa) — se conserva el espíritu (bloqueo con camino de reparación), no el mecanismo. **R2.1**: la medición no la hizo el instrumento sino auto-reporte con unidad declarada (D-V2.1). **NR7**: P1 no produce mutation check (fase sin código); queda escrito AC por AC para P2/P3-A/P3-B |
| FASE-P3-A | L-V.1 (fixture ≠ régimen real), NR7/L-T4A.5/L-T2C.4/L-VUP-5 (mutation check por AC), NR8/L-PF6/L-PF10 (ausencia ≠ hallazgo), L-V.2 (re-leer, no heredar), L-SR3 (una fuente de verdad por hecho), R2.4 (AC legible en artefacto), R2.6/R2.7 (baseline real + delta por resta), DA-P1.5 (AC8 y AC-F2 = mismo defecto, dos víctimas) | **NR7**: 4 pares verde/rojo reales (`nr7_mutation_checks.py` restaura el árbol y comprueba el rojo por capa y por AC), no un verde por fase. **R2.6**: los 2 tests ZIP/dir del baseline real **corrieron** (baseline disponible), no se saltaron. **L-V.1**: la detección se probó contra el **stub real de 468 B**, no contra un fixture ingenuo — es exactamente lo que hacía fallar a AC8. **NR8**: `_impl_order_check` publica `OK`/`ARTIFACT_MISSING`/`READER_FAILED`/`NOT_RUN` y un fallo de lectura **nunca** emite "vacío". **L-SR3/DA-P1.5**: `"B+"` corregido en una sola fuente | **L-P1.4/L-R.1 en su cura**: el presupuesto seguía siendo "nº de fase de código" (20) y volvió a explotar (106/126) → nace L-P3A.1; la novedad es que **esta vez la cifra es medida**, no auto-reportada (D-V2.1 no se reprodujo). **R2.7 contra el ancla**: el riesgo del +66 falso (L-P3A.3) se evitó por usar el snapshot PRE propio, no el pre-plan de v4.76.0 — la regla ya lo pedía, P3-A lo ejecutó |
| FASE-P3-B | L-T2C.2 (`except` anchos que enmascaran un NameError), NR7/L-T4A.5/L-VUP-5 (mutation check por AC), L-VUP-1 (par NR1 con combinación declarada y flaky nombrado), L-V.4 (un AC que falla se documenta con dueño, no se arregla en otra fase), L-SR3 (una fuente de verdad por hecho), L-V.2 (re-leer, no heredar), R2.2 (símbolos, no líneas), R2.4 (AC legible en artefacto), R2.7 (delta por resta), DA-P1.8 (Q5=a como par inseparable con AC-F2), §5.1 del contrato (el segundo emisor de `asset_path`) | **L-T2C.2**: verificada **por AST**, no por lectura — `test_hoist_fuera_de_todo_except_ancho` y `test_hoist_alcanzable_donde_lo_es_fase_k` comprueban que ningún `try` con handler ancho envuelve el hoist y que está tan accesible como su consumidor, y las dos tienen mutación que las pone rojas (`NR7-AC-F5-b/c.txt`). **NR7**: **6 pares**, y a AC-F3 se añadió la mutación que el plan no pedía (aparece un tercero no contratado) porque retirar la autorización por sí sola no prueba que la barra no sea decorativa. **L-VUP-1**: PRE tomado **antes** de la primera edición, con la combinación de archivos escrita y `test_function_default_flags` nombrado. **L-SR3**: eliminar la segunda computación de `ga4_available` y apuntar `gsc_configured` a la variable única. **R2.4**: los tres ACs son legibles en su artefacto (`revision_assets.json`/`acta_revision.md`/`financial_scenarios_*.json`) y su clave. **§5.1**: la whitelist cita el contrato que la justifica, no es un `xfail` | **L-P3A.1/L-R.1 en su cura**: la cura de P3-A era "presupuestar por coste de verificación" y **no se aplicó** al declarar P3-B: siguió un número fijo (25) y volvió a explotar (120) → nace L-P3B.1 pidiendo el cambio de **regla**, no otra nota. **L-V.2 a medias**: se re-leyeron los símbolos del bloque FASE-K y del barreda, pero **no la premisa de la segunda fuente** — GSC se descubrió muerto al intentar propagarlo (L-P3B.3); medir el síntoma no es medir cada mitad del fix. **D-V2.1**: queda **en observación**, no confirmada — el instrumento alcanzó el transcript por segunda fase, así que la coartada del auto-reporte se debilita. **L-V.4 cumplida en negativo**: la divergencia `diagnóstico↔tier` y el `` **Artefacto fuente**: MANIFEST.json`` obsoleto del acta **no** se arreglaron de paso pese a vivir en el archivo que P3-B editaba; se registraron con dueño (`constancia-Q5.md` §4 y Seguimientos) |
| ⟨P2, P4…⟩ | | | |

Capa fría y consultas literales: §1 de `00-lecciones-capitalizadas.md`. Límite declarado (L-R.4): la **forma** del Paso 0 la verifica `scripts/validate_lesson_capitalization.py` desde `e02a688`; su **pertinencia** sigue disciplinada por lectura humana — este plan no debe dar por verificado lo que ningún check mira. **Estado al cerrar P1**: el §2 del `00-` sigue en 19 filas (ninguna lección nueva de P1: lo producido son **decisiones**, que viven en §1 de este archivo) y sus **4 hallazgos de §3.b quedaron resueltos** (ver §6.1 de `decision-enforcement.md`). **Estado al cerrar P3-A**: el §2 del `00-` **sigue en 19 filas** — P3-A no capitalizó ninguna lección externa nueva; **consumió** seis de esas 19 (L-V.1, NR7/L-T4A.5/L-T2C.4/L-VUP-5, NR8/L-PF6/L-PF10, L-V.2, L-SR3, R2.6/R2.7) sobre artefactos reales. Las tres lecciones propias de P3-A (**L-P3A.1** presupuestar por coste de verificación, **L-P3A.2** serialización del enum `"B+"`, **L-P3A.3** baseline R2.7 ≠ ancla pre-plan) son de **este plan** y viven en la tabla de arriba. **L-P3A.1 y L-P3A.2 son candidatas a ascender al índice de capa fría en el archivado de RELEASE** para que el sucesor no vuelva a presupuestar una fase de detección con un número de fase de código. **Estado al cerrar P3-B**: el §2 del `00-` **sigue en 19 filas** — P3-B tampoco capitalizó lección externa nueva; **consumió** cinco (L-T2C.2, NR7/L-T4A.5/L-VUP-5, L-VUP-1, L-SR3, L-V.4) y le añadió una fila de estado propia al `00-`. Sus tres lecciones (**L-P3B.1** presupuestar por coste de verificación como **cambio de regla**, no como nota; **L-P3B.2** un rojo deja sin vigilar lo que está después de su primera causa; **L-P3B.3** "propagar el valor real" hay que medirlo **por fuente**, porque la segunda puede no existir) son de **este plan** y viven en la tabla de arriba. **L-P3B.1 y L-P3B.2 son candidatas a la capa fría en el archivado**: la primera es la tercera ocurrencia de la misma causa raíz y ya no es anecdótica; la segunda nombra un modo de test vacuo que la familia L-T4A.5 no cubría (no un verde que miente, sino **un rojo que oculta**).
