# Lecciones capitalizadas — REFACTOR-WHATSAPP-ENTREGA-2026-09-18

Creado el 2026-09-18, antes del maestro y los prompts. Preparación documental; ninguna implementación ni corrida ejecutada. Actualizar al cierre de cada fase.

**Revisión 2 (2026-09-19).** Esta revisión existe porque la preparación consultó el corpus **por las palabras del síntoma** ("WhatsApp promesa asset sin dato…") y no **por la superficie que el plan toca ni por su artefacto de cierre** (acta, ZIP, veredicto). Al rehacer la consulta por ese segundo eje aparecieron cinco lecciones ya definidas que invalidaban premisas del maestro y una que señala la causa por la que su corrida única quedaría suprimida. Añadidas Q7-Q10 y las filas `L-E2E.1`, `L-E2E.2`, `L-V.3`, `L-T2A.2`, `L-PF11`, `L-VUP-13`, `DA-P1.9`.

## 1bis. Mediciones de la revisión 2 (con instrumento, no con lectura)

| # | Superficie | Comando o medición | Resultado |
|---|---|---|---|
| M1 | Lector de presencia | `SitePresenceChecker._check_html_element(url, ["whatsapp","wa.me","chat"])` sobre las tres URLs del hotel | `https://www.donalfonsohotel.com/` y `https://donalfonsohotel.com/` → `found=True` vía `css_class: joinchat joinchat--left joinchat--btn`, `whatsapp_href_number` ausente; `https://hoteldonalfonso.com/` → `found=False`. **Refuta** la fila "el detector no puede ver el botón que existe" del maestro §1 |
| M2 | Corrida real existente | `output/TAREA7-2026-09-19/` (v4complete, 2026-09-19 15:01, mismo hotel y URL del §5) | `acta_revision.verdict: "BLOQUEADO"`, `evidence_tier: "B"`, readiness `READY_FOR_PUBLICATION` con **13/13 gates verdes**; un único CRITICAL `VACUOUS_RECALL`; ZIP `.zip.tmp` suprimido tras registrar `member_count: 52`. `coherence_validation.checks[whatsapp_verified]` = `passed: true, score: 1.0, message: "No hay asset de WhatsApp button"` (verde vacuo); `pain_ledger.entries` con 12 pains, **ninguno** de WhatsApp |
| M3 | Contrafactual del veredicto | `TribunalJudge._compute_verdict` invocado **en memoria** sobre el acta de M2 (solo lectura; sin mutar `output/`) | Con el hallazgo registrado → `BLOQUEADO`; con `details.critical_issues_count` fundado y `verdict_recommendation` recalculada → `APROBADO-CONDICIONAL-PENDING-ONBOARDING`, no bloqueante por no estar en `BLOCKING_VERDICTS`. **Intermedio falso:** hacer zero de `critical_count` sin recalcular la recomendación **no** volta el veredicto, porque `ReviewerReport.verified_block` bloquea igual. Medido y corregido en la misma revisión |
| M4 | Blast radius de AC19 | conteo sobre `tests/` **por el método canónico del repo**: base `grep -rlE "whatsapp_button\|site_presence_report" tests --include=*.py` y `grep -hE "^\s*def test_"` sobre esa lista; subconjunto de estado hardcodeado = esa lista filtrada por `xargs grep -lE '"(status|site_verified|presence_status)"'` (comillas dobles literales; con comilla simple admisible serían 32/552) y el mismo conteo de funciones sobre ella | 52 archivos / **816** funciones canónicas (866 casos collectados por pytest); **545** funciones en los **31** archivos que hardcodean `status`/`site_verified`/`presence_status`; **4** asserts de igualdad exacta sobre la forma; 8 consumidores en producción. Dimensionó la división AC19a/AC19b del maestro §2. **Segunda confirmación medida de `L-ENT.4` y de `L-V.3`** (sin ID nuevo: la convención ya está definida y repetir el patrón no aporta una fila): la primera medición de esta fila (50 archivos / **779** / 328 / 2) provenía de un **subagente** y se propagó a los documentos del plan sin re-medirla contra el método canónico. Lo predicho por `L-V.3` —"la premisa del writer ya serializa X cae contra el artefacto real"— se cumplió sobre una cifra propia; lo de `L-ENT.4` —"un estado heredado caduca"— se cumplió en cuatro días. **El patrón se repitió una vez dentro de la propia revisión:** el par intermedio **26 / 476**, anotado el 2026-09-19 sin criterio reproducible, cayó ante los trece criterios medidos (banda 22/427 → 32/552) y fue sustituido por 31 / 545 con el criterio de arriba. Trazabilidad completa en el anexo de anclas |

Anclas de línea medidas el 2026-09-19 en HEAD 938f59f: `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/REVISION-2/anclajes_medidos.json`

## 1. Consultas ejecutadas

| # | Capa | Consulta literal | Resultado |
|---|---|---|---|
| Q1 | Índice completo | `python scripts/build_lesson_index.py --check` | Índice fresco, 305 IDs antes de crear este plan. |
| Q2 | Índice completo | `Grep(pattern="L-NC6\|L23\|L19\|L-PF6\|L-PF10\|L-T4A.5\|L-VUP-5\|L-R.4\|L-NC10\|L-V.1\|L-SR5\|L-HF1", path=".opencode/LECCIONES-INDEX.md", output_mode="content")` | Se consultó el corpus completo; se contrastaron las definiciones pertinentes en sus análisis, no solo sus resúmenes. |
| Q3 | QMind, notebook iah-cli-lecciones | `qmind retrieve --nb 01a04d98-b7bd-778c-8441-26fdc7e35f45 -q "WhatsApp promesa asset sin dato requerido detect_pains caller cableado" --max-results 3 --format agent --non-interactive` | 3 resultados: fugas narrativas, RC1-RC2 y DT-4 histórico. |
| Q4 | QMind, notebook iah-cli-lecciones | `qmind retrieve --nb 01a04d98-b7bd-778c-8441-26fdc7e35f45 -q "IMPLEMENTATION_ORDER stub evidencia ZIP VERIFY mutation checks onboarding dominio" --max-results 3 --format agent --non-interactive` | 3 resultados: dos del plan de enforcement y uno del tribunal offline. Advertencia: hay correcciones P6/P6-R posteriores a P4; el código actual decide si sigue existiendo el defecto. |
| Q5 | Memoria del proyecto | `Read("C:/Users/Jhond/.qoder/projects/C--Users-Jhond-Github-iah-cli/memory/project-edits-de-plan-vencen-el-indice-de-lecciones.md")` | Regenerar índice MD+JSON tras escribir el plan; no editarlo manualmente. |
| Q6 | Memoria del proyecto | `Read("C:/Users/Jhond/.qoder/projects/C--Users-Jhond-Github-iah-cli/memory/reference-qmind-lecciones.md")` | Verificar contenido del write-back: un SKIP por título no prueba que se haya publicado el cierre actualizado. |
| Q7 | Índice completo, **segundo eje** | `grep -nE -e corrida -e única -e intento -e supres -e cuarentena -e verificador -e "L-PF6" -e "L-NC6" .opencode/LECCIONES-INDEX.md` (2026-09-19, sobre 421 líneas; `-e` repetidos equivalen a la alternancia y evitan pipes dentro de la celda) | Recuperó lo que Q1-Q3 no habían traído: `L-E2E.2`, `L-V.3`, `L-T2A.2`, `L-E2E.1`, `L-PF11`, `L-VUP-13`, `DA-P1.9`. Confirmación medida de que la cobertura de la consulta depende del eje con que se formula, no del tamaño del corpus |
| Q8 | Corpus de evidencia | `grep -rn "critical_issues_count" --include=*.md .opencode/plans/ evidence/` | `evidence/FASE-I/comparacion-vs-baseline.md`, en su sección «2. NRs verificados», ya medía el delta `{critical_issues_count: 0, recall_basis: …}` frente a `{}`. El defecto de AC20 estaba documentado como observación y nunca recibió dueño ni AC |
| Q9 | QMind, notebook iah-cli-lecciones | `mcp__plugin_qoder-qmind_qoder-qmind__retrieve` (nb `01a04d98-…`, dos intentos: presencia/WhatsApp y acta/cuarentena) | **DENEGADO por el clasificador de permisos en Auto mode.** No se reintentó ni se evadió. La revisión 2 se construyó solo con el índice local y sus fuentes en `Archives/`; límite declarado en §4 y en el maestro §7 |
| Q10 | Definiciones, no resúmenes | Lectura de las filas en su documento definitorio: `L-E2E.1` en `Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` (sección «FASE-E2E (2026-09-11)»), `L-PF11` en `Archives/SR-PIPELINE-FIXES-2026-08-27/10-analisis-post-implementacion.md` (sección «Lecciones nuevas de este plan (L-PF1+)»), `L-VUP-13` en `Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` (sección «Lecciones nuevas de este plan (numeración L-VUP-n)») y `DA-P1.9` en `Archives/TRIBUNAL-ENFORCEMENT-OBS-2026-09-11/10-analisis-post-implementacion.md` (sección «FASE-P1 — decisiones DA-P1.1 … DA-P1.10») | Cada fila capitalizada se leyó en su documento definitorio, no en el resumen del índice |

Anclas de línea medidas el 2026-09-19 en HEAD 938f59f: `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/REVISION-2/anclajes_medidos.json`

## 2. Lecciones capitalizadas

| ID | Enunciado | Definida en (ruta) | Qué cambia en ESTE plan | Dónde se aplica |
|---|---|---|---|---|
| L-NC6 | El cable perdido se busca en el caller, no creando otra fuente. | `.opencode/plans/Archives/REFACTOR-COHERENCIA-NARRATIVA-2026-08-22/10-analisis-post-implementacion.md` | AC1 y AC7 exigen igualdad de pains y descubrimiento de callers, incluyendo detect_pains. | Cableado y verificador AST. |
| L-NC10 | La narrativa estática puede contradecir un ledger correcto. | `.opencode/plans/Archives/REFACTOR-COHERENCIA-NARRATIVA-2026-08-22/10-analisis-post-implementacion.md` | AC2 y AC10 cruzan propuesta, identidad de servicio, guía y ledger; no basta quitar una entrada del catálogo. | Promesa y entrega. |
| L-T4A.5 | Un test verde puede no alcanzar la rama que dice certificar. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | AC1 y AC6 usan entradas producibles y mutaciones del símbolo real; se prohíbe certificar la rama HTML inalcanzable. | Implementación y VERIFY. |
| L-V.1 | Contenido y layout reales son dos ejes distintos del contrato. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | AC10 lee IMPLEMENTATION_ORDER del ZIP escrito por DeliveryPackager, no un MD fabricado. | Entrega y VERIFY. |
| L-V.4 | VERIFY registra fallos con dueño; no los remedia. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | AC18 separa SUPERADO, FALLA y NO EJERCITADO; prohíbe reparar o repetir la corrida en VERIFY. | VERIFY. |
| L-R.4 | Una regla sin verificador declara expresamente su límite. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | AC7 publica cobertura y límites del AST; AC17 limita la cuenta de ejecuciones al runner, no a comandos externos. | Verificador y corrida única. |
| L-PF6 | Ausencia observada y lector fallido no son equivalentes. | `.opencode/plans/Archives/SR-PIPELINE-FIXES-2026-08-27/10-analisis-post-implementacion.md` | AC9 exige estados separados al leer evidencia; fallo de red no prueba ausencia de canal. | Preflight, análisis y cobertura de lectores (B/C). |
| L-PF10 | Una lista vacía válida no equivale a falta de fuente. | `.opencode/plans/Archives/SR-PIPELINE-FIXES-2026-08-27/10-analisis-post-implementacion.md` | AC9 cubre vacío, ausente y lectura fallida sin rellenar defaults ni crear pains por el parser. | Lectores y cobertura. |
| L-VUP-5 | Un contrato ya verde exige mutación para demostrar sensibilidad. | `.opencode/plans/Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` | AC10 revalida el writer actual sin reimplementar fixes ya hechos; desactivar el guard debe producir rojo. | Entrega y no-regresión. |
| L-VUP-9 | Los comandos delegados deben usar flags comprobados. | `.opencode/plans/Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` | AC14 y AC17 verifican parser, identidad del hotel y argumentos antes de consumir la única corrida. | Onboarding, preflight y E2E. |
| L-VUP-12 | La evidencia se conserva antes de analizar. | `.opencode/plans/Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` | AC17 exige snapshot saneado con hashes y exit code antes del análisis; baseline P4 inmutable. | E2E. |
| L-VUP-17 | Los tests locales no certifican integración renderizada. | `.opencode/plans/Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` | AC18 cruza los ACs con los artefactos de la única corrida; una rama no ejercitada no se declara superada por ese hotel. | VERIFY. |
| L-E2E.2 | La predicción de que "en la corrida E2E el gate ya declara `details` y el recall vacuo no dispara" resultó falsa: el `gate_report` real llegó con `critical_recall.details: {}` y Bot 1 emitió el CRITICAL. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | Es la causa de AC20 y de la FASE-0. El plan la heredó sin saberlo: su corrida única estaba destinada a reproducirla. Cambia la meta del §4, que pasa de "READY" a "ZIP publicado con acta legible" | FASE-0, AC20, AC12, AC18 |
| L-V.3 | Las predicciones del plan son hipótesis, no hechos: la de T2-A sobre `details` cayó contra el artefacto real. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | Toda premisa de este maestro que dependa de "el writer ya serializa X" debe re-medirse contra un artefacto producido, no contra el código fuente. Aquí se violó: la premisa del lector ciego se midió con un sondeo ad hoc y no con la corrida del mismo día | FASE-0, AC20, §1 y §7 del maestro |
| L-T2A.2 | Fundado vs vacuo se había certificado solo con fixture, no con corrida real. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | AC20 exige el par verde/rojo sobre el `gate_report` real archivado y sobre el contrafactual con `_compute_verdict`, no sobre un JSON fabricado | FASE-0, AC20 |
| L-E2E.1 | El punto de cableado importa tanto como el cableado: `MANIFEST.json` y `ASSETS/` solo existen después de `packager.package()`, y el Juez corre antes. | `.opencode/plans/Archives/TRIBUNAL-OFFLINE-2026-09-09/10-analisis-post-implementacion.md` | Fundamenta AC11 y AC12: tres de los cuatro revisores no leen el ZIP, y los que buscan `deliveries/*/MANIFEST.json` por directorio nunca lo encuentran en modo ZIP-only. También explica por qué AC10 debe capturarse dentro de la corrida | E, AC10, AC11, AC12 |
| L-PF11 | Dos corridas del mismo hotel comparten identidad de memoria aunque los directorios de salida sean distintos: la segunda encontró el análisis de la primera. | `.opencode/plans/Archives/SR-PIPELINE-FIXES-2026-08-27/10-analisis-post-implementacion.md` | Destruye el supuesto de aislamiento del §5: `--output` no aísla `.agent/memory` ni `find_latest_analysis`. AC17 incorpora snapshot de memoria y declaración de análisis reutilizable | H, E2E, AC17 |
| L-VUP-13 | "Using defaults (no fresh onboarding data found)" en el log es condición de equivalencia, no un fallo; hay que mirar el loader y el log antes de un E2E de comparación. | `.opencode/plans/Archives/VALIDADOR-URL-PROPIA-2026-08-30/10-analisis-post-implementacion.md` | AC14 se reescribe: la prueba es **qué sucursal tomó el loader** (YAML derivado / warehouse / defaults) y el `canonical_url` con que buscó, no la existencia del archivo. El nombre del hotel ni siquiera participa en el matching | A, H, AC14 |
| DA-P1.9 | La decisión comercial de "quién aporta el dato" es la única que no es de diseño: el agente jamás llena el YAML ni estima datos plausibles para destrabar. | `.opencode/plans/Archives/TRIBUNAL-ENFORCEMENT-OBS-2026-09-11/10-analisis-post-implementacion.md` | Frontera explícita de H: derivar el YAML con `_observation_to_onboarding_format` es una **transformación trazable de fuente existente**, no una llenada; los campos que el transformador no propaga (`adr_cop`, `occupancy_rate`) se declaran no disponibles y no se estiman | H, AC14 |

## 3. Candidatos evaluados y descartados

| ID | Por qué NO se capitaliza como tarea de este plan |
|---|---|
| L-T4A.1 | No se introduce un protocolo de extracción LLM; modificar runtime_checkable no ataca este defecto. |
| L-T4A.2 | El parsing de cercas Markdown de respuestas LLM no forma parte de los fixes propuestos. |
| L-T4A.4 | No se cambia el matching difuso de promesas; solo se verifica que el nuevo servicio conserve identidad entre consumidores. |
| L-VUP-3 | La blocklist de plataformas no se refactoriza. La identidad exacta del hotel se valida sin introducir matching por substring. |
| L-PF5 | Aparecía como candidata en la revisión 2 (dos fallos aparentes, una causa raíz) y **se descarta como fila propia**: su enseñanza ya está operativa en el maestro §2 ("una ausencia inducida por retención no se presenta como otro fallo"). Registrarla de nuevo sería inflar el índice sin cambiar ninguna tarea. Lo que sí se capitaliza de ese mismo corpus es `L-PF11`, que no estaba |
| L-B4 | Dos planes pueden compartir nombre de carpeta de evidencia. **Descartada** porque este plan ya aísla su evidencia por nombre de plan (`evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/FASE-<ID>/`, contrato §límites). Se mantiene como riesgo conocido, no como tarea |

## 3bis. Lecciones definidas por este plan

Este archivo capitaliza lecciones **de planes anteriores**. Las definidas aquí —hoy `L-ENT.1` a `L-ENT.15`— viven en `10-analisis-post-implementacion.md`, subsección "Lecciones nuevas de este plan". `L-ENT.1` a `L-ENT.6` se registraron el 2026-09-19 durante la **preparación**; `L-ENT.7` a `L-ENT.9`, durante la **intervención del 2026-09-19**; `L-ENT.10` y `L-ENT.11`, en la **revisión 2** del mismo día, con las mediciones M1-M4 de arriba; `L-ENT.12` a `L-ENT.14`, al cerrar FASE-G y en su write-back; y `L-ENT.15`, al cerrar FASE-0 (medir el estado anterior con `git grep <rev>` / `git archive`, no con `git stash` sobre trabajo sin commitear). Ninguna está certificada post-implementación: VERIFY las contrasta contra lo realmente ejecutado y puede degradarlas o reescribirlas. **Nota de honestidad:** `L-ENT.1` y `L-ENT.2` quedan **rectificadas** en su enunciado por la medición M1; se conservan con su corrección a la vista en lugar de borrarlas, porque el patrón que describen (heredar un estado agregado) es exactamente lo que produjera el error.

## 4. Cobertura declarada

Este archivo es verificado por `scripts/validate_lesson_capitalization.py`: forma, consultas, al menos tres descartes, AC existente, IDs y dueños reales, y al menos dos fuentes. **No verifica la pertinencia**, ni que los prompts hayan aplicado las lecciones. El orquestador debe revisar esto por separado.

QMind fue accesible por CLI en ambas consultas de la preparación (Q3, Q4); no se publicó contenido nuevo. **En la revisión 2 (2026-09-19) el acceso quedó denegado por el clasificador de permisos (Q9), así que esa revisión se construyó solo con el índice local y sus fuentes en `Archives/`.** La afirmación de que QMind es accesible describe la sesión de preparación, no un estado permanente: A debe reintentar la consulta con autorización y registrar si el corpus remoto añade algo. Los snapshots de QMind no sustituyen el código vivo, y ninguna de las consultas fue autorización para subir datos del hotel.

**Reintento ejecutado en FASE-A (2026-09-19, Q11 abajo): RECUPERADO.** La denegación de Q9 no se repitió. El detalle está en `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/FASE-A/qmind-consulta-reintentada.md`. **Aplicación efectiva de las filas de §2 medidas por A:** ninguna de las catorce filas de §2 quedó desmentida por el código vivo; tres de sus afirmaciones se re-verificaron por símbolo el 2026-09-19 (L-V.3 → P4/P5/P6; L-E2E.1 → P11 y R6; L-NC6 → P12 y R4) y **una fila del corpus resultó caducada** — el falso positivo multi-sede que Q11 trajo (F12) ya está cerrado en `CrossValidator._reconcile_whatsapp_multisede` con su suite, que es `L-ENT.4` ("un estado heredado caduca") cumpliéndose otra vez, esta vez contra el corpus remoto. Consecuencia registrada: **una denegación de permiso es un estado de la sesión, no del sistema**; el reintento pedido por el plan era obligatorio y produjo material que la revisión 2 no tenía.

## 1ter. Consulta ejecutada por FASE-A

| # | Capa | Consulta literal | Resultado |
|---|---|---|---|
| Q11 | QMind, notebook iah-cli-lecciones (reintento de Q9) | `mcp__plugin_qoder-qmind_qoder-qmind__retrieve` nb `01a04d98-…`, dos ejes: presencia/WhatsApp (`maxResults 5`) y acta/cuarentena/veredicto (`maxResults 3`) | **Permitida y respondida.** 6 aportes (R1-R6). Tres ya cerrados en código vivo (`ASSET_GENERATED` en `_JUSTIFIED_STATUSES`, reconciliador multi-sede, exclusión del acta del paquete); dos filas que **sí estaban** en `.opencode/LECCIONES-INDEX.md` (L-E2E.3 y L-E2E.4, líneas 67-68) y no se capitalizaron → confirma el límite de §4 sin ampliar el corpus; una dependencia nueva para B/C. Ninguna subida |
| Q12 | QMind, notebook iah-cli-lecciones — **eje de CIERRE de G, no del síntoma** | `mcp__plugin_qoder-qmind_qoder-qmind__retrieve` nb `01a04d98-…`, consulta: «verificador AST descubrimiento de poblacion de callers sin lista fija kwargs obligatorios firma de funcion discover check en run_all_validations falsos verdes allowlist de archivos», `maxResults 6` | **Permitida y respondida** (6 aportes, score 0,38–0,51). Trajo `L-V2.1` (asertar al **mensaje** de la violación, no al rótulo del check), `L-V2.2` (un verificador no se apoya en el artefacto de otro gate: recalcular en memoria y fallar con nombre propio), `L-V2.3` (antes de cambiar un artefacto, ir a los tests que asertan **sobre ese artefacto**; si pinan una numeración, atarlos a coherencia interna y no al literal), `L-V3.1` (el hook versionado **no** invoca `run_all_validations.py`), `L-V3.2` (gate que compara contra «hoy») y `L-VUP-7` (never-block de `main.py`; declarado no aplicable a G). **G1-G3 se aplicaron y se midieron:** existía un contract test pineando el literal `[10/10]` del quick, así que añadir el check 11 habría producido un rojo por re-numeración y no por el guard — el mismo mecanismo que dejó rojo `test_registrado_como_check_5_en_el_hook` dos commits. Se reescribió a coherencia estructural. Ninguna subida. Detalle: `evidence/…/FASE-G/qmind-eje-cierre.md` |

**Límite nuevo, medido en esta revisión:** la cobertura de una consulta de lecciones depende del **eje** con que se formula. Las siete filas añadidas estaban en el mismo índice que Q1-Q2 ya habían recorrido, y no aparecieron porque se buscaron términos del síntoma ("WhatsApp", "promesa", "onboarding") y no de la superficie de cierre ("acta", "veredicto", "corrida", "cuarentena"). Recuperar una lección no es función solo del corpus: es función de la pregunta.

**Aplicación efectiva de §2 medida por FASE-G (2026-09-20).** Tres filas dejaron de ser prosa y se materializaron en mecanismo: `L-NC6` —el cable perdido se busca en el caller— vive ahora en `scripts/validate_wiring.py`, que descubre la población por AST sin lista fija; `L-R.4` —una regla sin verificador declara su límite— vive en la clave `limites` del reporte (7 límites) y en `cobertura.receptores_no_resueltos_en_produccion`, que se aserta en 0; `L-T4A.5` —un test verde puede no alcanzar la rama— obligó a probar el verificador con callers nuevos en archivos nuevos y no con el caller que B corregirá. **Y una fila del corpus se cumplió contra una premisa propia del plan:** `L-V.3` («toda premisa que dependa de que el writer ya serializa X cae contra el artefacto real») le ocurrió a la fila F-A' del maestro §1, que describe **una** invocación divergente donde el AST midió **tres**. Cuarta fila aplicada de forma no prevista: `L-V2.3`, que avisaba de los tests que asertan sobre la numeración del artefacto a cambiar — había uno, pineando `[10/10]`.

**Aplicación efectiva de §2 medida por FASE-0 (2026-09-20).** Cuatro filas dejaron de ser prosa y se verificaron contra el artefacto, no contra el fuente: `L-E2E.2` —la predicción caída— se cerró en el **productor** (`_critical_recall_details`), y el hecho que la produjo sigue visible en el `gate_report` archivado (`value: 1.0, details: {}`), que es la prueba de que el hueco existió. `L-V.3` se volvió en contra de una premisa propia de esta fase: el prompt daba el conteo del audit como el del gate, y medido con el `AssessmentBuilder` real el assessment lleva **4** issues, no 3 (`with_geo_flow` de FASE-G anexa el de banda GEO) — O1/O2 en `evidence/…/FASE-0/resultados-y-observaciones.md`. `L-T2A.2` se cumplió de forma exigente: el par verde/rojo se calculó sobre el `gate_report` y el acta reales de la corrida, y la suite nueva se ejecutó además contra `git archive HEAD` (16 rojos, 13 de ellos por aserción). `L-VUP-5` obligó a mutar el símbolo real: sin la anotación del camino fundado (M1) caen 5 pruebas, y el verde por vacuidad sigue bloqueando (M5). `DA-P1.4` se respetó al pie de la letra: la evidencia del paquete publicado se añadió **después** de `publish()` y sobre la ruta publicada, sin mover la decisión ni el contrato del packager. `L-R.4` quedó en los límites declarados del propio reporte: tope de 20 hallazgos por revisor con `findings_omitted`, P6.2/P6.5 sin activar y dependencia de que el ZIP exista al anotar. `L-V2.3` (traída por Q12 en G) se cumplió por segunda vez: un test pineaba la forma exacta de `reviewer_reports` y habría rojo por re-numeración; se re-ató a coherencia interna en vez de debilitar la aserción. Ninguna consulta nueva a QMind en esta fase, ninguna subida.

`build_lesson_index.py --check` cubre frescura del índice, no calidad del diseño. El par MD+JSON se regenera después de los cambios del plan. Las futuras fases documentarán la aplicación real de cada fila y el write-back autorizado antes del archivado.
