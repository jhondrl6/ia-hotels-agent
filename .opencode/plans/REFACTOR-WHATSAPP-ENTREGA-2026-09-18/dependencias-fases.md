# Dependencias de fases

Fuentes de contrato: [maestro](01-plan-maestro.md), [ejecución](04-contrato-ejecucion.md) y [lecciones](00-lecciones-capitalizadas.md). **Estado reconciliado el 2026-09-24 por el bloque C de `ORDEN-CAMBIO-CALIDAD-PROCESO-2026-09-22.md`: A, G, 0 y B están cerradas y empujadas (A `3e97d95`, G `66e17bd`, 0 `7c6e75f`, B `473ed0f` + `05d0cc6` + `7553f51`). Contador v4complete: 0/1. Punto de reanudación: FASE-C**, que además hereda la **deuda AC5 de B** (dueño C-D) como insumo propio. La frase que este archivo publicaba antes —«Siguiente sesión: FASE-B (su PRE parte de un árbol ya sucio con el trabajo de 0)»— estaba vencida y queda sustituida; el árbol sucio al que aludía se commiteó con `7c6e75f` y `473ed0f`.

**Revisión 2 (2026-09-19):** doce sesiones. Se añade **FASE-0** (AC20) y **G adelanta a segunda posición** porque su verificador es el guard de las ediciones de B–F. Cadena: `A → G → 0 → B → C → D → E → F → H → E2E → VERIFY → RELEASE`.

## DAG secuencial estricto

```text
[A] -> [G] -> [0] -> [B] -> [C] -> [D] -> [E] -> [F] -> [H]
                                                        |
                                                        v
                                                     [E2E]
                                                        |
                                                        v
                                                    [VERIFY]
                                                        |
                                                        v
                                                    [RELEASE]
```

Cada nodo corresponde a una sesión nueva. Todas las aristas son obligatorias: no hay tracks de implementación paralelos. La fase siguiente exige cierre real de la anterior; un bloqueo o checkpoint INCOMPLETA mantiene cerrada la arista. Una recuperación requiere sesión y alcance autorizados, no un salto ni un segundo E2E implícito.

## Estados y condiciones de transición

| Fase | Depende de | Condición de salida prevista | Consumo v4complete | Estado |
|---|---|---|---|---|
| A | Mandato de sesión y fuentes del plan | Baseline y contratos ratificados, **incluida FASE-0, la división AC19a/AC19b y el re-anclaje de la meta E2E**; permisos centrales, identidad y vigencia resueltos o checkpoint explícito | 0 | **CERRADA 2026-09-19 — condición de salida cumplida, cierre commiteado y empujado (`3e97d95`)**: matriz y los cuatro puntos de la revisión 2 ratificados sin reinterpretar; 20 premisas re-medidas (`decisiones.md` §1); cierre con commit y push (`3e97d95`); **vigencia/consentimiento pendiente del operador** (necesario para H/E2E, no para G) |
| G | A cerrada | Inventario AST, `wiring_report.json` en el quick y contrato muerto retirado; AC7/AC16. **Su función ahora es proteger las ediciones posteriores: debe cerrar antes de que B toque callers** | 0 | **CERRADA 2026-09-20 — condición de salida cumplida y verificada por el propio instrumento**: check 11/11 del quick en verde por cobertura (169 llamadas, 70 gobernadas, **0 receptores sin resolver en producción**), `wiring_report.json` publicado con límites y excepciones tipadas, `with_validation` sin el parámetro muerto. cierre **commiteado y empujado el 2026-09-20** (`66e17bd`, paridad 0/0) |
| **0** | **G cerrada 2026-09-20 (arista habilitada)** | `details` del gate fundado, `findings` en el DTO del acta y `package_evidence` en la rama publish; AC20 con el contrafactual medido sobre el acta archivada | 0 | **CERRADA 2026-09-20 y empujada el mismo día (`7c6e75f`, paridad 0/0) — condición de salida cumplida y verificada con el propio instrumento**: los tres cambios aditivos están en el árbol (`PublicationGatesOrchestrator._critical_recall_details`, `ReviewerReport.to_dict` con `findings`, `main._record_published_package_evidence` llamado en las DOS ramas de publish); AC20 **VERIFICADO OFFLINE** con el par `BLOQUEADO → APROBADO-CONDICIONAL-PENDING-ONBOARDING` medido sobre el acta archivada y 6/6 mutaciones rojas por el guard; regla de decisión, umbrales y contratos de cuarentena verificados idénticos a HEAD. **Commit y push ejecutados el 2026-09-20** |
| B | 0 cerrada | Pains y productores de promesas alineados (incluidos `nap_consistente`, generador de propuesta y `PAIN_TO_ASSET`); setup honesto; AC1/AC2/AC19a-consumo y PRE/POST | 0 | **CERRADA CON DEUDA REGISTRADA 2026-09-20 — código de producto ejecutado, medido y
commiteado (`473ed0f`); la arista a C queda HABILITADA y la deuda AC5 (S-B1) entra como
insumo suyo, no como prerrequisito de B.** AC1 **VERIFICADO OFFLINE**: señal derivada una vez en `generate_assets` y propagada a `detect_pains` + las dos `validate`; las 3 excepciones tipadas de G retiradas; guard 21 conformes / 0 omisiones; M1/M2 rojos por el guard. AC2 **VERIFICADO OFFLINE CON DEUDA AC5 ASSERTIONADA**: `whatsapp_setup_guide` es entregable real (sin número, sin `wa.me`, sin placeholder), la ausencia no promete botón, `can_generate` forzado del conflicto retirado, la guía de conflicto ya no elige número por reseñas, identidad propagada. AC19a-consumo: `NO_VERIFICADO_EN_SITIO` en `evidence_refs` y narrativa reescrita. **A1 autorizado y ejecutado:** `proposal_asset_alignment.py` separa `RESOLUCION_SERVICIO_A_ASSET` (todos los servicios) de `PROPOSAL_SERVICE_TO_ASSET` (universo contado) sin mover el denominador del gate; cerró los 7 rojos de matriz (83 passed). **Rojo residual con dueño:** `test_publication_gates.py::test_get_blocking_issues` — el gate toma el pase trivial cuando el único servicio comprometido es condicional (decisión A4 con O5: fixture re-anclado a `whatsapp_conflict` + test que caracteriza el pase
trivial y debe ponerse rojo cuando AC5 lo gobierne; superficie **AC5, dueño C-D** — el "D/E"
era un error de registro). **Commit `473ed0f` ejecutado con autorización literal; push
pendiente (A3).** Detalle: `evidence/…/FASE-B/` |
| C | B cerrada | Campo/destino seguro, bloqueo del botón inseguro, **dos lectores de WhatsApp unificados o designados** y AC19a aditivo con alcance declarado; AC3/AC5/AC6/AC19 | 0 | PENDIENTE |
| D | C cerrada | Veredicto, causas y fail-fast persistidos; AC4/AC5/AC8/AC9 | 0 | PENDIENTE |
| E | D cerrada | **Resolvedor único para los cuatro revisores** ("no leído" ≠ OK), ZIP real y snapshot interno revisable sin filtración; AC9–AC12 | 0 | PENDIENTE |
| F | E cerrada | **Sumidero único de redacción** y verificador que cubra `output/` y `logs/`; revocación acreditada o pendiente explícito que impide certificar AC13 | 0 | PENDIENTE |
| H | F cerrada | Integración offline; binding/frescura por sucursal del loader, campos no transportados declarados, runner con hijo falso, `attempts=0` y snapshot de `.agent/memory`; AC9, AC12–AC14, AC17 offline | 0 | PENDIENTE |
| E2E | H cerrada y preflight favorable | Un proceso, exit code y evidencia saneada preservados; resultado real registrado aunque bloquee; AC20 verificado en el flujo real | 1 como máximo | PENDIENTE |
| VERIFY | E2E cerrada con evidencia preservada | Lectura directa, matriz por AC **AC1–AC20**, diff estructural, límites y triage; sin remediar ni ejecutar | 0 | PENDIENTE |
| RELEASE | VERIFY cerrada y alcance de cierre explícito | Documentación y validaciones documentales autorizadas; no declarar éxito integral si la meta quedó parcial/FALLA | 0 | PENDIENTE |

AC15 atraviesa implementación/H. Cada fila incluye el cierre incremental exigido por el contrato; no basta cumplir solo el objetivo funcional. VERIFY puede documentar un resultado fallido: eso no lo convierte en certificación favorable ni habilita una reparación dentro de RELEASE.

## Conflictos de archivos y consumidores compartidos

| Superficie compartida | Fases que la consumen o modifican según su allowlist | Riesgo de interferencia | Orden obligatorio |
|---|---|---|---|
| `main.py` | G, 0, B, C, D, E, H | Cableado de pains, precedencia WhatsApp, causas/fail-fast, retención, firmas; en 0 también `package_evidence` de la rama publish y el writer del reporte | G antes de 0 antes de B antes de C antes de D antes de E; nunca ediciones concurrentes. En `run_v4_complete_mode`, los pares `_diag_var`/`_prop_var` + `locals()` resuelven rutas por lookup de `locals()`: frágil, E debe reemplazarlo por variables explícitas |
| Tribunal y gates de entrega: `publication_gates.py`, `tribunal/outcome.py`, `tribunal/diagnosis_reviewer.py`, `judge.py` | **0 es dueña del cambio**; D/E/H consumen su contrato | Un cambio de severidad o de `BLOCKING_VERDICTS` compraría el verde ocultando un bloqueo real | 0 cierra la serialización **sin tocar la regla de decisión**; D y E no vuelven a escribir sobre esas claves |
| Mapper y catálogo: `PainSolutionMapper`, `PAIN_SOLUTION_MAP`, `asset_catalog.py` | B es dueño del cambio; C/E/G consumen su contrato | Una promesa residual o identidad distinta rompe specs, narrativa y entrega | B congela identidades; consumidores posteriores prueban el mismo contrato |
| Productores asociados: `ConditionalGenerator.PAIN_TO_ASSET`, `get_assets_for_pain`, `_solutions_to_asset_specs` | B; C consume/gobierna seguridad del botón | Quitar una constante no elimina promesas en otras rutas | B alinea productores antes del guard de C |
| Coherencia: `CoherenceValidator`, reporte y callers | G, C, D | G verifica firmas/cableado **antes** de que C toque los callers; C elimina boost de presencia; D consume veredicto y causas | G antes de C antes de D; no reintroducir rutas paralelas |
| Lectores de presencia: `site_presence_checker.py`, `site_presence_adapter.py` y los 8 consumidores del reporte canónico | C (19a, aditivo); B consume el estado; **la migración tri-estado ya no está en ninguna fase: es AC19b diferido** | Cambiar la forma canónica rompe 4 asserts de igualdad exacta y exige redefinir `site_verified`/`PRODUCTION_PRESENT_STATUSES` en 8 sitios | C solo agrega claves; cualquier consumidor que necesite el tri-estado espera a AC19b con su propia sesión |
| Delivery y revisores: writer, manifiesto interno, rutas de artefactos | E; H integra y prueba | Snapshot exportado por accidente o lectura del archivo equivocado | E define el límite interno/cliente; H no rehace enforcement |
| Sanitización y captura de salidas | F; H integra runner | Persistencia previa a redacción o fuga en stdout/stderr | F antes de H; solo secretos sintéticos en pruebas |
| Tests de integración, onboarding derivado y runner del plan | H | Gastar el intento desde un test o inventar flags de producción | H trabaja en tests/runner y preparación aislada: sin nuevos flags CLI ni nueva edición de `main.py` |
| Índice, checklist, dependencias y cierre documental | Todas, por turno | Estados inconsistentes o cierres anticipados | Un único cierre de fase a la vez; respetar permisos de cada sesión |

La tabla no amplía allowlists. Si un cambio exige otra superficie, detener y resolver alcance. No hay edición concurrente de ningún archivo compartido. Los inventarios read-only permitidos no autorizan ejecución de fases en paralelo ni escrituras por otro actor sobre esos archivos.

Anclas de línea medidas el 2026-09-19 en HEAD 938f59f: `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/REVISION-2/anclajes_medidos.json`

## Bloqueantes y decisiones pendientes reales

| Tema | Hecho de partida | Dueño / fase | Condición para resolverlo sin falsear evidencia |
|---|---|---|---|
| **Nueva (revisión 2): supresión determinista del ZIP en la ruta viva** | **CERRADO POR FASE-0 el 2026-09-20 (offline, con su par medido).** Sobre el acta archivada de `output/TAREA7-2026-09-19/`, `TribunalJudge._compute_verdict` en copia temporal: con el hallazgo → `BLOQUEADO`; con la anotación que hoy produce el productor (`critical_issues_count: 3`, `recall_basis: all_critical_issues_detected`) y la recomendación recalculada por el propio revisor → `APROBADO-CONDICIONAL-PENDING-ONBOARDING`, que no está en `BLOCKING_VERDICTS` = publicable. Queda pendiente ejercitarlo en la corrida única (E2E) | Quality gates/tribunal; **FASE-0 cerrada; E2E lo confirma en flujo real** | Cerrado antes de consumir el intento, que era la condición. Verde barato descartado por medición, no por promesa: M5 (relajar `_check_vacuous_recall`) y M6 (sustituir la serie SR-H2) se aplicaron en copia y rompieron 3 pruebas cada uno; el detector, el Juez y los umbrales siguen idénticos a HEAD |
| Version Sync (quick) | Resuelto por re-medición: 9/10 el 2026-09-18 por cuatro documentos sucios en el árbol; 10/10 el 2026-09-19 con esos archivos ya idénticos a HEAD. `_check_version_sync` solo invoca `sync_versions.py --check` | Operador; A | Ninguna autorización pendiente. A re-mide el quick al inicio y no arrastra el rojo como prerrequisito |
| Privacidad F-B / D1 | Transporte WhatsApp por warehouse diferido; no es la fase B de este DAG | Producto/privacidad y onboarding; A ratifica la exclusión | No añadir PII ni modificar formulario/esquema sin decisión escrita. El setup puede resolver una promesa falsa, no el transporte de contacto |
| Frescura de observación | Fuente del 2026-07-22 (59 días al 2026-09-19) y `ONBOARDING_FRESHNESS_HOURS` sin definir, medido hoy: ningún verificador mecánico actúa | Operador; **A DECIDIÓ: hace falta reconfirmación** y no puede emitirla el agente | Consentimiento datado sobre la URL viva con límite escrito, como en FASE-P4. **Añadido por A:** el consentimiento P4 ampara *una corrida de observación/diagnóstico* y declara que no es entrega a cliente, mientras E2E persigue un ZIP publicable; y el reloj de 20 días de `MemoryManager.cleanup_old_sessions(days=20)` **sí** está activo (3 artefactos de memoria de Don Alfonso presentes), así que el preflight de H debe mirar ambos relojes |
| Binding de hotel | Medido 2026-09-19: la URL del warehouse no resuelve (NXDOMAIN) y la solicitada responde 200 con redirección observable; el fixture versionado ya usa la URL viva | A/H | Procedencia con ambas URLs, la evidencia de que una no resuelve y atribución de la corrección al operador. Sin edición del warehouse sin instrucción expresa y sin alias universal |
| Key expuesta | La prevención parcial en código no demuestra revocación operativa | Operador; F/H | Evidencia sin secreto de revocación o estado pendiente explícito. Key nueva y tests verdes no bastan; no ejecutar rotación remota sin permiso |
| Cierre documental central | **RESUELTO POR A el 2026-09-19 sin editar ningún documento central.** El executor canónico (§E7 y su tabla de estándares) y la regla contractual de DOMAIN_PRIMER en `docs/CONTRIBUTING.md` mandan **regenerar** con `doctor.py --regenerate-domain-primer` al cerrar cada fase de implementación y **verificar** (`--context`) solo en RELEASE; el bloque IMPORTANT de `AGENTS.md` (§Flujo Documental Obligatorio) condensa ambos en "se regenera en FASE-RELEASE", que coincide con la pata de verificación y contradice la de regeneración. A aplica la lectura del executor/CONTRIBUTING | Responsable documental; **A cerrado** | Aplicar desde G en adelante. `AGENTS.md` y el workflow **no** se modifican en este plan; si el operador quiere alinear el texto de AGENTS, es instrucción expresa aparte. Añadido por A: `.agent/knowledge/DOMAIN_PRIMER.md` está versionado, así que cada regeneración ensucia el árbol y exige autorización de commit. ⟦Confirmado y vuelto a leer el 2026-09-24 por el bloque C de la orden de calidad: `executor` §E7 y `CONTRIBUTING` Paso 5b/regla 4 mandan **regenerar al cerrar cada fase de implementación** y **verificar con `--context` solo en RELEASE`; G ya ejerció una regeneración con su writer. Este plan trata ambas como operaciones distintas y su instrucción ejecutable está en `04-contrato-ejecucion.md` paso 4 del cierre; `AGENTS.md` y la tabla de `CONTRIBUTING` quedan como desalineación declarada de configuración central, con su dueño⟧ |
| QMind/write-back | **Consulta recuperada por A (2026-09-19)** (`evidence/…/FASE-A/qmind-consulta-reintentada.md`) **y por G por el eje de cierre** (Q12). **Upload EJECUTADO Y VERIFICADO por G el 2026-09-20** con autorización literal separada: instantánea saneada del `10-analisis` (5 identidades del cliente sustituidas, fidelidad probada por sha inverso), fuente `01a0bfc9-5f5a-783e-9492-16367bbff596` verificada por **descarga byte a byte y sha256**, no por título → `evidence/…/FASE-G/qmind-writeback-G.md`. **RELEASE sigue necesitando autorización propia** para su ingesta final | Responsable documental/operador, y dueño del límite: `--upload` no sanea | La consulta no concede permiso de subida. Dos trampas medidas: (1) `--upload` lanza `qmind source upload --file` **crudo**, sin reescritor, así que hay que sanear antes a mano; (2) el check [15/15] de la validación **completa** sí invoca el validador (método `run()` de `run_all_validations.py`, medido al retractar esta fila), pero no corre en `--quick` ni en un hook, escanea **solo `Archives/`**, degrada a exit 0 si falta el CLI y decide **por título**: omitir el upload en raíz no produce rojo, y un título obsoleto lo oculta aunque ya estés archivado. **Orden no permutable (R2.10):** upload con el plan aún en raíz → `build_lesson_index.py` → `git mv`. **Título de RELEASE pre-acordado:** `10-analisis: REFACTOR-WHATSAPP-ENTREGA-2026-09-18 (cierre <versión final>, lecciones finales <fecha>)`, porque el de G ya existe y `--upload` respondería **SKIP**, dejando el contenido de la era G publicado como si fuera el cierre. SKIP por título no prueba actualización. **El writer no expone `--title` ni `--file`** (medido en su `main()` el 2026-09-20): publicar el cierre con título nuevo exige el CLI directo o ampliar `validate_qmind_writeback.py` con `--title` y un saneador. Dueño de esa cura: el mini-plan **`VERIFICADOR-ESCRITURA-QMIND-2026-09-20`** (creado 2026-09-20), cuyo disparador es la sesión previa a este RELEASE; si vence, aplica su §5 (CLI directo con título nuevo y verificación por descarga) |

Estas condiciones no se consideran satisfechas por existir un plan. La preparación no ejecuta validaciones, consultas de red, cambios de configuración ni operaciones sobre credenciales.

Anclas de línea medidas el 2026-09-19 en HEAD 938f59f: `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/REVISION-2/anclajes_medidos.json`

## Único intento y aislamiento de la evidencia

- H prepara el runner acotado al plan, con reserva persistente exclusiva, hashes y preflight `attempts=0`; lo prueba con hijo falso, no con v4complete.
- Solo E2E invoca ese runner. El único proceso hijo autorizado es v4complete para `https://www.donalfonsohotel.com/`, Hotel Don Alfonso, con el output aislado del plan y flags existentes.
- El intento se consume al crear el proceso, incluso si falla o termina por timeout. La vigilancia de un PID existente no es un relanzamiento; no ejecutar además el comando manual, loops ni reintentos.
- No invocar una auditoría preliminar externa en A–H, ni force, deploy o desactivación del Tribunal para forzar READY.
- Snapshot saneado, exit code, inventario y hashes se preservan antes del análisis. No copiar secretos, `.env` o logs históricos crudos ni conservar un ZIP suprimido contra O1.
- VERIFY trabaja directamente sobre esa evidencia: no delega, modifica código, ejecuta tests ni repite la corrida. RELEASE se limita al cierre documental autorizado.

## Control de avance y R2

PRE/POST de cada implementación usan la misma selección y entorno; mutantes prueban el guard real y no errores de import/sintaxis. Los lectores distinguen READ_OK, ABSENT y READ_ERROR; retención deliberada se registra aparte. Umbrales, blocking y `write/publish/suppress` no se debilitan.

R2 conserva la referencia de **60 tool_use hasta el corte que la sesión tenga autorizado** (con el commit de código autorizado, ese corte es «hasta el commit»; sin él, «hasta listo para revisión»), medida con el instrumento del contrato y corte temporal, y **declarando cuál de los dos se usó** para no comparar tramos distintos. Sin transcript o ante acceso denegado: **FUERA DE SERVICIO (R2.1)**, auto-reporte con unidad separado y sin comparaciones inventadas. Sin código se usa corte documental. ⟦Regla del proceso común que dejó el bloque B de la orden de calidad, alineada el 2026-09-24⟧: **los cinco cortes se sostienen sin commit** —implementación terminada, verificación terminada, cierre documental, listo para revisión, espera de autorización—, así que la ausencia de autorización **no** deja ningún corte «no consumado»; deja el tramo medido en «listo para revisión» y el commit como acción posterior, separada y opcional. Agotar presupuesto exige checkpoint y nueva sesión, nunca adelantar otra fase.

**Cierre de FASE-A (2026-09-19, HEAD `d4dacb4`, sin código de producto):** ratificaciones cerradas y 20 premisas re-medidas en `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/FASE-A/decisiones.md`. Quick **10/10** al abrir y al cerrar (el rojo 9/10 intermediario lo produjo **una recaída propia en citas por número de línea**, corregida a símbolos sin tocar el baseline del verificador). Registro en `REGISTRY.md` ejecutado con `--check-manual-docs` (sin GAP) y par del índice regenerado (316 IDs, `--check` fresco). **R2: métrica FUERA DE SERVICIO (R2.1)** — el instrumento exige el transcript y su acceso no estuvo disponible; se declara **corte documental** con auto-reporte (~62 intervenciones, unidad propia, no comparable). **Contador v4complete: 0/1.** El cierre quedó **commiteado (`3e97d95`) y empujado** a `origin/master` en la misma sesión; **G no se inició** (así lo exige R1) y su arista queda habilitada. Pendiente con dueño: **reconfirmación de vigencia/consentimiento sobre la URL viva (operador, antes de H/E2E)**.

**Cierre de FASE-G (2026-09-20, HEAD de partida `d7ff932`, con código de producto).** AC7 y AC16 verificados offline con su par de rojos; AC15 con PRE/POST de selección idéntica (delta 0) y POST extendido (+18 funciones canónicas, 4.246 → 4.264); 7/7 mutaciones con rojo causado por el guard. Quick **11/11** (se añadió el check 11 y se re-numeró el modo completo a /15). **R2: métrica FUERA DE SERVICIO (R2.1)** con auto-reporte en unidad contable. Hallazgos abiertos con dueño: tres invocaciones de `v4_asset_orchestrator.py` que omiten `whatsapp_html_detected` (FASE-B / AC1). Rojo preexistente con causa raíz medida y sin tocar: `test_medido_contra_el_predecesor_entra_en_alcance…`, por el archivado de `9c4a001`. Commit y push **ejecutados el 2026-09-20** con autorización literal del usuario (`66e17bd` → `origin/master`, paridad 0/0, hooks `agent-ecosystem` y `version-sync` en verde sin saltarlos). Sigue sin autorización: write-back a QMind. Tag no corresponde (no se cambió `VERSION.yaml`; el repo etiqueta releases y `v4.77.0` es el último).

**Cierre de FASE-0 (2026-09-20, HEAD de partida `ad0cc84`, con código de producto).** AC20 **VERIFICADO OFFLINE**: los tres cambios aditivos están y ninguno roza la regla de decisión. PRE/POST con la misma selección literal (12 archivos): 189 passed / 1 skipped → 189 passed / 1 skipped, **delta 0**; con el suite nuevo 210 passed / 1 skipped exit 0; la suite nueva sobre `git archive HEAD` rompe 16 (13 por aserción, 3 por `ImportError` del helper nuevo, declarados como tales). Funciones canónicas **4.264 → 4.285 (+21)**. Regresión completa POST: **3 failed, 4.247 passed, 41 skipped, 4 xfailed** en 232 s — los tres rojos son preexistentes y con dueño (dos ya registrados en `AGENTS.md`, uno heredado de G). Quick **11/11**. Contrafactual y crecimiento del acta medidos: JSON **+1.151 bytes (+84 %)**, MD **+0 bytes**, `revision_*.json` de la corrida 9.155 bytes. **6/6 mutaciones con rojo causado por el guard, todas restauradas y verificadas por sha256.** Dos rojos que FASE-0 encontró y resolvió sin debilitar ninguna aserción: un test pineaba la forma exacta de `reviewer_reports` (`L-V2.3`, mismo mecanismo que en G) y un fixture de revisor emparejaba conteo 3 con la base del camino de cero críticos. **R2: métrica FUERA DE SERVICIO (R2.1)** con auto-reporte en unidad propia. **Commit `7c6e75f` y push a `origin/master` ejecutados el 2026-09-20** con instrucción literal del operador (paridad 0/0 verificada por `git ls-remote`; los 7 checks del pre-commit pasaron sin saltarlos). Contador v4complete **0/1**.

Estado actual de validación y medición (reconciliado el 2026-09-24 por el bloque C de la orden de calidad, leyendo los cierres y sus commits, no esta cabecera): **A CERRADA (documental, commit `3e97d95` empujado); G CERRADA y empujada 2026-09-20 (commit `66e17bd`); 0 CERRADA, commiteada y empujada el 2026-09-20 (`7c6e75f`, paridad 0/0); B CERRADA CON DEUDA y empujada (`473ed0f` + `05d0cc6` + `7553f51`); C–RELEASE PENDIENTES, con C como punto de reanudación.** El contador v4complete sigue en **0/1**. Lo ya ejecutado y publicado en `origin/master` es la medición y el cierre documental de la preparación y de la revisión 2, y además el código de G; los cierres de A y de G quedaron **commiteados y empujados** (`3e97d95` y `66e17bd`, paridad 0/0 en ambos casos) dentro de su misma sesión. Sigue sin autorización e implícitamente no hecho: el release operativo, cualquier auditoría externa preliminar y toda salida de material del cliente que no sea la ya hecha. Única subida ejecutada: la copia **saneada** del `10-analisis` de G a QMind (2026-09-20, verificada por descarga + sha256, `evidence/…/FASE-G/qmind-writeback-G.md`); para publicar el cierre hará falta **título nuevo**, porque el de G ya existe y `--upload` respondería SKIP.
