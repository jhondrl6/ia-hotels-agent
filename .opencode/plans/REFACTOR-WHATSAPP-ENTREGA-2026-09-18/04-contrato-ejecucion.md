# Contrato de ejecución — REFACTOR-WHATSAPP-ENTREGA-2026-09-18

Cada prompt es ejecutable en una sesión nueva leyendo este contrato, el maestro y las filas pertinentes de `00-lecciones-capitalizadas.md`. Este archivo no reemplaza `.agents/workflows/phased_project_executor.md`; concreta su aplicación a este plan.

## Límites y precedencias

- Esta sesión solo prepara documentos. Cada fase posterior requiere su propia sesión y mandato de ejecución; R1 prohíbe encadenarlas.
- Fases intermedias: no cambiar VERSION.yaml, ROADMAP.md, workflow, hooks, permisos ni umbrales. No ejecutar v4complete excepto en E2E. No publicar, desplegar ni enviar material al hotel.
- La copia canónica del workflow está en el repositorio `iah-cli`, no en la ruta `iahcli` recibida inicialmente, que no existe.
- El template se adapta al executor vigente: TOTAL PASS dinámico, referencias a símbolos y delta PRE/POST. No copiar sus ejemplos obsoletos de conteos fijos ni citas de línea.
- Configuración central, rotación remota, commit, push, tag y subida a QMind requieren autorización expresa para esa acción. El plan describe pasos futuros; no los autoriza ahora. Un permiso negado no se evade.
- Si hay autorización explícita de commit: revisar diff y staged, stagear archivos concretos, commit nuevo sin saltar hooks. Sin ella, dejar checkpoint y pedirla. ⟦Alineado el 2026-09-24 con el proceso común del bloque B de la orden de calidad⟧: la ausencia de commit **no** deja ningún corte inconsumado. Los **cinco cortes son utilizables sin commit** y el `git commit` no es el quinto ni condición de ninguno — es una acción posterior, separada y opcional con autorización explícita. Lo que sí queda pendiente sin autorización es el **push** y el archivado.
- No tocar `evidence/FASE-P4/` ni históricos archivados. Evidencia nueva exclusivamente bajo `evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/FASE-<ID>/`.
- **Lectura de corridas ajenas (añadido en la revisión 2, 2026-09-19).** `output/TAREA7-2026-09-19/` es baseline medido de este plan: se permite **leerlo** y se permite invocar código del repo sobre sus artefactos **en memoria** (p. ej. `TribunalJudge._compute_verdict` para el contrafactual de AC20). Prohibido escribir, borrar o reordenar cualquier archivo bajo `output/` de esa corrida, y prohibido presentar una lectura de artefactos ajenos como propia de este plan. Una ejecución de lectura no consume el contador v4complete: solo el spawn del hijo de E2E lo hace.
- No leer ni imprimir valores de secretos. Los fixtures usan marcadores sintéticos. Nuevas salidas se sanean antes de disco/consola; no subir datos reales por defecto.

## Inicio de cada fase

1. Leer su prompt y estado anterior real; comprobar git status y símbolos, no heredar números de línea del contexto.
2. Verificar prerrequisitos y allowlist. Cambios ajenos se preservan; no se mezclan con el trabajo de fase.
3. Declarar hasta cuatro tareas, incluyendo validación y documentación incremental. E2E tiene tres tareas y un solo comando largo externo. No sumar otra auditoría completa o scraping independiente.
4. Tomar PRE antes de editar código o tests. Esperar terminación real del proceso; una notificación o un log parcial no constituyen resultado final.

## R2 — presupuesto e instrumento

Presupuesto de referencia por sesión: **60 tool_use hasta el corte que la sesión tenga autorizado**, sujeto a medición con `evidence/FASE-D/measure_iterations.py <transcript> <corte-ISO>`. La duración de pared se registra aparte. Los prompts no estiman cumplimiento.

**Qué corte se toma.** Con el commit de código autorizado, el corte canónico es **hasta el commit**. Sin esa autorización —que es el caso normal de este plan hasta que el operador la dé—, el corte utilizable es **hasta «listo para revisión»**, y la sesión **publica cuál de los dos usó** para que su número no se compare con el de una sesión que midió otro tramo. Los cinco cortes (implementación terminada, verificación terminada, cierre documental, listo para revisión, espera de autorización) se sostienen **sin commit**; el commit es posterior, opcional y con autorización propia (§Límites y precedencias).

Si el transcript no está disponible o su acceso es denegado, declarar desde ese momento **métrica FUERA DE SERVICIO (R2.1)**, registrar el auto-reporte con su unidad y no compararlo ni sumarlo al instrumento. No intentar evadir la denegación. Si se mide, recalibrar el presupuesto posterior con la distribución observada conforme a R2.1; un exceso produce checkpoint, no varias fases en una sesión. En fases sin código declarar el corte documental separado, nunca fingir un commit de código.

## Tests y evidencia

- Python local: `./venv/Scripts/python.exe`; comprobar existencia y entorno antes de usarlo. No reinstalar dependencias para hacer funcionar un subagente.
- PRE/POST de la misma selección pytest y entorno en `tests_baseline_pre.txt` / `tests_baseline_post.txt`; registrar exit code y sumas de passed, failed, skipped, xfailed (y xpassed si aparece). Delta de casos explicado por adiciones, bajas y parametrizaciones de ESTA fase; funciones canónicas se contabilizan aparte.
- No agregar exclusiones o cambiar expectativas para ocultar regresiones. Fallos previos requieren par medido, no la cifra histórica de AGENTS.md.
- **Proporcionalidad y reuso con invalidación explícita** (⟦proceso común del bloque B de la orden de calidad, incorporado el 2026-09-24 por su bloque C⟧). Las comprobaciones y las lecturas se dimensionan al cambio real de la fase: no se corre una auditoría completa donde una selección pertinente responde la misma pregunta, y **un resultado ya obtenido en la misma sesión puede reutilizarse**. Pero la reutilización solo vale si coinciden **entradas, configuración y entorno relevantes**; cualquier edición de la fase que afecte lo comprobado **invalida el resultado y obliga a re-medir**, y la fase declara qué reutilizó y por qué seguía vigente. Reutilizar no es heredar el verde de otra fase ni de otro plan. **Un rojo deliberado (mutación de un guard para probar que tiene dientes) no es una mutación accidental del código**: el primero se restaura y se verifica por sha, el segundo es un defecto.
- Cada AC detector/bloqueante exige verde y rojo al mutar el símbolo real; hacer mutaciones en proceso aislado/copia temporal, sin alterar el worktree vivo durante PRE. Registrar guard, test, motivo del fallo, exit codes y restauración. No mutar la aserción del test. **Su evidencia va al destino propio de la fase** (`evidence/REFACTOR-WHATSAPP-ENTREGA-2026-09-18/FASE-<ID>/…`): ningún arnés apunta por constante al directorio de otra fase, porque re-muestrear no puede re-escribir un registro cerrado.
- AC3 conserva su expectativa negativa: el test está verde cuando el producto bloquea el botón inseguro. Su mutante debilitado debe romper el test, no convertirse en la nueva expectativa.
- Lectores nuevos: vacío válido, artefacto ausente y lector fallido con tests separados y estados visibles. Al menos un test sobre baseline real para cada lector; si falta, skip explícito y AC sin certificar, no retorno silencioso.
- Leer JSON/MD desde el writer real y el ZIP cuando corresponda. No equiparar presencia de un string en código con evidencia de salida.

## delegate_task

Usar `delegate_task` únicamente donde el prompt lo permita: búsqueda independiente acotada, tracks sin archivos compartidos, comandos externos y documentación. Si el cliente ofrece `Agent` en lugar de `delegate_task`, usar `Agent` como equivalente disponible; no inventar parámetros de timeout ni herramientas inexistentes. Para procesos largos usar notificación de término (`run_in_background` en el cliente actual).

Brief obligatorio: objetivo, archivos permitidos/prohibidos, lectura o escritura, contrato ya decidido, salida esperada, criterios y prohibición de ejecutar fases futuras. El principal conserva decisiones de arquitectura y verifica diff/artefactos al integrar. En entorno WSL con venv Windows inaccesible al delegado, el principal ejecuta imports y tests. VERIFY no se delega.

## Cierre incremental obligatorio de cada fase

Esta sección forma parte de T4 (T3 en E2E). No se difiere a RELEASE.

1. Guardar evidencia saneada y actualizar el estado real de su prompt, `06-checklist-implementacion.md`, `dependencias-fases.md` y el índice del plan. Marcar INCOMPLETA si falta un requisito. **Cada fase escribe su propio estado y el de las fases cerradas se conserva como histórico: ninguna re-transcribe cifras de otra fase ni de este contrato** (un resultado, una fuente).
2. Actualizar `09-documentacion-post-proyecto.md` (A/B/D/E), `10-analisis-post-implementacion.md` (métricas, ACs, lecciones, seguimientos) y `00-lecciones-capitalizadas.md` (aplicación efectiva). ⟦Regla corregida el 2026-09-24 por el bloque C de la orden de calidad, alineada con el bloque B⟧: **no hay cuota de lecciones nuevas**. Una fase puede cerrar declarando **«sin lecciones nuevas»** y esa declaración es un resultado legítimo, no un hueco que rellenar; fabricar una lección para cumplir un número es exactamente el defecto que el proceso común vino a quitar. Lo que sí conserva su exigencia mínima es la **consulta al corpus** del Paso 0 (los descartes del §3 de la plantilla miden consultas hechas y rechazadas, no lecciones producidas). Prohibido también lo contrario: borrar una observación medida para achicar el cierre.
3. Añadir subsección de esa fase en CHANGELOG.md bajo versión vigente y nota en docs/GUIA_TECNICA.md; no anticipar nueva versión. Ejecutar el comando `log_phase_completion.py` incluido en el prompt, con archivos y tests realmente medidos. No usar el flag de release en fases intermedias. Confirmar REGISTRY y ausencia de GAP.
4. **Regenerar DOMAIN_PRIMER con su writer (`doctor.py --regenerate-domain-primer`) al cerrar esta fase de implementación**, y solo con su writer: no editarlo a mano. ⟦Divergencia resuelta el 2026-09-24 por el bloque C de la orden de calidad, leyendo la gobernanza; la instrucción anterior mandaba «resolver explícitamente esa divergencia **en A** antes de aplicarla», y A está cerrada desde el 2026-09-19⟧. La regla vigente es la del executor (§E7 y su tabla de estándares) y de `docs/CONTRIBUTING.md` (Paso 5b y su regla 4): **regenerar es operación de cada cierre de fase** y **verificar (`doctor.py --context`/`--status`) es operación de FASE-RELEASE**. Son dos operaciones distintas y ninguna sustituye a la otra: ni el `--context` del RELEASE exime a la fase de regenerar, ni la regeneración de la fase se publica como si hubiera verificado. El bloque de `AGENTS.md` que condensa ambas patas en «se regenera en FASE-RELEASE» y la fila de la tabla de `CONTRIBUTING` que titula «Se VERIFICA» a un paso que se llama «Regenerar» quedan **declarados como desalineación de configuración central**, con su dueño: alinearlos pide instrucción literal expresa y este plan no los edita. Como DOMAIN_PRIMER está versionado, cada regeneración ensucia el árbol: se regenera cuando la fase tiene mandato para escribirlo, y si no, se declara el checkpoint en vez de tocarlo. En RELEASE ejecutar también `doctor.py --context`/`--status`.
5. Para write-back durable **con su autorización literal propia** (es una operación remota: no la concede el mandato del cierre documental, y el hecho de que el paso esté en el orden no lo habilita): `./venv/Scripts/python.exe scripts/validate_qmind_writeback.py --upload REFACTOR-WHATSAPP-ENTREGA-2026-09-18`. Revisar lo que sube: no material del hotel ni secretos. **SKIP por título no prueba frescura**; si cambió el aporte, verificar snapshot y acordar título nuevo antes de subir. ⟦Alineado el 2026-09-24 con `VERIFICADOR-ESCRITURA-QMIND-2026-09-20`⟧: ese mini-plan separa **entrega offline** (writer, instantánea saneada, verificación por descarga + sha, prueba de sha inverso) de **aceptación remota** (la subida real), y esa separación es la que gobierna este paso. Si no hay autorización/acceso: checkpoint del cierre con el motivo y el estado `PENDIENTE-AUTORIZACION`, **no** simular éxito ni cerrar sin decirlo.
6. Regenerar `./venv/Scripts/python.exe scripts/build_lesson_index.py`, comprobar `--check` y ejecutar `./venv/Scripts/python.exe scripts/run_all_validations.py --quick` más `scripts/validate_document_integration.py`. Resolver los rojos dentro del alcance o mantener fase incompleta. No modificar baselines de validadores para absorber errores nuevos. **El número que imprime la corrida no se copia a ningún documento de este plan**: se referencia el comando (medición A6 de CONTEXTO —copiar una cifra de una fuente dinámica vence la aserción sin que nadie la edite—).
7. Medir R2 al corte efectivamente autorizado (ver §R2: **con commit no autorizado, el corte es «hasta listo para revisión»**), revisar git diff y finalizar la sesión. No iniciar la siguiente fase. Si la fase no acabó, guardar checkpoint con lo pendiente y sin volver a ejecutar lo ya completado.

## Corrida única y límites de certificación

Solo E2E puede iniciar `main.py v4complete`. El contador se consume al crear el proceso, incluso si falla. Un timeout del delegado no autoriza lanzar otro proceso; verificar PID y checkpoint. No usar reintentos de proceso, loops ni una segunda corrida con flags distintos. Las llamadas internas normales del pipeline no equivalen a una segunda invocación CLI.

VERIFY y RELEASE no ejecutan v4complete. Si la corrida falla por red, credenciales, datos insuficientes o un defecto nuevo, conservar el resultado, registrar causa y dueño, y pedir una modificación de alcance en otra sesión. Una sola muestra no certifica todos los hoteles ni todas las ramas; declarar por separado el régimen offline y lo realmente ejercitado.
