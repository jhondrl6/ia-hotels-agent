# Análisis Post-Implementación — TRIBUNAL-ENFORCEMENT-OBS-2026-09-11

> **Estado**: ✅ **COMPLETADO — 9/9 fases cerradas** (P1, P3-A, P3-B, **P2 — la que puso los
> dientes: el veredicto ya gatea el rename del ZIP**, **P4 — la corrida de observación con datos
> reales que los ejercitó: veredicto `BLOQUEADO` y ZIP suprimido**, **P5 — seguridad y privacidad
> con auditoría + remediación**, **P6 — generación y validación multi-hotel con los 5 AC-G
> cerrados y los 3 caminos causales verificados**, **FASE-VERIFY — certificación de los 25 ACs
> con matriz RE-V/CIT/CON, 6 cruces cross-fase, 6 greps residuales y triaje de Seguimientos**,
> y **FASE-RELEASE-4.77.0 — bump, CHANGELOG/GUIA_TECNICA, write-back QMind y archivado R2.5**).
> Tier A **no** observado, con el dueño del techo nombrado (analítica del hotel, no cableado).
> *(la cabecera quedó en "0 fases cerradas" tras P1 — descuido que se corrigió en P3-A y se
> mantiene al día desde entonces)*
> **Plan**: TRIBUNAL-ENFORCEMENT-OBS-2026-09-11 · **Versión objetivo**: 4.77.0 (confirmar en P1)
> **Creado**: 2026-09-14 en la sesión de ajuste — el executor lo exige **desde la concepción** del plan (v1.4.0+ del template); este plan lo debió desde el 2026-09-11 y la deuda queda aquí registrada, no retrodatada. Se actualiza al cierre de **cada** fase.

---

## Resumen de Ejecución (llenar al cierre de cada fase)

| Fase | Sesión | Estado | Iteraciones (ids + tool_use, corte = commit) | delegate_task | Notas |
|------|--------|--------|----------------------------------------------|---------------|-------|
| Ajuste de preparación *(orquestación, fuera de presupuesto de fase)* | 2026-09-14 | ✅ | auto-reporte (D-V2.1: el instrumento no alcanza el transcript bajo el cliente actual; unidad declarada) | No | Etapa 1 completada: prompts P3-A/P3-B/P4/RELEASE + 09/10 creados; P2 diferido a Q1 con declaración; P3 dividida por R3; FASE-VERIFY condicionada; cierre sin P4 fijado; citas `3bdc14e`/origin/tag `v4.76.0` corregidas |
| FASE-P1 | 2026-09-14 | ✅ | **≈30 `ids` / ≈58 `tool_use`** (auto-reporte, **unidad declarada** — D-V2.1: `measure_iterations.py` no alcanza el transcript bajo el cliente actual), corte = commit de cierre | No | Q1=sí · Q1b=escalar · Q2=O1-cuarentena · Q2b=2 capas · Q3=P3→P2 · Q4=el hotel · Q5=a · Q6=4 estados · Q7=knob heredado · VERIFY no activa · prompt P2 creado. **Presupuesto de 30 superado** → causa y cura en L-P1.4 |
| FASE-P3-A | 2026-09-14 | ✅ | **106 `ids` / 126 `tool_use`** — medido con `measure_iterations.py`, corte = commit de código `0d4d072`. **El instrumento SÍ alcanzó el transcript → D-V2.1 NO se reprodujo** (cifra medida, no auto-reportada) | No | AC-F1 (dos capas ZIP-aware + stub estructural, 4 estados NR8) + AC-F2 (tier desde `financial_scenarios`, MANIFEST fallback) + AC-F4 (`FIRST_FLOOR_TIERS` += `"B+"`). R2.7 4.109→4.130 (+21 íntegro en `passed`, 0 regresiones); 4 pares NR7 verde/rojo; `--quick` 9/9; sonda real: AC-F1 1 CRITICAL (468 B), AC-F2 acta `"B"`==pipeline `"B"`, AC-F4 `B+`→CONDITIONAL. **Presupuesto de 20 superado** → causa y cura en L-P3A.1 |
| FASE-P3-B | 2026-09-14 | ✅ | **120 `ids` / 120 `tool_use`** — medido con `measure_iterations.py`, corte = commit de código `bad0a5e`. **El instrumento volvió a alcanzar el transcript → D-V2.1 no se reprodujo (segunda fase).** **Presupuesto de 25 superado = tercera fase seguida** → causa y cura en L-P3B.1 | No | AC-F5 **ejecutado** (Q5=a): hoist de `ga4_available`/`gsc_available` sobre el bloque FASE-K de `main.py`; **medido**: GSC no tenía valor real que hoistear (nadie lo computaba en `v4complete`) así que hubo que calcularlo, y `gsc_configured` del MANIFEST se apuntó a la misma variable para no divergir del tier. AC-F3 cierra **D-V.1** (whitelist barreda test-only, §5.1 del contrato, **dos** mutaciones) y AC-F6 lee `VERSION.yaml` en cada escritura. R2.7 **4.130→4.153 = +23** (17 funciones; `passed` +24 = 23 nuevos + el barreda que migró de rojo a verde), **3 fallos restantes todos ajenos → 0 regresiones**. **6 pares NR7** verde/rojo; `--quick` 9/9 y hooks 7/7. Sonda de techo de tier sobre el baseline real: `B` con las cuatro combinaciones de banderas → el techo lo pone el dato, no el cableado (entrada para AC-O0). **Cero cambios en `judge.py`/`asset_reviewer.py`/`delivery_packager.py`** (`git show --stat`) |
| FASE-P2 | 2026-09-14 | ✅ | **≈65 `tool_use` / ≈60 `ids`** — **auto-reporte con unidad declarada**: D-V2.1 **sí se reprodujo** bajo este cliente (`measure_iterations.py` exige un transcript `.jsonl` y su directorio está fuera del workspace; el acceso fue **rechazado por el clasificador**). Corte = commit de código `df60c24`. **Presupuesto de 55 superado = cuarta fase seguida** → L-P2.4 | No | **O1-cuarentena ejecutada**: `DeliveryPackager` en `write()`/`publish()`/`suppress()` (`package()` queda como envoltorio para `execute` y los 69 tests previos de `tests/delivery/`), `_validate_zip` movido al `.tmp` (DA-P2.3), y en `main.py` el orden FASE-T1 → cuarentena → 4 Bots → **FASE-T1b** decide el rename. `tribunal/outcome.py` con los DTOs del §2.3; `_compute_verdict` recibe el 4º argumento y evalúa la matriz §2.1 **en su orden**. **AC-E0…AC-E5** con **8 pares NR7** (AC-E0 a/b, E1, E2, E3, E4, E5 a/b), todos con rojo real. R2.7 **4.153→4.181 = +28** (19 tribunal + 9 delivery, `passed` +28 exacto, **0 regresiones**, el mismo par de flaky en PRE y POST); el **POST se repitió** porque un cierre de seguimiento añadió un test después del primero. **Verificación obligatoria medida: el ZIP contenía el acta** (`ASSETS/v4_audit/acta_revision.{json,md}`, 2/40) y resultó ser un **círculo estricto** → DA-P2.1, el acta ya no viaja con el cliente. Retro estructural sobre `evidence/FASE-E2E/`: mismo artefacto, veredicto `APROBADO-CONDICIONAL…` → **`BLOQUEADO`**, `reviewer_reports` 0→4, **las 6 cláusulas idénticas**. Cierra además el seguimiento del ``Artefacto fuente: MANIFEST.json`` que P1 asignaba a P2 |
| FASE-P4 | 2026-09-14 | ✅ | **≈52 `tool_use` auto-reporte con unidad declarada** — D-V2.1 **se reprodujo de nuevo** (segunda sesión seguida, como P2): `measure_iterations.py` exige el transcript `.jsonl` y no existe ningún `.jsonl` dentro del workspace (`find . -name "*.jsonl"` → 0); el directorio del cliente quedó fuera del alcance y su acceso fue denegado por el clasificador. **Presupuesto de 40 NO verificable con esta unidad** → no se declara cumplimiento ni incumplimiento (L-P4.4) | **Sí** (corrida larga) | **Corrida real con Don Alfonso** (`--url https://hoteldonalfonso.com/`, `--output` alternativo para no pisar el baseline). **El tribunal bloqueó por primera vez en el pipeline real**: `[OK] Paquete en cuarentena: hoteldonalfonso_20260914.zip.tmp` → `Verdict: BLOQUEADO` → `⛔ ZIP SUPPRIMIDO`, y `deliveries/` queda sin ningún `*.zip`. `reviewer_reports` de longitud **4** en el artefacto real (todos `OK_WITH_FINDINGS`, 1 CRITICAL cada uno, los cuatro `BLOQUEAR`), `corrective_actions` con `owner`, `enforcement.{blocking_env,enabled,suppressed_by_operator}` y **AC-F2/AC-F4 vistos en vivo**: `evidence_tier "B+"` con `first_floor_rule.source_artifact` apuntando a `financial_scenarios_*.json`. **T3a: el disparador de diferimiento NO se cumple** (6 hoteles resuelven por el fallback `observations.json` del cargador) → P4 se corrió, no se difirió. **Techo `B_PLUS` medido antes y después** con los predicados de AC-F5 (`ga4_available=False`, `gsc_available=False`) → AC-O0: el dueño es la analítica del hotel. **No se observó el contrafactual de Q1**: la primera pasada ya era `BLOQUEADO` por gates, así que los dientes no decidieron (§punto 5) → F-P4.1/L-P4.3. **9 hallazgos nuevos con dueño, cero código de producción tocado** (L-V.4) |
| FASE-P5 | 2026-09-15 | ✅ | presupuesto FUERA DE SERVICIO (R2.1/D-V2.1); medida real auto-reportada con unidad declarada = commits de ejecución: 1 (`b25b63a`) + 1 remediación; D-V2.1 reproducido | No | AC-S1 ✅ (sanitización providers LLM, 11 tests, NR7), AC-S2 ✅ remendada (tracked+staged, NO_CUBIERTO bloqueante, NameError L-T2C.2), AC-S3/AC-S4 ✅ (inventario + puerta). NR1 4.155→4.169 (+10 tras remediación). `--quick` 10/10. Cero cambios en `main.py`/`judge.py`/`acta_writer.py` |
| FASE-P6 | 2026-09-15 | ✅ | presupuesto FUERA DE SERVICIO (R2.1/D-V2.1); medida real auto-reportada con unidad declarada; D-V2.1 reproducido | No | AC-G1 ✅ (asset_zip_paths + unknown assets), AC-G2 ✅ (onboarding fallback independiente), AC-G3 ✅ (SHA256/member_count del .zip.tmp), AC-G4 ✅ (matriz offline ≥3 perfiles), AC-G5 ✅ (3 caminos causales con NR7). **NR1 4.169→4.189 (+20)**, 0 regresiones, 5 pares NR7. Archivos producción: `main.py`, `delivery_packager.py`, `asset_responsibility_contract.py`, `acta_writer.py`. Cero cambios en `judge.py` |
| FASE-P6-R | 2026-09-15 | ✅ (remediación post-auditoría) | unidad declarada = **commits de ejecución: 1 (`c31422a`) + 1 documental de cierre; corte = HEAD al cerrar**; D-V2.1 reproducido (transcript fuera del workspace) | No | Auditoría forense (`evidence/FASE-P6/AUDITORIA-forense-2026-09-15.md`) halló: T4 ejecutada con actas a mano (prohibidas por el plan), NR7 sobredichado (0 pares para G1/G2/G3), cláusula 2 de AC-G2 sin tocar (converter fabricaba `verified`/rooms/pct), 6 falsedades documentales, fase sin commitear. Remediación: `tests/test_p6r_full_flow_matrix.py` (packager.write → ZIP real → 4 Bots → Juez → publish/suppress; 5 perfiles, incluido **B+ Don Alfonso anonimizado** y kill switch con las dos llaves separadas), **5 pares NR7 por reversión del fix** con restauración verificada por hash (`nr7_p6r_mutation_checks.py`), converter honesto + test migrado con causa, re-escritura del acta fuera del try publicador, mapeo ZIP de fuente única en el packager, `ImplementationOrderGenerator` dispuesto (legacy sin consumidores). **NR1 POST-P6-R re-medido**: 4.189→**4.196 `passed`** (+7), mismos 2 fallos ajenos, 0 regresiones |
| FASE-VERIFY | 2026-09-15 | ✅ **Cerrada 2026-09-15** | auto-reporte con unidad declarada (D-V2.1 reproducido: transcript fuera del workspace); corte = HEAD al cerrar | No | **DIRECTO, no delegable.** Matriz de certificación de los **25 ACs** con nivel RE-V/CIT/CON (`evidence/FASE-VERIFY/MATRIZ-CERTIFICACION.md`: 18 RE-V / 4 CIT / 3 CON), 3 filas CIT re-medidas (11 tests pasaron), 6 cruces cross-fase (`evidence/FASE-VERIFY/T2-crosses.md`: 5 coherentes + 1 incoherente — `google_places_client._save_cache` sin redacción), 6 greps residuales (`evidence/FASE-VERIFY/T3-greps.md`), triaje de Seguimientos (`evidence/FASE-VERIFY/T4-triaje.md`: 0 bloqueantes, 5 límite declarado, 9 plan sucesor). **2 hallazgos CON**: CON-1 (docs citan `GATE_ENFORCEMENT_ENABLED` inexistente — solo `GATE_BLOCKING_ENABLED`), CON-2 (cláusulas P6.2/P6.5 dicen NOT_EVALUABLE pese a revisores corrieron). **4 lecciones**: L-VERIFY.1 (docs claim dos switches, código tiene uno), L-VERIFY.2 (campos DTO vs JSON serializado), L-VERIFY.3 (cross-phase revela gaps que fases individuales no ven), L-VERIFY.4 (cláusulas no actualizadas post-enforcement). No tocó código |
| FASE-RELEASE-4.77.0 | 2026-09-15 | ✅ | **≈25 `tool_use` — auto-reporte con unidad declarada** (D-V2.1 reproducida: el transcript sigue fuera del workspace); corte = commit único de cierre con tag `v4.77.0` | Sí (docs) | Cierre documental: **AC-V1 = verificar y citar la matriz de VERIFY** (25 filas con Real/Status/nivel, ningún `—`, ver §Matriz de certificación); bump 4.77.0 + `sync_versions` (6 archivos) + CHANGELOG + GUIA_TECNICA (3 notas por grupo de fase) + fila Tests de AGENTS.md (4.233 canónicas, D-V.1 cerrada, denominadores 10/14); CON-1/C8 corregido anotado en `01` y `10`; write-back QMind **antes** del archivado; plan archivado (R2.5) + diff de `--fix` revisado a mano; tag anotado `v4.77.0` en el mismo cierre |

**Regla L-R.1 (vigente para este archivo)**: una fila en `⬜` con celda de iteraciones en `—` **no bloquea**; lo que bloquea el ✅ de fase cerrada es `—` **después** de cerrada. Una fase diferida (P2 por Q1=no, P4 por cierre sin P4) se marca `Diferida` con la decisión como causa, nunca `—`.

---

## Decisiones Arquitectónicas / de Proceso

### D-AJUST.1 — División de FASE-P3 en P3-A / P3-B (2026-09-14)
- **Decisión**: P3 empaquetaba 6 fixes (AC8, tier del acta, AC-F4, AC-F5, barreda, versión) y excedía el máximo de R3 (≤4 tareas). Se divide: **P3-A** = AC-F1 (AC8) + AC-F2 (tier acta) + AC-F4 (primer piso `B_PLUS`) sobre `judge.py`/`asset_reviewer.py`; **P3-B** = AC-F3 (barreda, test-only) + versión del acta + AC-F5 solo condicional a Q5=(a), sobre tests/`acta_writer.py`/`main.py`.
- **Rationale**: la división es por naturaleza de cambio (detección/fidelidad del acta vs cableado/test), no por tamaño: P3-A no toca `main.py` ni `acta_writer.py`; P3-B solo toca `main.py` bajo Q5=a. Cada fase nace dentro de R3 y con baseline NR1 propio.
- **Alternativas rechazadas**: (i) agrupar "tier acta + versión acta" en una sola tarea para caber en 4 — rechaza medir R3 con agrupaciones convenientes, que es como la regla muere; (ii) dejar P3 inteira "porque son fixes chicos" — R3 existe precisamente contra el agotamiento por acumulación (la regla lo dice: el orquestador que crea fases grandes es responsable del agotamiento de las sesiones siguientes).

### D-AJUST.2 — FASE-VERIFY condicionada, no decidida en la preparación (2026-09-14)
- **Decisión**: los 3 criterios de §4.6 no son evaluables hoy porque el conteo de fases de implementación depende de Q1 (P2), Q5 (AC-F5 en P3-B) y Q4/T3a (P4). Se registra la evaluación condicional en `dependencias-fases.md` y **FASE-P1 la cierra**.
- **Rationale**: activar o excluir VERIFY con una decisión no tomada sería el gesto que L-V.4 prohíbe (decidir en la fase equivocada); el plan ya tenía el patrón embebido en RELEASE (AC-V1) sin declararlo sustituto.
- **Alternativas rechazadas**: (i) fijar "no activa" (conteo P2+P3=2) — omitía que P4 es fase con ejecución E2E y que el conteo cambia con Q1; (ii) crear ya el prompt VERIFY — prompt condicional a una decisión pendiente es un entregable falso.

### D-AJUST.3 — Escenario de cierre sin P4 (2026-09-14)
- **Decisión**: si T3a no se cierra (con Q5≠c), P4 se difiere con la misma mecánica que Q5=(c) — §Cierre válido sin P4 en `dependencias-fases.md`. El fallo de T3b no difiere: AC-O0 (`B_PLUS` con límite). Si P1 eligió Q1=(c) y T3a falla después, la resolución por defecto es O4 documentada.
- **Rationale**: sin esto, RELEASE quedaba bloqueada por una precondición externa que quizá nunca llega; con esto, el diferimiento es una decisión con rastro, no un `—`.

### FASE-P1 (2026-09-14) — decisiones DA-P1.1 … DA-P1.10

Desarrollo completo con alternativas rechazadas: `evidence/FASE-P1/decision-enforcement.md` §1.

| ID | Decisión | Rationale (una línea) | Alternativa rechazada |
|----|----------|-----------------------|------------------------|
| DA-P1.1 | **Enforcement sí** | G0 (`ROADMAP.md` §9) exige acta `APROBADO-PARA-ENTREGA`, y esa rama está cerrada por un guard que hoy no puede recibir los hallazgos: auditoría-only = **G0 incerrable por diseño** | (b) O4 documentado · (c) decidir tras P4 (inobservable con el cableado actual) |
| DA-P1.2 | **Escalar, no ciclar** | Los defectos que bloquean son estructurales (stub, asset ausente): regenerar produce el mismo stub. El `suggestion` de L-PF3 no tiene forma de restricción aquí | reintento con guard anti-bucle · entrega parcial (contradice el acta) |
| DA-P1.3 | **Orden P3-A → P3-B → P2 → P4** | Cablear dientes sobre detecciones rotas produce bloqueos inevaluables y invita a apagar el tribunal | P2 primero |
| DA-P1.4 | **O1-cuarentena** | El packager ya escribe `.zip.tmp` y renombra de forma atómica con los bytes finalizados: la decisión se mueve del *write* al **publish** sin duplicar serialización ni cambiar el contrato ZIP-only | O1-staging (layout fantasma) · O2 (memoria) · O3 (abandona single-write) · O4 |
| DA-P1.5 | **AC8 en dos capas** | Arreglar solo la lectura deja pasar el stub; arreglar solo el heurístico no sirve porque el archivo nunca se lee. **AC8 y AC-F2 son el mismo bug con dos víctimas** | (a) sola · (b) sola · dejarlo caer con el reordenamiento |
| DA-P1.6 | **Cuatro estados, no tres** | El tri-estado omitía `NOT_RUN`, que es el estado que la corrida real ya exhibe; diseñar los estados alrededor del bug dejaría P2 sin palabra para nombrar su propio fallo | tri-estado del plan · colapsar `NOT_RUN` con `READER_FAILED` |
| DA-P1.7 | **Heredar `GATE_BLOCKING_ENABLED`** + `enforcement.suppressed_by_operator` en el acta | Medido: el tribunal **no** heredaba el knob. Un solo botón para todo lo que suprime entrega, y el escape queda declarado en el artefacto en vez de ser silencio | knob sin escape · knob propio `TRIBUNAL_BLOCKING_ENABLED` (dos switches confundibles) |
| DA-P1.8 | **Q5=(a) como par con AC-F2** | Propagar banderas sin arreglar la fuente del tier es invisible: `_read_evidence_tier` seguiría leyendo `"C"` | (b) `B_PLUS` como decisión de producto (deja `APROBADO-PARA-ENTREGA` como código muerto) · (c) diferir |
| DA-P1.9 | **T3a la da el hotel, por contacto del operador** | Es la única decisión del plan que no es de diseño sino comercial; sus líneas rojas (el agente jamás llena el YAML, nada de datos plausibles para destrabar, consentimiento y frescura) son la garantía de que el tier no vuelve a mentir | que el agente o el plan "estimen" el dato · correr con defaults |
| DA-P1.10 | **FASE-VERIFY no activa** 🔁 **[reabierta 2026-09-15 por D-AJUST.4 — ver su sección arriba]** | El criterio §4.6-2 depende de que un hotel responda: atar la certificación a eso deja el cierre en `—` indefinido. La certificación se ancla al par NR7 por fase, que sí depende de este plan | crear el prompt VERIFY con una condición abierta · dejarla "condicionada" otra sesión |

**Tensión registrada, no resuelta en P1** (para que P2 no la descubra tarde): la decisión Q2b asume que los revisores leen el ZIP, y la decisión Q2 los pone a leer el `.zip.tmp`. Son el mismo archivo en regímenes distintos del mismo nombre; el contrato lo cierra fijando que **el resolutor de entrega lee un ZIP sea `.tmp` o publicado**, y P2 debe verificar además **si el acta viaja dentro del ZIP** (Tarea 1 de su prompt).

---

### FASE-P4 (2026-09-14) — decisión D-P4.1

- **D-P4.1 — los hallazgos de la corrida NO se heredan a RELEASE: hubo sesión propia antes.** Decide el
  operador al cerrar P4, ante la pregunta explícita de si F-P4.1 y F-P4.5/F-P4.9 se delegaban a
  FASE-RELEASE-4.77.0 o se abrían fase propia. **Elección: sesión propia antes de RELEASE.**
  - **Rationale**: mientras `IMPLEMENTATION_ORDER.md` siga siendo un stub (F-P4.1), **toda** corrida
    real termina en `BLOQUEADO` y el contrafactual que justificó Q1 —gates aprueban y un revisor
    objeta— es inobservable por construcción (L-P4.3). RELEASE con AC-V1 certificaría entonces una rama
    que nadie ejercitó, y el acta de esa certificación no podría distinguirse de un artefacto que solo
    repite el bloqueo de la plantilla.
  - **Alternativa rechazada**: (i) llevarlos como seguimientos dentro de RELEASE — es el destino
    automático que el plan ya tenía escrito, y convierte el bump 4.77.0 en la release que declara
    dientes sin haberlos visto morder una vez fuera del régimen «todo bloqueado»; (ii) cerrar P4 con
    una segunda corrida «arreglando» el stub sobre la marcha — prohíbenlo la restricción de L-V.4 y el
    alcance de la fase.
  - **Consecuencia para la secuencia**: FASE-RELEASE-4.77.0 deja de ser la siguiente sesión. La fase
    nueva se concibe con sus propios ACs y su par NR7 (es fase de detección/bloqueo, no de papeles), y
    le toca también decidir la política del F-P4.5 (barrido de secretos a `*.md`/`*.log`/`*.json`) y la
    de F-P4.9 (que el acta conserve el sha256 del paquete desechado).

### Actualización del plan (2026-09-15) — decisión D-PRE.1

- **D-PRE.1 — «sesión propia antes» se concreta en DOS fases: P5 (seguridad/privacidad) y P6 (generación multi-hotel).** Decide el operador al revisar los 9 hallazgos de P4, ante la pregunta de si P5/P6 se fusionaban en una sola fase o se dejaban como seguimientos de RELEASE. **Elección: dos fases secuenciales antes de RELEASE** — `P4 → P5 → P6 → RELEASE` (progreso 5/8). Refina, no reabre, D-P4.1: la «fase nueva» de aquella decisión resultan ser dos, porque los hallazgos tienen dueños y naturalezas distintas.
  - **Rationale**: F-P4.5 (clave expuesta en el log del provider + control de secretos que solo camina `*.py` + 41 blobs de un cliente real en `origin/master` de un repo **público**, medidos con `git ls-tree` sobre el commit `c12ef73`) es de **seguridad/privacidad** y tiene precondición externa (rotación/contención del operador) que no puede mezclarse con la corrección del **productor** de generación (F-P4.1/F-P4.3/F-P4.7/F-P4.9, dueños de código). Fusionarlas obligaría a bloquear el fix de generación detrás de una autorización de cuentas externas; separarlas deja que **P6 arranque con el cierre técnico de P5** sin esperar el acceso, y que la puerta de publicación (AC-S4) siga siendo responsabilidad del operador.
  - **Contratos**: P5 = AC-S1 (sanear errores del provider), AC-S2 (control de secretos sobre contenido staged, multi-extensión), AC-S3 (inventario público medido), AC-S4 (registro de rotación/contención con **puerta para publicación**). P6 = AC-G1 (orden desde la ruta real del ZIP), AC-G2 (onboarding sin dependencia de YAML ajeno, WhatsApp solo con evidencia), AC-G3 (`package_evidence.sha256`/`member_count` del `.zip.tmp` antes de `suppress()`), AC-G4 (matriz offline ≥3 perfiles), AC-G5 (tres caminos causales con NR7). **AC-V1 de RELEASE se amplía** a AC-S1…AC-S4 y AC-G1…AC-G5, sin nueva sesión VERIFY **[🔁 revocado por D-AJUST.4, 2026-09-15: la sesión VERIFY sí se crea y AC-V1 queda reducido a citarla]**.
  - **Rectificación causal medida**: la premisa de D-P4.1 («toda corrida real termina en BLOQUEADO») se precisa — P4 ya estaba **bloqueada por gates** (coherence + `whatsapp_verified` sin `whatsapp_number`, por el hueco del adaptador `observations.json→onboarding`) **antes** de los revisores, así que el stub no hace lógicamente imposible el contrafactual gates-aprueban/revisor-objeta; lo que P4 no observó es que coherence bloqueaba primero. La muestra fue **una corrida Don Alfonso + tres ZIP Salento Real (dos hoteles)**, no una tasa universal del 100 %. AC-G5 debe separar las causas sin reescribir la observación.
  - **Alternativas rechazadas**: (i) **una sola fase P5** con las dos naturalezas — acopla el fix de código a una autorización externa y estanca P6; (ii) **dejarlo como seguimiento de RELEASE** — era el destino automático que D-P4.1 ya rechazó; (iii) **reabrir Q1/Q1b/Q2/Q5/Q6/Q7** — no procede: la ampliación añade fases, no re-decide el contrato de enforcement.
  - **Consecuencia para la secuencia y la medición**: los presupuestos de iteraciones de P5 y P6 quedan **FUERA DE SERVICIO (R2.1, D-V2.1)** por falta de calibración comparable — se registra medida real, unidad y corte al cerrar, sin inventar estimación. El cierre técnico de P5 habilita P6 pero **no habilita RELEASE público** mientras rotación/contención sigan pendientes sin resolución verificable o aceptación explícita del riesgo por el operador (puerta AC-S4). Esta actualización es **puramente documental**: no ejecuta fixes, pruebas, rotación, retirada de datos, cambio de visibilidad, bump ni push.

### D-AJUST.4 — Reapertura de FASE-VERIFY (2026-09-15, sesión de decisión fuera de fase): **SÍ activa**

- **Decisión**: se crea sesión `FASE-VERIFY` entre P6-R y RELEASE, con prompt `05-prompt-inicio-sesion-fase-VERIFY.md`, **DIRECTO y no delegable**, presupuesto 60, 4 tareas / 0 comandos largos. **AC-V1 se reduce** a verificar y citar su matriz; nace **AC-V0** (la matriz misma).
- **Qué se revierte y qué no**: se revierte la **exclusión de la sesión** decidida por P1; **no** se revierte su criterio — el par NR7 por AC de detección/bloqueo sigue siendo obligatorio y un AC sin él queda ⚠️, nunca ✅ (R2.4/NR7 vigentes).
- **Medición que la motiva** (las tres, posteriores al cierre de P1):
  1. **Los tres criterios de §4.6 se cumplen hoy**, cuando P1 cerró la fase sobre uno que no: el criterio 2 se evaluó como "no garantizable desde la ingeniería" porque P4 colgaba de T3a — **P4 se corrió** con Hotel Don Alfonso y P6-R añadió una matriz contra el flujo real; el criterio 1 contaba 3 fases de implementación y el plan tiene **5** (P3-A, P3-B, P2, P5, P6); el criterio 3 pasó de 3 a 6 familias (25 ACs: AC-D1, E0–E5, F1–F6, G1–G5, O0–O2, S1–S4). §4.6 es imperativo cuando TODOS se cumplen.
  2. **Dos auto-certificaciones de fase resultaron falsas** y las desmontó auditoría externa el mismo día: P5 (4/8 puntos post-ejecución, afirmación falsa en `NR7-AC-S2-green.txt`, escaneo staged **muerto** por `NameError` tragado por su propio `except` — el par NR7 de AC-S2 mutaba valores, no la ruta staged) y P6 (tarea 4 con actas a mano pese a la prohibición expresa del prompt, `DeliveryPackager` con **0 coincidencias** en los 4 archivos nuevos de test, NR7 sobredichado "uno por AC-G" con cero pares para G1/G2/G3, cláusula 2 de AC-G2 sin tocar). La sustitución de P1 descansaba en que el par por fase bastaba; **medido: el par por fase se declaró y no existía**.
  3. **AC-V1 no cabía en su contenedor**: RELEASE es DELEGABLE / BAJA / **30 iteraciones** para once obligaciones documentales, y §4.6 prohíbe delegar esta tarea. El precedente (VALIDADOR-URL-PROPIA, archivado) certificó **8 ACs con 41–50 iteraciones en DIRECTO** (tope 60); a esa tasa, 25 ACs ≈ 140.
- **Alternativas rechazadas**: **A** mantener AC-V1 tal cual — coste 0, pero certifica el claim central del plan ("el tribunal tiene dientes") con 4–5x menos presupuesto y en modo prohibido, tras dos ✅ falsos · **A'** RELEASE en DIRECTO con 60–70 iteraciones — corrige modo y presupuesto, sigue mezclando certificación con bump/tag/archivado en la misma sesión · **B** VERIFY completa re-verificando los 25 ACs desde cero — fiel al precedente pero ~140 iteraciones y **duplica** lo que P5-R y P6-R ya midieron con instrumento.
- **Por qué C y no B**: P6-R curó I1–I5 con evidencia medida (matriz `packager.write()` → ZIP real → 4 Bots → Juez → `publish`/`suppress`, 5 pares por **reversión del fix** con restauración verificada por hash, converter sin defaults, fuente única de la ruta ZIP) y la remediación de P5 dejó su camino vigilado por test. Re-medir eso sería inflar el presupuesto sin añadir cobertura. De ahí la profundidad escalonada **RE-V / CIT / CON** con muestreo obligatorio de ≥3 filas: se re-verifica lo declarativo o tocado por remediación, se cita lo ya medido comprobando que el archivo existe y dice eso, y se contradice el registro donde no se sostenga.
- **Lo que ninguna auditoría de fase cubrió y esta sí** (diferencia con la verificación local, §4.6): la **integración** de la ruta de delivery — P2 (`write`/`publish`/`suppress`) × P3-A (detección ZIP-aware, fidelidad del tier) × P3-B (banderas reales → techo de tier) × P6 (huella del `.zip.tmp`, matriz multi-hotel) × P5 (redacción en artefacto) × P1 (las **dos** llaves del bloqueo: `GATE_BLOCKING_ENABLED` heredado + `GATE_ENFORCEMENT_ENABLED`, ambas declaradas en el acta) **[ANOTACIÓN RELEASE 2026-09-15 — CON-1/C8 cerrado aquí: solo `GATE_BLOCKING_ENABLED` existe en código; la segunda llave era afirmación falsa de los documentos del plan, corregida también en `01-plan-maestro.md` y divulgada en CHANGELOG 4.77.0]** — más greps residuales, el antes/después de P5 y el triaje de Seguimientos.
- **Consecuencias**: (i) orden P6-R → **VERIFY → RELEASE**; (ii) Tarea 1 de RELEASE pasa a "verificar y citar"; (iii) el presupuesto de RELEASE vuelve a alcanzar para lo documental; (iv) **ninguna fase de código se reabre**: un ❌ abre sesión de recuperación al patrón P5-R/P6-R; (v) `01-plan-maestro.md` §1/§4/§6, `README.md`, `dependencias-fases.md`, `06-checklist` y `09` anotados en sus seis puntos.
- **Límite declarado por la reapertura, no resuelto por ella**: el pipeline **no volvió a correrse desde FASE-P5** (P6 y P6-R certificaron offline), así que el antes/después de VERIFY es artefacto de P4 contra artefacto generado por tests, nunca dos corridas reales; y Tier A y el contrafactual del enforcement siguen sin observar (T3b es precondición del hotel). VERIFY los escribe como límite del plan, igual que los declaraba AC-V1.

### FASE-P2 (2026-09-14) — decisiones DA-P2.1 … DA-P2.4

> **Ninguna re-abre Q1/Q1b/Q2/Q5/Q6/Q7.** Las cuatro son consecuencias que el contrato
> no podía prever sin la medición que él mismo ordenaba hacer, y se registran aquí en vez
> de ejecutarse como refactor conveniente (regla §15.4.1).

- **DA-P2.1 — el acta deja de viajar dentro del ZIP de cliente** (respuesta a la
  verificación obligatoria de la Tarea 1, abierta en DA-P1.4).
  - **Medición**: `output/v4_complete/deliveries/hotelsalentoreal_20260911.zip` contenía
    `ASSETS/v4_audit/acta_revision.json` y `ASSETS/v4_audit/acta_revision.md` (2 de 40
    miembros). El acta cae dentro de `source_dir.rglob("*")` porque `ActaWriter` escribe
    en `v4_audit/` y el packager empaqueta `output/<hotel>/` completo.
  - **Por qué no se podía cumplir el enunciado**: DA-P1.4 exige «si la contuviera, el acta
    publicada debe ser la enriquecida, nunca la pre-veredicto». Pero los bytes del ZIP se
    serializan en `write()`, y el enriquecimiento requiere los 4 informes, que requieren
    leer **ese ZIP**. Pedir que el ZIP contenga el acta enriquecida es pedir un objeto que
    contiene su propia consecuencia: **círculo estricto, no un problema de orden**.
  - **Decisión**: el acta se excluye del paquete de cliente por nombre
    (`DeliveryPackager._INTERNAL_DOC_PREFIXES`) y queda solo en `v4_audit/`, que es la
    ruta que el propio contrato §3.3 muestra al operador. Con esto «nunca se publica un
    acta pre-veredicto» se cumple **por construcción** y no por disciplina de orden.
  - **Alternativas rechazadas**: (i) reescribir los miembros del `.tmp` tras el veredicto
    — segundo pase de escritura, que es exactamente O3 (descartada en DA-P1.4) y rompería
    `MANIFEST.files[].size_bytes` contra `_validate_zip`; (ii) no escribir el acta hasta
    publicar — deja el `enforcement` y las `corrective_actions` fuera de cualquier
    paquete, pero un re-run sobre un `v4_audit` con acta vieja la embarcaría (el filtro
    de frescura es de 60 s, no una garantía). **Cambio visible para el cliente** → va al
    CHANGELOG en RELEASE (lista de §E del `09`).
- **DA-P2.2 — la familia `OK_*` tiene dos miembros**, no uno.
  - **Motivo medido**: el ejemplo del §4 del contrato fijaba
    `"status": "OK_NO_FINDINGS"` **junto con** `"findings_count": 1`, y la matriz §2.1
    escribe la condición como `status == OK_*` (comodín). Con un solo `OK_`, el acta de la
    corrida real diría «sin hallazgos» sobre un informe con 1 CRITICAL: el mismo defecto
    de fidelidad que NR8/`DA-C3` existen para impedir.
  - **Decisión**: `ReviewerStatus` conserva **los cuatro estados de Q6 literales** y añade
    `OK_WITH_FINDINGS` como segundo miembro de la familia que la propia matriz ya usaba.
    `blocks_approval` es `False` solo para los dos `OK_`; el resto del contrato no cambia.
  - **Lo que NO es**: no es un quinto estado de NR8 (los cuatro de la lección siguen
    siendo cuatro y `test_los_tres_estados_no_colapsan` los verifica) ni se re-decide Q6.
- **DA-P2.3 — `_validate_zip` corre sobre el `.tmp`, antes del rename.**
  - El contrato §3.1 pide que «el ZIP final nunca exista para el cliente, ni siquiera
    momentáneamente con nombre definitivo». El orden heredado validaba **después** del
    rename y borraba el ZIP publicado si fallaba, es decir: existía con nombre definitivo
    durante la validación. Mover la validación a la cuarentena elimina esa ventana sin
    tocar el criterio ni el `DeliveryValidationError`.
- **DA-P2.4 — la fila 4 de la matriz se implementa como guard del veredicto, no como
  mutación de la cláusula.**
  - El contrato dice «fuerza `NOT_EVALUABLE` en su cláusula → `APROBADO-PARA-ENTREGA`
    imposible». El **efecto exigido** (hacer imposible el veredicto máximo) se implementa
    en `_compute_verdict`; **no** se reescribe el estado de la cláusula que publicaron los
    gates.
  - **Por qué**: mutar P6.1/P6.3/P6.4 a `NOT_EVALUABLE` porque un revisor no pudo leer
    escribiría en el acta que el *gate* no se evaluó, cuando sí se evaluó y pasó — es la
    familia de NR2 (el tribunal no recalcula gates) en su versión destructiva, y vaciaría
    de contenido las cláusulas que los tests del Juez assertan desde `gate_report`.
    **Medido en el retro**: con la implementación elegida las 6 cláusulas del acta
    congelada quedan idénticas y el veredicto igualmente no puede ser
    `APROBADO-PARA-ENTREGA` mientras falte un informe.
  - **Consecuencia registrada**: `evaluate()` (primera pasada, 4×`NOT_RUN`) ya no puede
    certificar entrega. Es el diseño pedido (regla 2 de DA-P1.6), y por eso **dos tests de
    `test_judge.py` se migraron** a la segunda pasada en lugar de relajarse el contrato.

---

## Lecciones Aprendidas (mínimo 3 por fase con aprendizaje; al cerrar cada fase)

| ID | Pasó / Qué | Por qué | Cura (y si es regla sin verificador, declarada — L-R.4) | Pertinencia |
|----|-----------|---------|----------------------------------------------------------|-------------|
| L-AJUST.1 | Un plan citaba como viva (`bd2bf57`, "local sin push", "sin tag") una verdad que un rebase y un push posteriores invalidaron | Las citas de commit se escribieron en presente en la concepción y ningún check las contra-verifica al re-verificar el plan | Higiene aplicada 2026-09-14: citar el commit **vivo en origin** y distinguir en el texto lo histórico ("existen desde v2.21.0") de lo presente. Sin verificador mecánico → límite declarado (L-R.4) | INCLUIR — aplicable a todo plan largo con predecesor |
| L-P1.1 | La opción barata de Q2 estaba **fuera de la lista**: O1/O2/O3/O4 se concibieron como alternativas mutuamente excluyentes y ninguna aprovechaba que `_create_zip_single_write` ya recibe `manifest_bytes`/`implementation_order_bytes` **finalizados** y que `package()` escribe `.zip.tmp` antes del rename atómico | El menú se escribió desde el diseño concepción ("¿dónde corren los revisores?") y no desde la medición del artefacto ("¿qué costura ya existe?"); la pregunta bien hecha — *¿dónde cae la decisión?* — no estaba en la tabla | Toda pregunta de arquitectura de este plan se formula después de re-leer el artefacto y **nombra la costura existente** si la hay. Sin verificador mecánico: lo disciplina la Tarea 1 del prompt de fase → límite declarado (L-R.4) | **INCLUIR** — la clase de error que produce refactor más caro del necesario |
| L-P1.2 | El docstring de `_compute_verdict` prometía "Finding CRITICAL de revisores → DEVOLVER-CORRECCIONES" y el cuerpo de la función no tenía ni el parámetro: contrato documentado y código divergían | Un docstring escrito en T1 describió la intención del plan, no lo que se implementó, y ningún check compara el texto con la firma | En P2, la matriz del contrato se refleja en la **firma** de `_compute_verdict` y su test; un docstring no es evidencia. La lectura del texto para juzgar el comportamiento es la misma falla que L-V.2 prohíbe sobre las notas | **INCLUIR** — leer el cuerpo, no el comentario |
| L-P1.3 | La pregunta Q2b estaba formulada como "(a) **o** (b)" y la medición mostró que eran **las dos mitades de un mismo fix**; además AC8 y AC-F2 resultaron ser el mismo defecto (un lector escrito contra un layout descomprimido) con dos víctimas | El plan ereditó la disyuntiva de la concepción sin medirla; al medir las dos capas, la disyunción se autodestruyó | Antes de fijar una pregunta de decisión, **verificar que las opciones son excluyentes**. Si la medición muestra que un "o" es un "y", se re-enuncia la pregunta, no se elige una rama | **INCLUIR** — aplicable a cualquier tanda de decisiones |
| L-P1.4 | **Presupuesto superado**: 30 iteraciones declaradas ≈ 30 `ids` / 58 `tool_use` (unidad auto-reportada, D-V2.1) | Causa medida: (i) el Paso 0 de fuentes obligatorias de esta fase son **11 documentos** y hubo que leerlos; (ii) la Tarea 1 exige re-leer 14 símbolos por evidencia propia (L-V.2) y no heredar; (iii) 8 preguntas de decisión con su tanda, dos de ellas corregidas en vivo | Un presupuesto de fase debe declararse **junto con su instrumento de medición**, y D-V2.1 (el instrumento no alcanza el transcript) debería forzar a revisar el presupuesto de las fases de decisión: 30 era un número de fase de código. Propuesta para el sucesor: fase de decisión con ≥6 preguntas → presupuesto en función de `fuentes obligatorias × símbolos a re-leer`, no del tamaño del entregable. Regla sin verificador → declarada (L-R.4) | **INCLUIR** — el nº va en la celda, no en la memoria |
| L-P1.5 | Un `except Exception` que solo imprime un warning envuelve **las tres** cadenas que este plan decide (FASE-K, el bloque del Juez y el de cada revisor): en las tres, el fallo se manifiesta como **ausencia silenciosa**, no como error | No es un defecto de la fase: es el patrón never-block del repo aplicado a código que además **declara valores por defecto** (`"C"`, `[]`, `financial_breakdown = None`) | NR8/Q6 extiende su regla a los gates: ningún camino puede producir "sin hallazgos" desde "no leí". En AC-F5 el test "sin `NameError` con `generate_proposal=False`" es obligatorio por esto mismo | **INCLUIR** — patrón transversal de `main.py` (familia L-T2C.2) |
| L-P3A.1 | **Presupuesto superado otra vez**: 20 declaradas, **106 `ids` / 126 `tool_use` medidas** (corte = `0d4d072`). A diferencia de P1, el instrumento **sí alcanzó el transcript** → D-V2.1 **no se reprodujo** y la cifra es medida | Causa medida: una fase de detección/fidelidad **paga su verificación, no su código** — el delta R2.7 exige **dos corridas de la suite completa** (4.130 tests, ~7-9 min cada una) y NR7 exige **4 pares verde/rojo** que a su vez relanzan subsets; el código editado son 2 archivos. 20 era un presupuesto de "tamaño del entregable", y el entregable chico esconde la verificación cara | Presupuestar la fase por **coste de verificación** (`corridas NR1 × duración + pares NR7`), no por líneas de código. Corolario: como D-V2.1 no se reprodujo, **no hay excusa para auto-reportar** — la celda de iteraciones se mide con el instrumento. Regla sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — segunda fase seguida que revienta el presupuesto por la misma causa raíz (nº de fase de código aplicado a trabajo de verificación) |
| L-P3A.2 | AC-F4 parecía un fix de una línea (añadir el tier a `FIRST_FLOOR_TIERS`) y la clave correcta es **`"B+"`**, no `"B_PLUS"`: `EvidenceTier.B_PLUS` **serializa como `"B+"`**. Con `"B_PLUS"` el set queda con un miembro fantasma, el primer piso sigue sin aplicarse y **un test que use la forma larga del enum daría verde igual** | El fix dependía de una constante string que espeja un enum, y el nombre del enum (`B_PLUS`) no es su valor serializado (`B+`). Es la misma trampa que L-T4A.5 (test vacuo): el verde no prueba el régimen real | Cuando un fix se ancla a un string que espeja un enum, el test **assertúa contra `Enum.MIEMBRO.value`**, nunca contra el nombre escrito a mano. Los 4 tests de AC-F4 usan `"B+"` literal + una comprobación de que `EvidenceTier.B_PLUS.value == "B+"` | **INCLUIR** — familia L-T4A.5; aplicable a todo gate que compare contra enums serializados |
| L-P3A.3 | El conteo canónico había **derivado**: 4.063 (v4.76.0) → 4.109 en el PRE de P3-A, porque el plan ajeno PASO0-VERIFICADOR-CAPITALIZACION añadió ~45 funciones test entre medias. Restar contra el ancla "pre-plan" documentada habría dado un **+66 falso** y se habría leído como baseline contaminado | El número "pre-plan" del `09`/AGENTS es un **ancla histórica** de v4.76.0, no el baseline R2.7 de la fase; entre una y otra entraron commits ajenos | R2.7 se resta contra el **snapshot PRE propio de la fase**, tomado al iniciar (4.130 − 4.109 = **21** = tests nuevos, íntegro en `passed`). El ancla pre-plan se reconcilia aparte (4.063 + 45 ajenos + 21 P3-A = 4.129 canónico). Es exactamente lo que la regla R2.7 ya dice; la lección es **no confundir el ancla con el baseline** | **INCLUIR** — aplicable a cualquier fase que arranque sobre un HEAD que ya no es el del plan |
| L-P3B.1 | **Presupuesto superado por tercera fase consecutiva**: 25 declaradas, **120 `ids` / 120 `tool_use` medidas** (corte = `bad0a5e`). P1: 30→≈30/58 (auto-reporte). P3-A: 20→106/126. P3-B: 25→120/120. La serie es el hallazgo, no el caso | El presupuesto se declara **por tamaño del entregable** y lo que domina el coste es la **verificación**: el par NR1 son dos corridas de la suite completa (4.130+ tests, 7:30 y 10:21 min), y NR7 son 6 mutaciones que cada una lanza el verde y el rojo. En P3-B el código editado son **2 archivos de producción**; la verificación costó ~6× más que el fix | **Cambio de regla propuesto, no nota de fase**: que el executor declare el presupuesto de una fase con AC de detección/bloqueo como `2 × corrida_completa + pares_NR7 × corrida_subset + edición`, y no como un número fijo por "complejidad". Las tres fases de este plan que llevaron la fórmula en el encabezado ("MEDIA/ALTA") fallaron igual. **D-V2.1 queda en observación, no confirmada**: el instrumento alcanzó el transcript en P3-A y P3-B, así que la limitación que justificaba auto-reportar **no se reprodujo** y la celda debe medirse. Regla sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — tercera ocurrencia de la misma causa raíz; ya es patrón, no excepción |
| L-P3B.2 | `test_barreda_un_solo_emisor_de_la_clave` tenía **dos** aserciones y solo una se ejecutaba: la de `emisores` fallaba desde v4.76.0, así que la de `consumidores` —escrita debajo— **nunca fue evaluada**. Al arreglar la primera resultó que la segunda también estaba rota (Bot 3 consume `entry.get("asset_path")` desde que existe) | Un test en rojo por su primera causa deja **sin vigilar todo lo que hay después de esa causa**, y el rojo visible ocultaba un segundo defecto. Es la cara opuesta de L-T4A.5: allí un verde no probaba nada; aquí un **rojo** no probaba nada de lo que venía después. NR7 mira si el test *puede* fallar; no mira si **llegó** a la aserción | (i) Al cerrar un test que llevaba rojo, **ejecutar y leer la salida completa**, no solo el cambio de color: el primer mensaje de error distinto al esperado es la señal de la aserción sucesiva rota. (ii) A una whitelist no le basta la mutación "retiro la autorización → rojo" (que es la que pedía el plan): se agregó **"aparece un tercero no contratado → rojo"** (`NR7-AC-F3-b.txt`), porque una contención que solo puede fallar por retirada es una barra decorativa. (iii) Candidato a regla: separar en dos tests las aserciones que hoy comparten un cuerpo, para que una no enmascare a la otra | **INCLUIR** — familia L-T4A.5/L-VUP-5, con un modo nuevo (rojo que oculta, no verde que miente) |
| L-P3B.3 | La promesa de Q5/AC-F5 decía "propagar la **disponibilidad real**", y medido en `main.py` solo existía la mitad: `ga4_available` se calculaba y se tiraba, pero `gsc_available` **no se calculaba en ninguna parte** del flujo — `AnalyticsStatus` lo declara en `False` y nadie lo asigna en `v4complete`. Propagar solo lo que había habría dejado el Tier A **exactamente igual de inalcanzable** (la regla exige `ga4_enabled and gsc_enabled`) y el AC se habría cerrado en verde sin arreglar el síntoma que lo justificó | El plan heredó del párrafo §2.1 del maestro una premisa formulada en plural ("la disponibilidad real se calcula más abajo") y la midió solo en su caso GA4. La restricción "NO re-decidir Q5" (L-VUP-6) **no** obliga a ejecutar literalmente un enunciado cuyo input resultó no existir | Antes de aplicar un fix, **verificar la premisa de cada una de sus mitades**: "¿existe ya un valor real que propagar, para cada fuente?" Si no existe, el fix es *calcularlo*, y eso hay que decirlo en la fase —no es re-decidir Q5, es ejecutarla de verdad. Y cuando calcularlo abre un segundo lector divergente (`gsc_configured` del MANIFEST leyendo el campo muerto), se cierra en la misma fase por **L-SR3**, no se deja para otra | **INCLUIR** — el fix que se habría cerrado falso de no medirse; aplicable a todo "propagar el valor real" |
| L-P2.1 | La «verificación obligatoria antes de tocar» de la Tarea 1 no confirmó un supuesto: **demostró que el enunciado que la pedía es insatisfacible**. Medido: el ZIP contenía el acta (`ASSETS/v4_audit/acta_revision.{json,md}`, 2 de 40 miembros). Bajo O1-cuarentena el acta enriquecida necesita los informes de los Bots, los Bots necesitan leer ese ZIP, y el contrato pide que el ZIP contenga ese acta. **Círculo estricto**, no un problema de orden que «P2 resuelve reordenando» | El contrato redactó la cláusula como imperativo condicional («si la contuviera, que sea la enriquecida») sin preguntar **si las dos mitades del condicional pueden coexistir en el mismo objeto**. Es la misma forma de defecto que L-P1.3 cazó en Q2b: un enunciado que supone un «o» donde la medición encuentra una imposibilidad | Toda cláusula de contrato con forma «si X, entonces Y» se re-lee buscando **si X e Y son simultáneamente satisfacibles**. Cuando no, la fase no elige una rama: registra la imposibilidad (DA-P2.1) y toma la tercera vía que la vuelve inofensiva. Sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — familia L-P1.3/L-V.2; cuarta fase del plan en la que una verificación ordenada cambia el diseño en vez de confirmarlo |
| L-P2.2 | El enum de estados del contrato **no alcanzaba para decir la verdad**: su §4 fijaba cuatro nombres y su propio ejemplo ponía `"status": "OK_NO_FINDINGS"` junto con `"findings_count": 1`. Con un solo `OK_`, el acta de la corrida real de Salento habría dicho «sin hallazgos» sobre el informe que contiene el CRITICAL `VACUOUS_RECALL` que el predecesor celebró como detección viva | La matriz §2.1 ya escribía la condición como `status == OK_*` — un comodín que presupone una **familia** que la enumeración no definía. El hueco estaba en el contrato, no en el código, y solo se hace visible al implementar la serialización del DTO | Antes de cerrar un literal del contrato, **chequear que la enumeración cubra cada ejemplo del propio contrato**. Si el ejemplo y la lista no son simultáneamente satisfacibles, se preserva el invariante (los cuatro estados de NR8 siguen siendo cuatro) y se refina la familia (`OK_WITH_FINDINGS`), registrándolo como decisión (DA-P2.2) y no como detalle de implementación | **INCLUIR** — familia L-P1.2 (texto que no cuadra con el artefacto) aplicada al contrato mismo, no al docstring |
| L-P2.3 | Un **bug de sombreado de variable** sobrevivió a 17 tests verdes: dentro de `derive_corrective_actions` el acumulador local se llamaba `blocking`, igual que el parámetro, así que al salir del bucle `if not blocking or actions: return actions` leía el valor del último informe y **el fallback de bloqueo por gate no se ejecutaba nunca**. Lo cazó el test nº 18, escrito para la *otra* mitad de AC-E5 | El test que cubría el camino obvio (CRITICAL de revisor → acciones) nunca pasaba por la rama posterior al bucle, y el mutante NR7 tampoco la ejercía: `NR7-AC-E5-b.txt` vacía las acciones desde el inicio, no desde el sombreado. NR7 pregunta «¿puede fallar este test?», no «¿llegó a ejecutarse esta rama» — hermano de L-P3B.2, que encontró lo mismo con una aserción que jamás se evaluaba | (i) Un fallback «que no debería dispararse nunca» **necesita su propio caso**, porque suele ser el único que ejercita el código posterior a un bucle. (ii) Prohibido reutilizar el nombre de un parámetro como acumulador local en una función con dos niveles de decisión: en Python no hay error y sí hay silencio. Candidato a regla: *toda función con parámetro booleano y bucle que asigne variables de nombre afín lleva un test por rama posterior al bucle* | **INCLUIR** — familia L-T4A.5/L-P3B.2; el verde de 17 tests no cubrió lo que cubrió el test 18 |
| L-P4.1 | El disparador del diferimiento de P4 estaba redactado como pregunta de existencia («si al iniciar P4 **no hay** dato con fuente → cierre sin P4») y, leído sin medir, se cumplía: no hay YAML de onboarding de Don Alfonso en `output/clientes/`. Medido: **6 de 6** hoteles del warehouse resuelven en el pipeline por el fallback de `_load_latest_onboarding_data` → `_observation_to_onboarding_format`, y el propio DA-P1.9 ya decía «T3a satisfecha hoy» para Don Alfonso/Luxor. **P4 se habría diferido por leer literal un disparador que el código del plan ya satisfacía** | El disparador se escribió en la concepción desde el estado del directorio de YAML, no desde la ruta de carga que el plan **modificó** en fases anteriores (AC-F5 tocó ese bloque; FASE-P2 tocó el resolutor de entrega). Un disparador de diferimiento es una afirmación sobre el runtime y envejece igual que el código | Antes de invocar un escenario de diferimiento, **ejecutar el camino que el disparador nombra** y pegar la salida en la evidencia (aquí: `t3a_sonda_candidatos.py`, que además midió el techo de tier). Y al cerrar una fase que toca el bloque que otro disparador consulta, anotar en ese disparador que fue re-medido. Sin verificador mecánico → límite declarado (L-R.4) | **INCLUIR** — aplicable a todo «fase opcional si no hay X»: la opcionalidad la decide una medición, no la redacción |
| L-P4.2 | Sospecha de **contaminación cruzada entre hoteles**: el acta de Don Alfonso trae `EMPTY_DELIVERY_TEMPLATE` sobre un `IMPLEMENTATION_ORDER.md` de 470 bytes, pero en el árbol final no hay ZIP ni ese archivo — y en `output/` sí hay **tres** `hotelsalentoreal_*.zip` con ese mismo miembro de 468 bytes. Todo apuntaba a que `_resolve_delivery_zip` tomó el paquete de otro hotel. **Falso**: el log registra `[OK] Paquete en cuarentena: …/hoteldonalfonso_20260914.zip.tmp`, el revisor leyó el correcto, y el archivo se **suprimió después** por el propio veredicto | La causa del falso rastro es estructural y nueva: **`suppress()` destruye el artefacto que los revisores leyeron**, así que el resultado de enforcement borra la evidencia con la que un auditor posterior puede comprobar qué vio Bot 3. Ausencia del artefacto ≠ ausencia del lectura | (i) Antes de registrar un hallazgo de «el lector tomó el artefacto equivocado», buscar la **ruta resuelta** en el log de la corrida, no concluirla del árbol final. (ii) hace falta que el ZIP suprimido deje rastro verificable (sha256 y recuento de miembros en el acta, o un archivo de cuarentena conservado) → **F-P4.9, con dueño en RELEASE**. Sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — consecuencia no prevista de O1-cuarentena: el enforcement pierde su propia prueba |
| L-P4.3 | **El caso que motivó el enforcement resultó inobservable con el enforcement puesto.** La primera pasada del Juez ya daba `BLOQUEADO` por gates (coherence), así que los 4 CRITICAL de los Bots no cambiaron el resultado: la rama «gates aprueban y un revisor objeta» —que es la razón de ser de Q1— no se ejercitó. Y no podrá: F-P4.1 muestra que la plantilla del paquete es un stub en **todas** las corridas, así que mientras eso siga así todo run real se bloquea antes de ese cruce | El plan trataba «el tribunal bloquea» como éxito sin preguntar **qué deja de poder medirse** cuando bloquea siempre. Un veredicto que solo puede ser BLOQUEADO informa de la plantilla, no del hotel | Declarar el contrafactual como **límite de certificación** (hecho: informe §punto 5 y §4.2) y abrir la decisión de producto «¿un stub debe bloquear?» (F-P4.1) **antes** de volver a correr P4 con otro hotel. Para AC-V1 de RELEASE: la certificación del enforcement descansa en los 8 pares NR7 de P2, no en esta corrida | **INCLUIR** — familia L-P2.1: una cláusula que solo puede satisfied en un sentido oculta la otra mitad del contrato |
| L-P4.4 | El comparator estructural de la propia fase (**no** código de producción) dio un **falso verde**: `discover()` emparejaba stems con `startswith`, así que `coherence_validation` se quedaba el archivo de `coherence_validation_post_gen` (igual `pain_ledger`/`_resolved`) y dos filas del diff comparaban **un archivo consigo mismo** mientras el selftest seguía en verde. Se detectó porque dos artefactos reportaban idéntico conteo de claves | La premisa del check era «diff cero sobre input idéntico» — exactamente lo que produce una colisión de stems. Un selftest que solo prueba identidad no distingue «no hay delta» de «no estoy mirando dos cosas» | (i) La regla L-VUP-14 («probar el parseo contra el baseline **antes** de la corrida») **se pagó sola**: sin ella la colisión habría llegado al informe. (ii) Cure aplicada al instrumento: patrón anclado por stem, stems de mayor a menor, cada archivo reclamado una vez, y un check mecánico de **colisión de stems** en `--selftest`. (iii) Candidato a regla del plan: todo comparator lleva **una mutación plantada** además del diff cero (ya está en el script) | **INCLUIR** — familia L-T4A.5/L-P3B.2 aplicada al instrumento de medición de esta fase, no al producto |
| L-P5.1 | **El escaneo de secretos staged nunca corrió**: `import re` quedó solo dentro de `_check_no_secrets` (scope de función) y `_check_staged_content` usaba el global → `NameError` → su propio `except Exception: return []` lo tragaba. El check `[4/9]` de b25b63a daba verde con secretos en el índice. Reproducido en repo temporal antes de arreglar (ver `REMEDIACION-auditoria-2026-09-15.md` §1) | **L-T2C.2 reproduce dentro del instrumento de este plan**, dos planes después de haberse capitalizado: un NameError latente sobrevive si su consumidor está bajo un `except Exception` ancho. Y el par NR7 de AC-S2 no lo cazó porque mutaba los patrones de valor, no la ruta staged — el test jamás llegó a la rama (hermano de L-P3B.2: no «si puede fallar» sino «si se ejecutó») | Cure estructural, no el import: la ruta queda vigilada por tests **con repo git real** que ejercitan la divergencia índice-vs-árbol (`TestStagedVsWorktreeDivergence`), y el `except Exception` ya no puede ocultar un NameError con `re` a nivel de módulo. Candidato a regla: todo detector nuevo lleva un test que **falle si su lector interno muere**, no solo si el patrón cambia | **INCLUIR** — familia L-T2C.2/L-P3B.2/L-T4A.5 aplicada al checker de secretos |
| L-P5.2 | `NR7-AC-S2-green.txt` declaró «Estados NR8: OK / SIN_HALLAZGOS / NO_LEGIBLE / NO_CUBIERTO **implementados**» cuando dos de los cuatro solo existían en el docstring, y «Contenido staged: sí» cuando estaba muerto (L-P5.1). La auditoría forense lo encontró leyendo el código, no la evidencia | El registro de cierre se escribió desde la intención del fix, no desde la salida del artefacto — exactamente lo que L-P1.2 prohíbe («un docstring no es evidencia»), pero aplicado al **acta de fase**: la promesa publicada sin verificar contra la ejecución es EVIDENCE-TIER-FALSE-CONFIDENCE con sello de verde | La corrección se hizo **como anotación sobre el registro histórico**, sin reescribirlo (la evidencia vieja debe mostrar su propio error). Y el estado ahora lo emite el código: el verde se nombra `SIN_HALLAZGOS` con el conteo de leídos, `NO_CUBIERTO` bloquea. Regla sin verificador mecánico (¿quién contra-verifica cada afirmación del acta?) → límite declarado (L-R.4) | **INCLUIR** — familia L-P1.2 extendida del docstring al acta de fase |
| L-P5.3 | El checklist de P5 exigía «Iteraciones medidas y escritas (unidad + corte declarado — un `—` no cierra)» y la fase cerró con la celda vacía en `06` y sin registro en `dependencias` — el presupuesto FUERA DE SERVICIO se leyó como exención de medir, cuando exige lo contrario: registrar **medida real con unidad declarada** al cerrar | L-R.1 nombró esta falla en 8 de 9 fases del predecesor y este plan la reprodujo en la primera fase con presupuesto suspendido: «fuera de servicio» cancela la **estimación**, no la **constatación** | La celda se cerró en la remediación: unidad declarada = commits de ejecución por sesión (P5: `b25b63a` + 1 de remediación), corte = HEAD al cerrar, D-V2.1 reproducido (transcript fuera de alcance). Candidato: que el `log_phase_completion` o el checklist exijan la celda **antes** del ✅ — regla sin verificador → declarada (L-R.4) | **INCLUIR** — tercera fase del repo perdiendo la celda de iteraciones; ahora es patrón con cura pendiente de verificador |
| L-P6.1 | El parámetro `blocks` de `blocks_delivery_zip(acta)` y la variable `blocks_publish` en `main.py` forman un **contrato de dos llaves**: `blocks_publish = bool(blocks) and enabled`. Un test que pasa `blocks=False` y aserta `blocks_publish == True` prueba una combinación imposible — el verde no mide nada porque la conjunción ya es falsa en su primera rama | La firma del gate devuelve un booleano y el caller lo combina con `enabled`, pero el test no distinguió «el gate dice que bloquea» de «la publicación se bloquea». Son dos preguntas distintas unidas por un `and`, y el test respondía la segunda sin ejercer la primera | Todo test de un gate compuesto **ejercita cada llave por separado**: (i) `blocks=True, enabled=True → publish blocked`, (ii) `blocks=True, enabled=False → publish allowed`, (iii) `blocks=False, enabled=True → publish allowed`. El NR7 de AC-G5 incluye los tres caminos causales como pares verde/rojo independientes | **INCLUIR** — familia L-T4A.5/L-P2.3; el `and` silencioso es la variante booleana del sombreado de variable |
| L-P6.2 | La regla del primer piso (`first_floor_rule`) para Tier C produce `VERDICT_CONDITIONAL`, **no** `VERDICT_BLOCKED`. El bloqueo de la publicación en Tier C viene del **caller** (`main.py` pasa `blocks=True` al gate), no del Juez. Un test que aserta `verdict == BLOCKED` sobre un Tier C está probando contra el mecanismo equivocado | El contrato del Juez dice «primer piso limita el veredicto» y la implementación lo cumple: `FIRST_FLOOR_TIERS` incluye `"C"` y el tope es `CONDITIONAL`. Pero el comportamiento observable (la publicación se bloquea) es real — solo que vive en el caller, no en el Juez. La capa de decisión está **partida** en dos archivos y el test la miraba como si fuera una | Al escribir un test de veredicto, **identificar dónde vive la decisión que se aserta**: si el Juez caporea y el caller bloquea, el test del Juez aserta el capado (`CONDITIONAL`) y el test del caller aserta el bloqueo (`blocks=True`). Mezclarlos produce un test que no puede pasar porque busca en el archivo equivocado lo que está en el otro | **INCLUIR** — familia L-P1.2 (texto que no cuadra con el artefacto) aplicada a la arquitectura de decisión partida |
| L-P6.3 | La matriz multi-hotel offline prueba **lógica de perfiles** (≥3 perfiles con combinaciones distintas de banderas), no **confianza estadística**. Un perfil que pasa no mide que el pipeline funcione para ese hotel; mide que el cableado responde correctamente a esa combinación de inputs | El AC-G4 pidió «≥3 perfiles» y la implementación entregó 3 perfiles con banderas booleanas (ga4/gsc/onboarding). Cada perfil ejercita un camino del cableado, pero ninguno ejercita un hotel real — la diferencia entre «el código hace lo que debe» y «el resultado es correcto para este hotel» es la misma que L-V.1 (fixture ≠ régimen real), aplicada a una matriz | La matriz offline es **necesaria pero no suficiente**: certifica que el cableado no se rompe con combinaciones extremas, no que el pipeline produce resultados válidos para un hotel concreto. La certificación contra datos reales sigue siendo P4 (corrida de observación) y su límite (Tier A sin observar, T3b) sigue abierto | **INCLUIR** — familia L-V.1; la matriz es el puente entre los tests unitarios y la corrida real, no su sustituto |
| L-P6.4 | Un instrumento de reversión del fix debe **auto-verificarse antes de certificar rojo**: el primer `nr7_p6r_mutation_checks.py` falló tres veces por criterios propios (hash truncado comparado contra hex completo → restauración "fallida" que dejó el árbol mutado a medias; patrones `\n` contra archivo CRLF → 3 de 5 pares abortados silenciosamente como «no aplicado»; criterio `red is False` invertido → pares buenos marcados FAIL). Y su predecesor de P6 mutó expectativas del test creyendo revertir el fix | L-T4A.5 en su segunda aplicación dentro del mismo plan: «un test puede pasar sin ejecutar la rama que certifica» vale también para el **script que certifica el rojo** — un ABORT por patrón no encontrado contaba como fila en la tabla y casi pasa por par. Es además la tercera fase seguida donde el registro de cierre se escribe desde la intención del fix, no desde la salida del artefacto (L-P5.2, L-P1.2) | El instrumento exige: coincidencia exacta obligatoria (n==1 o ABORT explícito impreso en el total), restauración verificada con fsync + relectura + reintento, y conteo final que distinga «par ejecutado» de «par no aplicado». Con esas tres barreras: 5/5 pares con rojo real. Candidato a regla: **todo verificador nuevo corre primero contra un fallo plantado** (como el `--selftest` del diff de P4, L-P4.4) | **INCLUIR** — familia L-T4A.5/L-P5.2; el instrumento de mutación es código de producción de la evidencia |
| L-VERIFY.1 | Los documentos del plan (D-AJUST.4, `01-plan-maestro.md`) mencionan `GATE_BLOCKING_ENABLED` y `GATE_ENFORCEMENT_ENABLED` como dos llaves del bloqueo. Medido con grep: **solo `GATE_BLOCKING_ENABLED` existe** en código. DA-P1.7 decidió correctamente «un solo botón», pero la documentación no se actualizó | L-P1.2 aplicada a documentos de plan: el texto afirma la existencia de un símbolo que el código no tiene. Las sesiones de decisión no contra-verificaron nombres de variables contra el código | Todo documento que nombre un símbolo de código debe verificar su existencia con grep antes de publicarlo. Hallazgo CON-1 → C8 del triaje (corrección documental en RELEASE). Sin verificador mecánico → declarada (L-R.4) | **INCLUIR** — aplicable a todo plan largo con múltiples sesiones de decisión |
| L-VERIFY.2 | La matriz T1 buscaba `blocks_publish` en el acta JSON y no lo encontró. Existe en `TribunalOutcome` (outcome.py L153) como atributo DTO, pero `acta_writer.py` no lo serializa. El acta comunica el bloqueo via `verdict` y `enforcement` | La distinción «campo del modelo interno» vs «campo del artefacto publicado» no estaba explícita. Un certificador puede esperar encontrar en el JSON todo lo que existe en el DTO | Al citar un campo como evidencia, verificar que el writer lo serializa (grep en `acta_writer.py`), no solo que el DTO lo declara. La matriz debe distinguir «campo DTO» de «clave JSON» | **INCLUIR** — aplicable a todo sistema con modelo interno + artefacto publicado |
| L-VERIFY.3 | El cruce P5×todo (T2) reveló que `google_places_client.py:_save_cache` persiste caché sin redacción, mientras `gbp_auditor.py:_save_cache` sí redacta. P5 había certificado AC-S2/AC-S4 pero ninguno cubría redacción en cachés de scrapers. **El gap solo se hizo visible al cruzar la ruta completa** | Cada fase certifica sus ACs en aislamiento. P5 miró el checker y el inventario, pero no auditó todos los `_save_cache`. La integración cross-fase es el único momento que puede ver «¿hay otro writer que persista datos sensibles sin redacción?» | FASE-VERIFY debe incluir siempre un cruce que audite la ruta completa de datos, no solo ACs individuales. Hallazgo → C1 del triaje (plan sucesor, prioridad media) | **INCLUIR** — es la razón de ser de FASE-VERIFY (§4.6 del executor) |
| L-VERIFY.4 | El acta de Don Alfonso muestra cláusulas P6.2/P6.5 con `NOT_EVALUABLE`, pero los revisores (Bot 2, Bot 4) sí corrieron y produjeron hallazgos (`OK_WITH_FINDINGS`, `BLOQUEAR`). El veredicto consume los 4 reportes, pero las cláusulas no reflejan que los revisores evaluaron | DA-P2.4 decidió que la fila 4 de la matriz es guard del veredicto, no mutación de la cláusula. Preserva la fidelidad de los gates pero deja cláusulas de revisores en `NOT_EVALUABLE` aunque hayan corrido. Limitación de diseño, no bug | Documentar como límite conocido (B3 del triaje). Si se requiere fidelidad clause→verdict, el sucesor decide si las cláusulas se actualizan post-enforcement o si el veredicto basta | **INCLUIR** — tensión entre «preservar la verdad del gate» y «preservar la verdad del revisor» |

---

## Métricas de Ejecución (al cerrar cada fase)

- Tests canonónicos: pre-plan 4.063 / 293 archivos (medido 2026-09-11, v4.76.0) — actualizar con delta R2.7 por fase. **P1: 4.063 / 293 sin cambio** (fase de decisión, cero código, cero tests nuevos ni tocados; verificado por `git status` en el commit de cierre).
- `--quick`: 9 checks desde `4ac139a` (eran 8 al cierre del predecesor). **P1: 9/9 al cierre (2026-09-14 14:00).** Además, el pre-commit aportó un dato propio: el hook `[6/7]` **rechazó el primer intento** de `fd8e4f4` porque editar los `.md` del plan vence el índice de lecciones generado — la regla existe, falla fuerte y obligó a regenerar el par `.md`+`.json`. Es el patrón que este repo prefiere (verificador que truena antes que disciplina manual).
- Coherence / publicación: sin cambio en P1 (no hay corrida).
- Archivos tocados por P1: `evidence/FASE-P1/` (2 nuevos), `05-prompt-inicio-sesion-fase-P2.md` (nuevo), y los **7** documentos de plan actualizados (`00`, `01`, `06`, `README`, `dependencias`, este `10`, más `09`).
- **FASE-P3-A (2026-09-14)**: canónico **4.129 funciones / 296 archivos**; R2.7 **PRE 4.109 → POST 4.130 = +21** propios, íntegro en `passed` (4.070→4.091), con los **4 fallos ajenos idénticos** pre/post (`test_faq_generator_output_is_jsonld`, `test_function_default_flags` flaky, `test_barreda_un_solo_emisor_de_la_clave` [deuda P3-B], `test_diagnostic_includes_geo_metrics`) → **0 regresiones**. `--quick` **9/9** al cierre (743 citas históricas, 0 nuevas, 0 crecimientos). Tests del tribunal **114 → 135**. Coherence/publicación **sin cambio** (no hay corrida en esta fase). Archivos tocados: **2 `.py` de producción** (`asset_reviewer.py`, `judge.py`), **1 test nuevo** (`test_p3a_zip_tier_firstfloor.py`, 21 tests), **1 fixture editada** (`test_judge.py`), `evidence/FASE-P3-A/` y los **6 docs de plan** del cierre (`06`, `dependencias`, `README`, `09`, este `10`, `00`). Commit de código `0d4d072` (14 archivos, +1.749/−75, hooks 7/7).
- **FASE-P3-B (2026-09-14)**: canónico **4.146 funciones / 298 archivos**; pytest recolecta **4.153** (6 de las 17 funciones nuevas están parametrizadas con 2 casos). R2.7 **PRE 4.130 → POST 4.153 = +23**, con `passed` 4.091→4.115 = **+24** porque además migró de rojo a verde `test_barreda_un_solo_emisor_de_la_clave`; **3 fallos en POST, los tres ajenos y ya presentes idénticos en el PRE** → **0 regresiones**. `--quick` **9/9** y pre-commit **7/7** en `bad0a5e`. Tests del tribunal **135 → 158** (recolectados). **Se cierra D-V.1**: la fila de Tests de `AGENTS.md` queda desactualizada por este plan y su actualización es de RELEASE con el bump. Coherence/publicación **sin cambio** (no hay corrida en esta fase; `v4complete` explícitamente fuera de alcance por la restricción del prompt). Archivos tocados: **2 de producción** (`main.py`, `acta_writer.py`), **4 de test** (2 nuevos, 1 ampliado, 1 edición test-only), `evidence/FASE-P3-B/` (13 archivos) y los **6 docs de plan** del cierre. Commit de código `bad0a5e` (19 archivos, +1.993/−18). **Verificación explícita de líneas rojas**: `git show --stat bad0a5e` confirma **cero cambios** en `judge.py`, `asset_reviewer.py` y `delivery_packager.py`. Iteraciones: **120 `ids` / 120 `tool_use`** con el instrumento (corte = el commit de código), presupuesto 25 superado → L-P3B.1.
- **FASE-P4 (2026-09-14)**: **cero `.py` de producción tocados y cero tests nuevos** (la restricción del
  prompt lo prohíbe: hallazgo → seguimiento con dueño, L-V.4). Canónico **sin cambio**: 4.174 funciones /
  300 archivos, pytest 4.181, **mismos 2 fallos ajenos** que al cerrar P2 → 0 regresiones por definición
  (no hay código que regredir). Lo que sí se midió:
  - **Coste de reloj de la corrida**: 18:36:33 → 18:39 ≈ **3 min** (el presupuesto de iteraciones del
    prompt avisa de esto: la corrida consume reloj, no iteraciones).
  - **Gate que bloqueó**: `coherence`, `blocking`, con `score 0.8967 ≥ umbral 0.8` pero
    `is_coherent=False` por un único check de severidad `error` (`whatsapp_verified`). Coherencia de
    publicación en corrida real: **no apta**, y por un hueco del adaptador de datos, no del hotel (F-P4.3).
  - **Delta estructural** (R2.3, instrumento propio con selftest previo): 14 artefactos con par,
    **435 cambios de valor**, 1 artefacto del baseline sin par (`ia_readiness_report.json`), 3 claves
    nuevas en el acta (`reviewer_reports`, `enforcement`, `corrective_actions`).
  - **Evidencia**: 10 archivos versionados en `evidence/FASE-P4/` + `corrida/` (62 archivos de la
    corrida, el log y el detalle con cifras COP) **deliberadamente fuera de version** porque el remoto
    es público; el baseline ajeno queda congelado con sha256 de sus 66 archivos.
  - **Itinerario comercial no tocado**: no se envió nada al hotel; la corrida es diagnóstico.
  - **Para RELEASE**: publicar (i) que el enforcement opera en datos reales y **suprime el ZIP**, (ii)
    los 9 hallazgos F-P4.1–F-P4.9 con sus dueños, (iii) que **Tier A sigue sin observarse** y su dueño
    es T3b, y (iv) la fila de Tests de `AGENTS.md`, que ya va **cuatro fases** por detrás.
- **FASE-P2 (2026-09-14)**: canónico **4.174 funciones / 300 archivos**; pytest recolecta **4.181** (sin parametrizaciones nuevas: funciones == recolectados, y la brecha de 7 con el conteo canónico es la misma que en P3-B). R2.7 **PRE 4.153 → POST 4.181 = +28**, íntegro en `passed` (4.116→4.144), con **el mismo par de fallos en PRE y POST** (`test_function_default_flags` flaky, `test_diagnostic_includes_geo_metrics` ajeno) → **0 regresiones**. **El POST se repitió**: el primero dio +27 y quedó invalidado por un test añadido al cerrar un seguimiento → L-P2.4(ii). Tests del tribunal **158 → 177** recolectados. `--quick` **9/9** (743 citas históricas, 0 nuevas, 0 crecimientos — R2.2 respetado) y pre-commit **7/7** tras regenerar el par de índice que `[6/7]` exigió. **Delta conductual medido en el retro** (`retro-estructural-FASE-E2E.md`, sobre los artefactos congelados de la corrida E2E del predecesor): veredicto `APROBADO-CONDICIONAL-PENDING-ONBOARDING` → **`BLOQUEADO`**, `reviewer_reports` `[]` → 4 entradas tipadas, `corrective_actions` inexistente → 1, `enforcement` añadido, **las 6 cláusulas idénticas** y ninguna clave perdida. Archivos tocados: **6 de producción** (`main.py`, `judge.py`, `acta_writer.py`, `asset_reviewer.py`, `delivery_packager.py`, `__init__.py` del paquete) + **1 módulo nuevo** (`tribunal/outcome.py`, 345 líneas), **2 archivos de test nuevos** (19 + 9) y **2 migrados** (`test_judge.py`, `test_acta_serialization.py`, 3 tests con causa escrita en `nr1_delta_r2_7.md`). Commit de código `df60c24` (26 archivos, +3.288/−80). Coherence/publicación **sin cambio**: esta fase **no ejecutó `v4complete`** (así lo fija su prompt) → el comportamiento nuevo se verificó por tests contra ZIP real y por retro estructural, no por corrida. **Para RELEASE**: el cliente deja de recibir `ASSETS/v4_audit/acta_revision.{json,md}` dentro del ZIP (DA-P2.1) — cambio visible, se declara en CHANGELOG. Iteraciones: **auto-reporte con unidad declarada** (D-V2.1 se reprodujo: el transcript está fuera del workspace y el acceso lo bloqueó el clasificador), corte = `df60c24`, presupuesto 55 superado → L-P2.4.
- **FASE-P5 (2026-09-15, cierre técnico + remediación post-auditoría el mismo día)**: canónico **4.195 funciones / 302 archivos** (P5 original: +11 en `tests/auditors/test_p5_ac_s1_secret_sanitization.py`; remediación: +10 en `tests/test_p5_ac_s2_remediacion.py`). R2.7 **PRE 4.134+11 = 4.145 → POST 4.155 = +10** en la remediación, `passed` 4.145→**4.155**, **mismos 2 fallos ajenos** (`test_function_default_flags` flaky, `test_diagnostic_includes_geo_metrics`) → **0 regresiones**. **Un primer POST de 4.153 con 4 rojos quedó invalidado y se re-corrió**: los 2 rojos nuevos los causó el YAML de política por faltarle `version`/`description` que exige el contract test de `config/` — el contrato de config funcionó como detector, no como burocracia. `--quick`: **10/10** (9→10 checks; full 13→14; contract test del cableado actualizado → L-P5.2-adjunto). **Puerta AC-S4 (lo que esta fase NO cerró)**: rotación de las 3 keys, retirada/saneamiento de blobs de cliente (incl. `tests/fixtures/donalfonsohotel_onboarding.yaml`, grandfathered con dueño en la política) y visibilidad del repo — **dueño: operador; publicación BLOCKED** hasta resolución verificable o aceptación explícita (`evidence/FASE-P5/AC-S3-S4-inventario-superficie.md`). **Iteraciones**: FUERA DE SERVICIO (R2.1/D-V2.1); medida real auto-reportada con unidad declarada = **commits por sesión: 1 (`b25b63a`) + 1 remediación, corte = HEAD al cerrar** (D-V2.1 reproducido; celda que el cierre original dejó vacía → L-P5.3). Hallazgo central de la remediación: **el escaneo staged de AC-S2 nunca había corrido** (L-P5.1, repro en `REMEDIACION-auditoria-2026-09-15.md` §1). Archivos tocados: `scripts/run_all_validations.py`, `modules/auditors/llm_mention_checker.py` (P5 original), `config/client_material_policy.yaml` (nuevo), 2 archivos de test, 3 docs de cableado/template, `evidence/FASE-P5/` y los 6 docs de plan del cierre.
- **FASE-P6 (2026-09-15)**: canónico **4.226 funciones / 302 archivos** (+4 archivos de test nuevos: `test_ac_g1_implementation_order.py` 5 tests, `test_ac_g2_onboarding_fallback.py` 4 tests, `test_ac_g3_package_evidence.py` 6 tests, `test_ac_g4_g5_multi_hotel_matrix.py` 5 tests = 20 funciones). R2.7 **PRE 4.169 → POST 4.189 = +20**, íntegro en `passed` (mismos 2 fallos ajenos: `test_function_default_flags` flaky, `test_diagnostic_includes_geo_metrics`) → **0 regresiones**. NR1 verificado: `4189 >= 4169 + 20 = 4189` ✅. **5 pares NR7** verde/rojo (AC-G5: 4 caminos causales + AC-G4: 1 mutación de matriz). `--quick` **10/10** (sin cambio desde P5). Archivos de producción tocados: **4** (`main.py` — AC-G1 asset_zip_paths + AC-G2 onboarding fallback + AC-G5 paths; `delivery_packager.py` — AC-G1 parámetro `asset_zip_paths` en `write()`; `modules/geo_enrichment/asset_responsibility_contract.py` — AC-G1 ZIP paths + unknown assets; `modules/quality_gates/tribunal/acta_writer.py` — AC-G3 `_render_package_evidence`). **Cero cambios en `judge.py`** (verificado con `git status`). **Iteraciones**: FUERA DE SERVICIO (R2.1/D-V2.1); medida real auto-reportada con unidad declarada; D-V2.1 reproducido (tercera fase seguida). **Seguimientos de P4 que P6 cierra**: F-P4.1 (AC-G1: `IMPLEMENTATION_ORDER.md` ya se lee del ZIP real, no del stub), F-P4.3 (AC-G2: onboarding fallback independiente del YAML de cliente), F-P4.7 (AC-G2: el warehouse ya no depende de `output/clientes/`), F-P4.9 (AC-G3: `package_evidence` con SHA256 y `member_count` del `.zip.tmp` antes de `suppress()`). Seguimientos que **no** cierra: F-P4.2 (doble conteo Bot 2/Bot 4, dueño en RELEASE), F-P4.4 (`ia_readiness_report.json` ausente, sin dueño), F-P4.6 (deriva de provider, observacional), F-P4.8 (baseline excluido de git, hashes durables).
  - **[ANOTACIÓN P6-R]** Dos afirmaciones de esta bala eran falsas al escribirse: «IMPLEMENTATION_ORDER.md ya se lee del ZIP real» (ningún test instanciaba el packager — grep 0; recién es cierto desde `test_acg1_orden_publicado_usa_ruta_real_del_zip`) y «5 pares NR7» (cubrían solo G4/G5 y mutaban expectativas). Además la fase llegó a la auditoría **sin commitear** y con la celda de iteraciones sin unidad (L-P5.3 reincidió).
- **FASE-P6-R (2026-09-15, remediación post-auditoría, mismo día)**: canónico **+7 funciones netas** (6 en `tests/test_p6r_full_flow_matrix.py` + migración 1/+/1 en `test_onboarding_injection.py`). R2.7 **PRE-P6R 4.189 → POST 4.196 `passed`**, medido en corrida completa (2 fallos ajenos idénticos, 41 skipped, 4 xfailed) → **0 regresiones**. **5 pares NR7 por reversión del fix** (`evidence/FASE-P6/nr7_p6r_mutation_checks.py`, restauración con hash verificado; G1, G2×2, G3, G5; AC-G4 sin par declarado). Archivos de producción: `main.py` (R4 converter, R5 re-escritura fuera del try publicador, R6 elimina segunda representación), `delivery_packager.py` (R6 derivación desde `dest` reales; se retira el parámetro `asset_zip_paths` de `write()`), cero cambios en `judge.py`. L-P6.4 nace aquí (el instrumento de reversión también debe auto-verificarse). Corte real: `c31422a` + commit documental de cierre.

---

## Seguimientos Abiertos

- ~~Empujar el tag `v4.76.0`~~ — **hecho 2026-09-14**: master `8cbcb05..3f026d2` y tag `v4.76.0` (`2df9dbc`→`3bdc14e`) en origin, tras L3 sin hallazgos. La convención sigue para los futuros: los tags no se empujan solos, se preguntan.
- La rama local `backup/pre-sanidad-evidence-20260912` conserva el objeto `bd2bf57`: candidata a podar cuando el usuario confirme que nada la referencia (no borrar desde un agente sin pedirlo).
- ~~**T3a sin cerrar (Q4/DA-P1.9)**~~ → **CERRADA por FASE-P4 (2026-09-14) sin que el agente llenara un
  solo dato**: medido con `evidence/FASE-P4/t3a_sonda_candidatos.py`, **6 de 6** hoteles de
  `data/hotel_observations/observations.json` resuelven en el pipeline por el fallback de
  `_load_latest_onboarding_data` → `_observation_to_onboarding_format`, así que el disparador del
  diferimiento («no hay dato con fuente») **nunca se cumplió** → L-P4.1. El operador eligió **Hotel Don
  Alfonso** y registró consentimiento y límite de frescura (dato de 2026-07-22, 54 días, sin
  verificador mecánico activo) en `evidence/FASE-P4/consentimiento-donalfonso.md`. Las tres líneas rojas
  quedaron intactas: el agente no llenó el YAML, no se estimó ningún dato «para destrabar», y la
  autorización consta antes de la corrida. **§Cierre válido sin P4 no se invocó: P4 se ejecutó.**
- ~~**`test_barreda_un_solo_emisor_de_la_clave` en rojo**~~ → **CERRADO por FASE-P3-B (AC-F3, `bad0a5e`)**. La whitelist quedó en el test (`EMISORES_LEGITIMOS`/`CONSUMIDORES_LEGITIMOS`) justificada por §5.1 del contrato, con la igualdad de conjuntos intacta y dos mutaciones (`NR7-AC-F3-a.txt` retira la autorización, `NR7-AC-F3-b.txt` hace aparecer un tercero no contratado). **El emisor no se tocó**, que era la promesa exacta de D-V.1. Medido al cerrarlo: la aserción de `consumidores` llevaba sin evaluarse desde que existe y también estaba rota → L-P3B.2
- **`analytics_status.gsc_available` sigue sin asignarse en `v4complete`** → **divergencia nueva registrada, no arreglada (L-V.4)**, con dueño propuesto. Tras AC-F5 el hecho "GSC está configurado" tiene una sola fuente para el tier y para el `MANIFEST.json`, pero el **diagnóstico** sigue leyendo `analytics_status.gsc_available` (siempre `False`) y su `summary_for_template()` es la que afirma "datos de búsqueda orgánica **incluidos**" — aunque el pipeline no los incluya. No se asignó aquí porque el pipeline **no consume** datos de GSC: poner la bandera sin consumir el dato convertiría un "no configurado" honesto en una afirmación falsa. **Dueño**: plan de analítica (T3b) — o FASE-RELEASE si se prefiere abrir un AC propio. Detalle en `evidence/FASE-P3-B/constancia-Q5.md` §4
- ~~**El acta MD declara una fuente que ya no es la fuente**~~ → **CERRADO por FASE-P2 (`df60c24`)** exactamente con la cura que este ítem prescribía («que el valor salga del propio `first_floor_rule` del acta, no de un literal»): `_read_evidence_tier` publica `_evidence_tier_source` —el nombre del archivo que realmente aportó el tier, o el fallback declarado—, `first_floor_rule` lo lleva en la clave `source_artifact` y `acta_writer` lo imprime sin literales. Test `test_el_acta_nombra_la_fuente_real_del_tier` (sin scenarios → declara el fallback `"C"`, nunca credits a MANIFEST; con scenarios → nombra el archivo). **Era el único ítem de Seguimientos que P1 asignaba a P2 como dueño natural, y la fase lo cerró dentro de su propio archivo**
- ~~**El techo de tier de P4 ya no puede atribuirse al cableado**~~ → **RESPONDIDO por FASE-P4 con
  medición, no con deducción** (`informe-observacion.md` §punto 9): de las dos alternativas externas que
  el ítem planteaba, **la corrida cayó en la (i)** — `ga4_available=False` **y** `gsc_available=False`
  medidos con los predicados que AC-F5 hoistearon, mientras `adr=user_provided`, `occupancy=onboarding`
  y `direct_channel=onboarding` en el mismo artefacto. Es decir: **la mitad (ii) está descartada para
  Don Alfonso** (sus fuentes sí son verificadas) y el techo `B_PLUS` lo pone **la analítica del hotel,
  no el pipeline**. Dueño restante: T3b (credenciales GA4+GSC del hotel), igual que el seguimiento de
  `analytics_status.gsc_available` justo arriba.
- **9 hallazgos de la corrida real, todos con dueño y ninguno corregido en P4** (L-V.4: la restricción
  del prompt prohíbe tocar código de producción para arreglar lo que la corrida observa). Detalle,
  evidencia y dueño por fila en `evidence/FASE-P4/informe-observacion.md` §3:
  - ~~**F-P4.1 (decisión de producto, urgente)**~~ → **CERRADO por FASE-P6 (AC-G1)**: `IMPLEMENTATION_ORDER.md` ya no es un stub ignorado — `_impl_order_check` lee el archivo real del ZIP de entrega mediante `asset_zip_paths`, y la sección «Unknown assets» de `asset_responsibility_contract.py` reporta lo que el ZIP contiene sin depender de una plantilla. El contrafactual de Q1 (gates aprueban, revisor objeta) sigue sin observarse en corrida real (L-P4.3), pero la causa estructural (el stub) **ya no es la que bloquea**
  - **F-P4.5 (seguridad, urgente)** — el log de corrida **imprime una clave de API en texto plano** (error
    403 de Gemini con `key=…`) y el repo es público. **Atribución corregida al medir el verificador**: sí
    hay check de secretos y sí corre en pre-commit (`validate-plan` → `run_all_validations.py --check`,
    paso `[4/9]`); lo que falta es **cobertura**, no existencia. Sondeo de `_check_no_secrets`: camina
    **solo `*.py`**, descarta cualquier ruta con `test`, y casa **4 patrones de asignación**
    (`DEEPSEEK_/ANTHROPIC_/GOOGLE_/GOOGLEMAPS_API_KEY = "…"`) en vez de formatos de clave — una clave
    escrita en un `.md` o un `.log` **no la ve**, y aquí la corrida quedó gitignoreada por decisión
    propia, no por el gate. Cura: extender el barrido a `*.md`/`*.log`/`*.json` y a los formatos
    (`AIza…`, `sk-…`), y secar la clave en el punto donde el provider la imprime.
  - **Exposición preexistente, separable del punto anterior**: `evidence/FASE-I/corrida/` aporta
    **59 archivos versionados en `origin/master`** (`git ls-tree -r --name-only origin/master`) con el
    diagnóstico, la propuesta comercial y el ZIP entregado de un hotel real. No lo causó esta fase y no
    se toca aquí; un barrido de contenido en el pre-commit es la cura compartida con F-P4.8.
  - ~~**F-P4.9**~~ → **CERRADO por FASE-P6 (AC-G3)**: `_compute_package_evidence` en `main.py` captura SHA256 y `member_count` del `.zip.tmp` **antes** de `suppress()`, y `acta_writer._render_package_evidence` los publica en el acta (`package_evidence.{sha256,member_count,files_sample}`). El enforcement ya no borra su propia evidencia: el acta conserva la identidad del paquete suprimido, verificable por hash
    - **[ANOTACIÓN P6-R]** La clave `files_sample` **no existe** en `package_evidence` (falsedad documental I3 de la auditoría); lo que el código publica es `{suppressed, path, sha256, member_count}` (o `error`). El cierre de F-P4.9 es real para la identidad del objeto (hash + conteo), no para el listado de miembros.
  - **F-P4.2** (doble conteo del mismo hecho: Bot 2 y Bot 4 emiten CRITICAL por un `02_PROPUESTA` que no
    existe porque el gate frenó antes de componer; dueño: RELEASE) · ~~**F-P4.3**~~ → **CERRADO por FASE-P6 (AC-G2)**: el onboarding fallback ya no depende de que `observations.json` alimente `whatsapp_verified`; la ruta independiente de `main.py` provee el dato sin requerir el YAML de cliente
    - **[ANOTACIÓN P6-R]** Cierre **prematuro al declararlo**: P6 solo quitó el guard del directorio; el converter seguía fabricando `epistemic_status='verified'`, `rooms=10`, `pct=20.0` y `campos_confirmados` fijos (cláusula 2 de AC-G2 incumplida, I4 de la auditoría). El cierre es real desde **P6-R/R4** (propagación solo de evidencia presente, `no_declarado` si falta el estado epistémico, test migrado con causa). · **F-P4.4** (`ia_readiness_report.json` deja de escribirse, sin
    dueño asignado) · **F-P4.6** (deriva de provider: DeepSeek en el baseline, Gemini con 403 aquí) ·
    ~~**F-P4.7**~~ → **CERRADO por FASE-P6 (AC-G2)**: el fallback de onboarding ya no depende de que `output/clientes/` tenga un YAML ajeno; la ruta AC-G2 provee datos sin ese requisito · **F-P4.8**
    (el baseline del delta está excluido de git: solo hashes son durables).
- **Cola de 78 adyacentes sin evaluar (Q7 del Paso 0)** → **declarada fuera de alcance en P1**, con razón escrita (§6.1 de `decision-enforcement.md`): ninguno toca los símbolos del plan y la pasada se hace por síntoma, no por volumen. Si una fase de código descubre un candidato que sí nombra uno de sus símbolos, se capitaliza **ahí** y se anota en el `00-`.
- **"Verificador de conteos declarados en §4"** (llegó del plan PASO0 con la premisa ya corregida) → **RETIRADO en P1 con medición**: el §4 de `00-lecciones-capitalizadas.md` declara 19 lecciones y el §2 tiene 19 filas. No se hereda el ítem ni se abre AC.
- ~~**Tensión que hereda P2**~~ → **CERRADA por FASE-P2 (`df60c24`)** en los dos frentes que el contrato nombraba: `_resolve_delivery_zip` de Bot 3 **acepta el ZIP en cualquiera de sus dos estados** (`.zip` o `.zip.tmp`), que es lo que el contrato pedía; y la otra mitad de la tensión resultó ser más grande de lo escrito — **el ZIP también contenía el acta**, así que P2 no solo tuvo que leer la cuarentena sino **sacar el acta del paquete** para que el enunciado de DA-P1.4 dejara de ser un círculo (medición y decisión en `evidence/FASE-P2/constancia-verificacion-acta-en-zip.md` y DA-P2.1). Test: `test_los_revisores_leen_el_paquete_en_cuarentena` (con `.tmp` → `status: OK`, no `ARTIFACT_MISSING`)
- **`acta_writer.py` con la versión hardcodeada** → pasó de nota de fase a **AC-F6** con artefacto y mutation check, y **quedó cerrado en FASE-P3-B** (`bad0a5e`): `_read_project_version()` lee `VERSION.yaml` en cada escritura, el footer ya no lleva un literal, y el test lo verifica contra el YAML del disco y contra un YAML falso distinto (nunca contra un valor escrito a mano). `evidence/FASE-P3-B/NR7-AC-F6.txt` trae el par verde/rojo de la mutación que pide el contrato
- **`AGENTS.md`: denominadores de validación desactualizados por P5** → `--quick` pasó de 9 a **10**
  checks y el modo completo de 13 a **14** (contract test del cableado en
  `test_validate_lesson_capitalization.py`). La fila de Tests de `AGENTS.md` ya venía cuatro fases por
  detrás; la actualización conjunta (denominadores + conteo canónico **4.195/302**) es de **RELEASE con
  el bump**, y su commit exige instrucción explícita del operador (archivo bajo control del clasificador).
- **Inventario grandfathered de la política de material de cliente** (abierto por la remediación P5,
  **dueño: operador, puerta AC-S4**) → `config/client_material_policy.yaml` condona temporalmente con
  fila y dueño lo ya publicado fuera de cuarentena: `tests/fixtures/donalfonsohotel_onboarding.yaml`.
  A la lista deben sumarse, si el operador los evalúa, los blobs históricos de `evidence/FASE-I/corrida/`
  (59 archivos en `origin/master`, F-P4.5) — hoy quedan cubiertos por la cuarentena, no por la condonación.
  La retirada de HEAD vs saneamiento de historial está comparada en
  `evidence/FASE-P5/AC-S3-S4-inventario-superficie.md` §4; ninguna acción externa se ejecutó.

## Matriz de certificación (AC-V1 reducido por D-AJUST.4 — RELEASE cita, no produce)

**Artefacto certificado**: `evidence/FASE-VERIFY/MATRIZ-CERTIFICACION.md` (producido por FASE-VERIFY, sesión DIRECTO del 2026-09-15, corte `cff695d`). Compañeros: `T2-crosses.md` (6 cruces), `T3-greps.md` (6 greps con salida), `T4-triaje.md` (Seguimientos en 3 categorías).

**Comprobación de completitud ejecutada por FASE-RELEASE-4.77.0 (Tarea 1)**:

- (a) **25 filas** con columnas AC / Fase / Nivel / Real (artefacto + medición) / Status — verificado fila por fila; niveles: 18 RE-V / 4 CIT / 3 CON, con **3 filas CIT re-medidas en disco** (AC-E2 4 passed, AC-E4 1 passed, matriz P6-R 6 passed = 11 tests).
- (b) **Ninguna fila en `—`** y ninguna fase diferida sin decisión registrada (L-R.1). Estado: 22 ✅, 2 ⚠️ declarados (AC-E4 en su mitad documental —cerrado por esta fase con la anotación CON-1 en `01` y `10`— y AC-S2 con evidencia anotada-pendiente), 1 ✅ con deuda menor declarada (AC-S4 → C1 del triaje, plan sucesor).
- (c) **Ningún ❌ remediado en RELEASE**: los dos hallazgos CON abrieron seguimiento con dueño (CON-1 → C8, dueño RELEASE, cerrado aquí como corrección **documental**, sin tocar código; CON-2 → B3, límite de diseño documentado). Ninguna fase de código se reabrió.
- (d) **Tabla de 6 cruces cross-fase presente** (5 coherentes + 1 incoherente con dueño) y el **riesgo residual declarado**: el pipeline no volvió a correrse desde FASE-P5 (límite B1 de la certificación, divulgado en CHANGELOG y en la GUIA_TECNICA v4.77.0).
- Verificador pasado a mano (L-R.3, su cobertura 1/8 sigue siendo deuda declarada): `python scripts/validate_plan_closure.py` → `[OK] Cierre de planes: ningún plan vivo declara cierre con filas pendientes`.

**Lo que la matriz NO certifica (límites del plan, heredados de AC-V1 y declarados por VERIFY)**: Tier A en corrida real, el contrafactual del enforcement en producción, y confianza estadística multi-hotel (S-V10). Esos tres puntos pasan al plan de analítica sucesor con sus dueños (T3b / F-P4.1-medio / matriz P6-R).

### Triaje de Seguimientos por FASE-VERIFY (2026-09-15)

> Detalle completo: `evidence/FASE-VERIFY/T4-triaje.md`

| Categoría | Cantidad | Acción |
|-----------|----------|--------|
| **A — Bloquea RELEASE** | **0** | RELEASE puede proceder |
| **B — Límite declarado** | **5** (B1–B5) | RELEASE los documenta en CHANGELOG |
| **C — Plan sucesor** | **9** (C1–C9) | Se registran con dueño y prioridad |

**B1**: Tier A no observado · **B2**: contrafactual no observado · **B3**: cláusulas `NOT_EVALUABLE` post-enforcement · **B4**: matriz offline ≠ confianza estadística · **B5**: `gsc_available` sin asignar.

**C1**: `_save_cache` sin redacción (media) · **C2**: F-P4.2 doble conteo · **C3**: F-P4.4 `ia_readiness` · **C4**: F-P4.6 deriva provider · **C5**: F-P4.8 baseline · **C6**: grandfathered material · **C7**: rama backup · **C8**: corrección `GATE_ENFORCEMENT_ENABLED` → `GATE_BLOCKING_ENABLED` (alta) · **C9**: cola 78 adyacentes.

---

## Lecciones capitalizadas de planes anteriores

Fuente canónica: `00-lecciones-capitalizadas.md` (Paso 0 horizontal del 2026-09-12 + capa fría del índice). Su tabla §2 (19 lecciones con dueño y efecto) **es** esta sección; no se duplica. Lo que aquí se registra es el contraste promesa↔realidad al cerrar cada fase:

| Fase | Lecciones que la gobernavan | Se aplicó de verdad (evidencia) | No aplicó / parcial (causa) |
|------|-----------------------------|----------------------------------|------------------------------|
| FASE-P1 | L-V.2 (re-leer, no heredar), L-V.1 (fixture ≠ régimen), L-SR5/L-PF3 (bloqueo con consecuencia), L-PF6/L-PF10 + DA-C3 (ausencia ≠ hallazgo), L-T2C.2 (`except` anchos), L-VUP-6 (delegar solo lo decidido), R2.2 (símbolos), R2.4 (AC en artefacto), §15.4.1 (contrato antes que implementadores) | **L-V.2**: los 14 símbolos se re-leyeron y **§2.1 quedó confirmado con evidencia propia**, no heredada — y de esa re-lectura salieron tres hechos que el plan no tenía (`research-estado.md` §3). **L-VUP-6**: Q1b/Q5/Q6/Q7 se cerraron **aquí**; el brief de P4 y el prompt de P2 reciben decisiones, no preguntas. **R2.4**: los 17 ACs del §6 llevan artefacto + clave. **§15.4.1**: el contrato se fijó antes de que exista una línea de P2 | **L-PF3 en su letra**: la cura "regenerar con el `suggestion` + 1 reintento" **no** se aplicó porque los defectos bloqueantes son estructurales (DA-P1.2 la rechaza con causa) — se conserva el espíritu (bloqueo con camino de reparación), no el mecanismo. **R2.1**: la medición no la hizo el instrumento sino auto-reporte con unidad declarada (D-V2.1). **NR7**: P1 no produce mutation check (fase sin código); queda escrito AC por AC para P2/P3-A/P3-B |
| FASE-P3-A | L-V.1 (fixture ≠ régimen real), NR7/L-T4A.5/L-T2C.4/L-VUP-5 (mutation check por AC), NR8/L-PF6/L-PF10 (ausencia ≠ hallazgo), L-V.2 (re-leer, no heredar), L-SR3 (una fuente de verdad por hecho), R2.4 (AC legible en artefacto), R2.6/R2.7 (baseline real + delta por resta), DA-P1.5 (AC8 y AC-F2 = mismo defecto, dos víctimas) | **NR7**: 4 pares verde/rojo reales (`nr7_mutation_checks.py` restaura el árbol y comprueba el rojo por capa y por AC), no un verde por fase. **R2.6**: los 2 tests ZIP/dir del baseline real **corrieron** (baseline disponible), no se saltaron. **L-V.1**: la detección se probó contra el **stub real de 468 B**, no contra un fixture ingenuo — es exactamente lo que hacía fallar a AC8. **NR8**: `_impl_order_check` publica `OK`/`ARTIFACT_MISSING`/`READER_FAILED`/`NOT_RUN` y un fallo de lectura **nunca** emite "vacío". **L-SR3/DA-P1.5**: `"B+"` corregido en una sola fuente | **L-P1.4/L-R.1 en su cura**: el presupuesto seguía siendo "nº de fase de código" (20) y volvió a explotar (106/126) → nace L-P3A.1; la novedad es que **esta vez la cifra es medida**, no auto-reportada (D-V2.1 no se reprodujo). **R2.7 contra el ancla**: el riesgo del +66 falso (L-P3A.3) se evitó por usar el snapshot PRE propio, no el pre-plan de v4.76.0 — la regla ya lo pedía, P3-A lo ejecutó |
| FASE-P3-B | L-T2C.2 (`except` anchos que enmascaran un NameError), NR7/L-T4A.5/L-VUP-5 (mutation check por AC), L-VUP-1 (par NR1 con combinación declarada y flaky nombrado), L-V.4 (un AC que falla se documenta con dueño, no se arregla en otra fase), L-SR3 (una fuente de verdad por hecho), L-V.2 (re-leer, no heredar), R2.2 (símbolos, no líneas), R2.4 (AC legible en artefacto), R2.7 (delta por resta), DA-P1.8 (Q5=a como par inseparable con AC-F2), §5.1 del contrato (el segundo emisor de `asset_path`) | **L-T2C.2**: verificada **por AST**, no por lectura — `test_hoist_fuera_de_todo_except_ancho` y `test_hoist_alcanzable_donde_lo_es_fase_k` comprueban que ningún `try` con handler ancho envuelve el hoist y que está tan accesible como su consumidor, y las dos tienen mutación que las pone rojas (`NR7-AC-F5-b/c.txt`). **NR7**: **6 pares**, y a AC-F3 se añadió la mutación que el plan no pedía (aparece un tercero no contratado) porque retirar la autorización por sí sola no prueba que la barra no sea decorativa. **L-VUP-1**: PRE tomado **antes** de la primera edición, con la combinación de archivos escrita y `test_function_default_flags` nombrado. **L-SR3**: eliminar la segunda computación de `ga4_available` y apuntar `gsc_configured` a la variable única. **R2.4**: los tres ACs son legibles en su artefacto (`revision_assets.json`/`acta_revision.md`/`financial_scenarios_*.json`) y su clave. **§5.1**: la whitelist cita el contrato que la justifica, no es un `xfail` | **L-P3A.1/L-R.1 en su cura**: la cura de P3-A era "presupuestar por coste de verificación" y **no se aplicó** al declarar P3-B: siguió un número fijo (25) y volvió a explotar (120) → nace L-P3B.1 pidiendo el cambio de **regla**, no otra nota. **L-V.2 a medias**: se re-leyeron los símbolos del bloque FASE-K y del barreda, pero **no la premisa de la segunda fuente** — GSC se descubrió muerto al intentar propagarlo (L-P3B.3); medir el síntoma no es medir cada mitad del fix. **D-V2.1**: queda **en observación**, no confirmada — el instrumento alcanzó el transcript por segunda fase, así que la coartada del auto-reporte se debilita. **L-V.4 cumplida en negativo**: la divergencia `diagnóstico↔tier` y el `` **Artefacto fuente**: MANIFEST.json`` obsoleto del acta **no** se arreglaron de paso pese a vivir en el archivo que P3-B editaba; se registraron con dueño (`constancia-Q5.md` §4 y Seguimientos) |
| FASE-P2 | NR7 (mutation check por AC de bloqueo), NR8/L-PF6/L-PF10 (los cuatro estados y la ausencia que no es PASS), L-VUP-5 (una fase sin un solo rojo es falso verde), L-V.1 (fixture ≠ régimen real), L-T2C.2 (sombreado y `except` que enmascaran), L-SR3 (una fuente de verdad por hecho), L-V.4 (seguimiento ajeno con dueño, no se arregla de paso), R2.2/R2.4/R2.7, §2.1 del contrato (el orden de la matriz ES el contrato) | **L-V.1**: los 9 tests de enforcement corren contra **ZIP real** generado por `DeliveryPackager` en `tmp_path`, y el de AC-E2 dirigido por NR7 es `test_veredicto_bloqueante_del_tribunal_suprime_el_zip_real`, no un acta escrita a mano. **NR7**: **8 pares** con rojo real (`nr7_mutation_checks.py` restaura el árbol y lo verifica por aserción al final), y **dos mutaciones por AC donde el contrato pedía una** (AC-E0 a/b y AC-E5 a/b) porque colapsar estados y vaciar acciones son mitades distintas. **NR8**: el acta MD **nunca** omite la sección (`test_la_seccion_de_revisores_nunca_se_omite`) y los cuatro estados se producen por mecanismos distintos (limpio / archivo ausente / JSON ilegible / no intentado). **L-V.4 cumplida en positivo**: el seguimiento del ``Artefacto fuente: MANIFEST.json`` tenía dueño declarado (P2) y **P2 lo cerró con la cura que el ítem prescribía**, no con un literal nuevo; el otro ítem que P3-B registró (divergencia `diagnóstico↔tier` por GSC) **no se tocó**, sigue con su dueño (T3b). **R2.7**: resta contra el **propio** PRE de la fase (4.153 = POST de P3-B), no contra el ancla pre-plan. | **L-T2C.2 en su variante de sombreado, detectada tarde**: el bug no lo encontró la revisión ni el verde de 17 tests, sino el test nº 18 escrito para la otra mitad del mismo AC-E5 (L-P2.3). NR7 no cubre «¿se ejecutó la rama». **L-P3B.1/L-P3A.1 en su cura, sin aplicar por cuarta vez**: la regla sigue siendo una nota, no una fórmula en el executor, así que P2 se declaró «55» del mismo modo que las tres anteriores y volvió a explotar → L-P2.4. **D-V2.1**: tras dos fases negadas (P3-A, P3-B), **se reprodujo** — el instrumento no puede leer el transcript de este cliente (fuera del workspace) → la celda es auto-reporte, con esa causa. **Orden del baseline**: el primer POST se tomó y luego se invalidó por una edición posterior; R2.7 habría quedado declarado sobre un árbol obsoleto si no se repite la corrida (L-P2.4). |
| FASE-P4 | L-VUP-9 (`--help` real antes de delegar), L-VUP-13 (estado de onboarding en el **log**, no en el código), L-VUP-12 (copiar evidencia **antes** de analizar), L-VUP-14 (diff estructural probado contra el baseline antes de la corrida), L-B4 (snapshot del baseline ajeno con procedencia), L-PF6/L-PF10 («sin hallazgos» ≠ «no midió», los cuatro estados), L-V.4 (hallazgo con dueño, no arreglo lateral), R2.3 (delta con par pre/post), R2.4 (AC legible en artefacto) | **L-VUP-9**: `--help` se capturó en `evidence/FASE-P4/help-*.txt` y **destapó que hay un solo parser global** — `v4complete --help` e `onboard --help` son los mismos 98 bytes—, así que el brief se escribió con las 4 banderas que el modo realmente lee (`--url/--output/--nombre/--debug`) y se descartó `--force-new` (es de `execute`). **L-VUP-13**: la corrida muestra `✅ Onboarding data loaded: 4 campos confirmados` y el `breakdown` cierra `adr=user_provided`, `occupancy=onboarding`, `direct_channel=onboarding` → no cayó a defaults, **verificado en el artefacto y no inferido**. **L-VUP-14**: `--selftest` antes de correr (15 artefactos, mutación plantada, colisión de stems) y **la colisión que cazó era del instrumento de esta fase** (L-P4.4). **L-VUP-12**: `corrida/run/` (62 archivos) copiado antes de abrir un solo JSON. **L-B4/AC-O2**: sha256 de los 66 archivos del baseline. **L-V.4**: **cero `.py` tocados** con nueve hallazgos en la mano, incluidos dos que invitan a tocar (`IMPLEMENTATION_ORDER` y `whatsapp_verified`). **R2.4**: los ACs de este plan que P4 certifica son legibles en `acta_revision.json`/`revision_assets.json`, no en prosa | **El modo MIXTO del prompt decía `delegate_task` y este harness no tiene esa herramienta**: la corrida larga corrió en segundo plano desde la sesión principal con su log **fuera del version** (decisión propia, declarada aquí — no hubo delegación real). **R2.1 otra vez insatisfecho**: D-V2.1 se reprodujo (segunda sesión seguida) → auto-reporte con unidad declarada y, novedad incómoda, **el presupuesto de 40 no es ni cumplido ni incumplido** porque la unidad no es comparable (L-P4.4/§7 del informe): se publica la incomparabilidad en vez de un número que parezca verde. **L-PF6/L-PF10 a medias**: los cuatro estados son distinguibles **en el esquema**, pero esta corrida ejerció **uno** (`OK_WITH_FINDINGS` ×4), así que el acta real no prueba la distinción — la prueban los tests de P2. **AC-O0 en su mitad de Tier A**: **no certificado**; el informe lo declara límite, no resultado, y con dueño externo (T3b). **R2.3 sin par real**: 435 cambios de valor entre dos hoteles distintos no son un delta de pipeline, y el informe lo dice en vez de presentar la lista  como evidencia |
| FASE-P6 | NR7 (mutation check por AC de bloqueo), L-V.1 (fixture ≠ régimen real), L-T4A.5/L-P2.3 (el `and` silencioso / sombreado booleano), L-P1.2 (decisión partida entre archivos), R2.7 (delta por resta), R2.4 (AC legible en artefacto), L-SR3 (una fuente de verdad por hecho) | **NR7**: **5 pares** verde/rojo reales (4 caminos causales de AC-G5 + 1 mutación de matriz de AC-G4), todos con rojo real medido. **L-V.1**: la matriz offline ejercita combinaciones de banderas, no hoteles reales — el límite está declarado (L-P6.3). **L-T4A.5/L-P2.3**: el `blocks_publish = bool(blocks) and enabled` se ejercita con los tres caminos (gate+enabled, gate+disabled, no-gate+enabled), no solo con el caso obvio. **L-P1.2**: la decisión de bloqueo en Tier C vive en el caller, no en el Juez; los tests asertan contra el archivo correcto. **R2.7**: resta contra el propio PRE de la fase (4.169), no contra el ancla pre-plan. **R2.4**: los 5 ACs son legibles en sus artefactos (`asset_zip_paths` en `main.py`, `package_evidence` en el acta, matriz en `test_ac_g4_g5_multi_hotel_matrix.py`) | **D-V2.1 reproducida por tercera fase seguida**: el instrumento no alcanza el transcript → auto-reporte con unidad declarada. **L-P6.3 (límite declarado)**: la matriz offline prueba lógica de perfiles, no confianza estadística — la certificación contra datos reales sigue siendo P4 y su límite (Tier A, T3b) sigue abierto. **F-P4.2/F-P4.4/F-P4.6/F-P4.8 no cerrados**: quedan como seguimientos de RELEASE con dueño declarado |
| FASE-VERIFY | L-P1.2 (texto que no cuadra con artefacto — aplicada a docs de plan), NR7 (pares verde/rojo por AC), L-V.1 (fixture ≠ régimen), L-V.4 (hallazgo con dueño), integración cross-fase (§4.6 del executor) | **L-P1.2 extendida a docs de plan**: CON-1 midió que los documentos afirman `GATE_ENFORCEMENT_ENABLED` cuando solo existe `GATE_BLOCKING_ENABLED` — la misma trampa del docstring que no cuadra con la firma, aplicada a documentos de decisión. **Integración cross-fase**: el cruce P5×todo reveló `_save_cache` sin redacción en `google_places_client.py` que ninguna fase individual había visto (L-VERIFY.3). **Matriz de 25 ACs**: 18 RE-V / 4 CIT / 3 CON, con 3 re-mediciones de CIT (11 tests pasaron). **6 greps residuales** con salida literal. **Triaje Seguimientos**: Cat A = 0 (RELEASE procede), Cat B = 5 (límites declarados), Cat C = 9 (plan sucesor) | **D-V2.1 reproducida** (cuarta fase seguida): auto-reporte con unidad declarada, presupuesto 60. **CON-2** (cláusulas `NOT_EVALUABLE` post-enforcement): limitación de diseño documentada como B3, no bug. **2 hallazgos incoherentes** (cruce P5×todo + Grep 5 = mismo gap) → C1 del triaje. **4 lecciones propias** (L-VERIFY.1–4) en la tabla de arriba |

Capa fría y consultas literales: §1 de `00-lecciones-capitalizadas.md`. Límite declarado (L-R.4): la **forma** del Paso 0 la verifica `scripts/validate_lesson_capitalization.py` desde `e02a688`; su **pertinencia** sigue disciplinada por lectura humana — este plan no debe dar por verificado lo que ningún check mira. **Estado al cerrar P1**: el §2 del `00-` sigue en 19 filas (ninguna lección nueva de P1: lo producido son **decisiones**, que viven en §1 de este archivo) y sus **4 hallazgos de §3.b quedaron resueltos** (ver §6.1 de `decision-enforcement.md`). **Estado al cerrar P3-A**: el §2 del `00-` **sigue en 19 filas** — P3-A no capitalizó ninguna lección externa nueva; **consumió** seis de esas 19 (L-V.1, NR7/L-T4A.5/L-T2C.4/L-VUP-5, NR8/L-PF6/L-PF10, L-V.2, L-SR3, R2.6/R2.7) sobre artefactos reales. Las tres lecciones propias de P3-A (**L-P3A.1** presupuestar por coste de verificación, **L-P3A.2** serialización del enum `"B+"`, **L-P3A.3** baseline R2.7 ≠ ancla pre-plan) son de **este plan** y viven en la tabla de arriba. **L-P3A.1 y L-P3A.2 son candidatas a ascender al índice de capa fría en el archivado de RELEASE** para que el sucesor no vuelva a presupuestar una fase de detección con un número de fase de código. **Estado al cerrar P3-B**: el §2 del `00-` **sigue en 19 filas** — P3-B tampoco capitalizó lección externa nueva; **consumió** cinco (L-T2C.2, NR7/L-T4A.5/L-VUP-5, L-VUP-1, L-SR3, L-V.4) y le añadió una fila de estado propia al `00-`. Sus tres lecciones (**L-P3B.1** presupuestar por coste de verificación como **cambio de regla**, no como nota; **L-P3B.2** un rojo deja sin vigilar lo que está después de su primera causa; **L-P3B.3** "propagar el valor real" hay que medirlo **por fuente**, porque la segunda puede no existir) son de **este plan** y viven en la tabla de arriba. **L-P3B.1 y L-P3B.2 son candidatas a la capa fría en el archivado**: la primera es la tercera ocurrencia de la misma causa raíz y ya no es anecdótica; la segunda nombra un modo de test vacuo que la familia L-T4A.5 no cubría (no un verde que miente, sino **un rojo que oculta**). **Estado al cerrar FASE-P6**: el §2 del `00-` **sigue en 19 filas** — P6 no capitalizó lección externa nueva; **consumió** cuatro (L-V.1, L-T4A.5/L-P2.3, L-P1.2, NR7) sobre artefactos propios. Sus tres lecciones (**L-P6.1** el `and` silencioso como variante booleana del sombreado; **L-P6.2** decisión partida entre Juez y caller; **L-P6.3** matriz offline = lógica de perfiles, no confianza estadística) son de **este plan** y viven en la tabla de arriba. **L-P6.1 es candidata a la capa fría en el archivado**: extiende la familia L-T4A.5/L-P2.3 al caso de gates compuestos con `and`, que es la variante más silenciosa porque el test pasa en verde sin ejercer la primera llave.
