# 04-prompt-fase-C — README derivado del delivery context

**Fase**: FASE-C — Template modular, Package Structure real, secciones por estado
**Plan**: DT-1-DELIVERY-CONTRACT-2026-07-23
**Sesión**: Nueva (fresh)
**Iteraciones máx**: 60
**Depende de**: FASE-A ✅, FASE-B ✅
**Bloquea a**: FASE-D
**Tipo**: DIRECTA (template + lógica de renderizado)

---

## Objetivo

Transformar el README de una pieza narrativa estática a una vista derivada del `DeliveryContext` y la lista real de archivos del ZIP. Eliminar TODOS los nombres de archivo hardcodeados de la template. Generar secciones según el estado canónico de cada asset.

## Contexto de fases anteriores

- FASE-A definió `DeliveryAssetState`, `DeliveryAssetEntry` y `DeliveryContext`.
- FASE-B corrigió rutas POSIX, tamaños reales, filename único y validación post-zip.
- `DeliveryPackager.package()` ahora tiene acceso a tamaños reales y un filename determinista.
- `create_readme()` sigue usando la template estática.

## Tareas

### T1: Reescribir la template como estructura modular

**Archivo**: `templates/delivery_readme_template.md` (reescribir completamente)

Eliminar TODOS los nombres de archivo hardcodeados. La nueva template debe ser un esqueleto con placeholders para secciones dinámicas:

```markdown
# Delivery Package - {{HOTEL_ID}}

**Generated:** {{DATE}}
**Package Type:** IA-HOTELES Automated Delivery
**Package file:** {{PACKAGE_FILENAME}}

---

## Overview

This package contains assets generated for your hotel.

**Contents:** {{TOTAL_FILES}} files ({{TOTAL_SIZE}})

---

## Package Structure

{{PACKAGE_STRUCTURE}}

---

## Implementation Instructions by Asset

### Core Documents
{{CORE_DOCUMENTS}}

### Deliverable Assets
{{DELIVERABLE_ASSETS}}

### Advisory Guides
{{ADVISORY_GUIDES_SECTION}}

{{PRESENT_IN_PRODUCTION_SECTION}}

{{PRESENT_WITH_ISSUES_SECTION}}

{{ESTIMATED_ASSETS_SECTION}}

{{EVIDENCE_SECTION}}

---

## Suggested Timeline

{{TIMELINE}}

---

## Implementation Checklist

{{CHECKLIST}}

---

## Support Resources

- **Schema Validation:** https://search.google.com/test/rich-results
- **GBP Best Practices:** https://support.google.com/business/
- **Google Search Console:** https://search.google.com/search-console

---

*Generated by IA-HOTELES CLI*
```

Placeholders nuevos:
- `{{PACKAGE_FILENAME}}` — nombre real del archivo ZIP
- `{{PACKAGE_STRUCTURE}}` — estructura derivada de destinos reales
- `{{CORE_DOCUMENTS}}` — instrucciones para DIAGNOSTICO.md y PROPUESTA_COMERCIAL.md
- `{{DELIVERABLE_ASSETS}}` — assets generados y entregados (DELIVERED)
- `{{PRESENT_IN_PRODUCTION_SECTION}}` — assets ya presentes sin issues
- `{{PRESENT_WITH_ISSUES_SECTION}}` — assets presentes pero con conflicto
- `{{ESTIMATED_ASSETS_SECTION}}` — assets estimados (ESTIMATED)
- `{{ADVISORY_GUIDES_SECTION}}` — guías advisory (no instalables, ej: whatsapp_conflict_guide)
- `{{EVIDENCE_SECTION}}` — archivos de evidencia y auditoría
- `{{TIMELINE}}` — timeline dinámico basado en assets reales
- `{{CHECKLIST}}` — checklist basado en estados reales

Placeholders eliminados:
- `{{HOTEL_ID}}_{{DATE}}.zip` → reemplazado por `{{PACKAGE_FILENAME}}`

### T2: Generar Package Structure desde destinos reales

**Archivo**: `modules/delivery/delivery_packager.py` (modificar `create_readme()`)

Agregar método `_generate_package_structure()`:

```python
def _generate_package_structure(self, files: List[Dict[str, Any]], zip_filename: str) -> str:
    """Genera estructura de árbol desde los destinos reales del ZIP."""
    from collections import defaultdict
    
    # Agrupar por directorio
    tree: Dict[str, List[str]] = defaultdict(list)
    for f in files:
        dest = f.get("dest", "")
        if "/" in dest:
            parts = dest.split("/")
            if len(parts) >= 2:
                dir_name = parts[0]
                file_name = "/".join(parts[1:])
                tree[dir_name].append(file_name)
            else:
                tree[""].append(dest)
        else:
            tree[""].append(dest)
    
    lines = ["```", zip_filename]
    
    # Documentos raíz
    root_files = tree.get("", [])
    for rf in sorted(root_files):
        if rf.endswith(".md") and not rf.startswith("README"):
            lines.append(f"├── {rf}")
    
    # ASSETS/
    asset_files = tree.get("ASSETS", [])
    # Agrupar por subdirectorio
    subdirs = defaultdict(list)
    for af in asset_files:
        if "/" in af:
            subdir, fname = af.split("/", 1)
            subdirs[subdir].append(fname)
        else:
            subdirs[""].append(af)
    
    if asset_files:
        lines.append("├── ASSETS/")
        sorted_subdirs = sorted([d for d in subdirs if d])
        for i, sd in enumerate(sorted_subdirs):
            prefix = "│   ├──" if i < len(sorted_subdirs) - 1 else "│   └──"
            lines.append(f"{prefix} {sd}/")
    
    # Metaarchivos
    lines.append("├── MANIFEST.json")
    lines.append("└── README_DELIVERY.md")
    lines.append("```")
    
    return "\n".join(lines)
```

### T3: Generar secciones por estado

**Archivo**: `modules/delivery/delivery_packager.py` (modificar `create_readme()`)

Agregar métodos para cada sección:

**`_generate_present_in_production_section()`**:

```python
def _generate_present_in_production_section(self, assets: List[DeliveryAssetEntry]) -> str:
    if not assets:
        return ""
    
    lines = [
        "## Already Present on Your Website",
        "",
        "The following elements are already present on your site and do NOT require installation:",
        "",
        "| Service | Status |",
        "|---------|--------|",
    ]
    for a in assets:
        lines.append(f"| {a.service_name} | Verified in production |")
    lines.append("")
    return "\n".join(lines)
```

**`_generate_present_with_issues_section()`**:

```python
def _generate_present_with_issues_section(self, assets: List[DeliveryAssetEntry]) -> str:
    if not assets:
        return ""
    
    lines = [
        "## Present but Requires Review",
        "",
        "These elements exist on your site but need attention before they are fully effective:",
        "",
        "| Service | Issue | Action |",
        "|---------|-------|--------|",
    ]
    for a in assets:
        lines.append(f"| {a.service_name} | {a.message} | Review the accompanying guide |")
    lines.append("")
    return "\n".join(lines)
```

**`_generate_estimated_section()`**:

```python
def _generate_estimated_section(self, assets: List[DeliveryAssetEntry]) -> str:
    if not assets:
        return ""
    
    lines = [
        "## Estimated Assets",
        "",
        "The following assets were generated with estimated data. Review before using in production:",
        "",
    ]
    for a in assets:
        path = a.delivery_path or a.asset_type
        lines.append(f"- **{a.service_name}** (`{path}`): {a.message}")
    lines.append("")
    return "\n".join(lines)
```

**`_generate_evidence_section()`**: Listar archivos en `v4_audit/` como evidencia (sin instrucciones de instalación).

**`_generate_advisory_section()`**:

```python
def _generate_advisory_section(self, assets: List[DeliveryAssetEntry]) -> str:
    """Genera sección para guías advisory (no instalables, solo de revisión)."""
    if not assets:
        return ""
    
    lines = [
        "The following guides are included for review. They are NOT installable assets,",
        "but reference materials to help you resolve specific issues:",
        "",
    ]
    for a in assets:
        path = a.delivery_path or a.asset_type
        lines.append(f"- **{a.service_name}** (`{path}`): {a.message}")
    lines.append("")
    return "\n".join(lines)
```

### T4: Adaptar `create_readme()` para consumir `DeliveryContext`

**Archivo**: `modules/delivery/delivery_packager.py` (modificar)

Extender la firma de `create_readme()`:

```python
def create_readme(
    self,
    delivery_dir: Path,
    hotel_id: str,
    manifest: Optional[Dict[str, Any]] = None,
    delivery_context: Optional[DeliveryContext] = None
) -> None:
```

Si `delivery_context` está disponible, usar sus datos para generar secciones dinámicas. Si no, comportamiento legacy (solo 4 placeholders).

```python
# Cargar template
content = template_path.read_text(encoding='utf-8')

# Placeholders básicos (siempre)
content = content.replace("{{HOTEL_ID}}", hotel_id)
content = content.replace("{{DATE}}", datetime.now().strftime("%Y-%m-%d"))

# DeliveryContext disponible → generación dinámica
if delivery_context:
    content = content.replace("{{PACKAGE_FILENAME}}", delivery_context.zip_filename)
    content = content.replace("{{TOTAL_FILES}}", str(len(delivery_context.files)))
    
    # Package Structure
    structure = self._generate_package_structure(
        delivery_context.files, delivery_context.zip_filename
    )
    content = content.replace("{{PACKAGE_STRUCTURE}}", structure)
    
    # Secciones por estado
    content = content.replace(
        "{{PRESENT_IN_PRODUCTION_SECTION}}",
        self._generate_present_in_production_section(delivery_context.present_assets)
    )
    content = content.replace(
        "{{PRESENT_WITH_ISSUES_SECTION}}",
        self._generate_present_with_issues_section(delivery_context.present_with_issues_assets)
    )
    content = content.replace(
        "{{ESTIMATED_ASSETS_SECTION}}",
        self._generate_estimated_section(delivery_context.estimated_assets)
    )
    content = content.replace(
        "{{ADVISORY_GUIDES_SECTION}}",
        self._generate_advisory_section(delivery_context.advisory_assets)
    )
    
    # Timeline y Checklist dinámicos
    content = content.replace("{{TIMELINE}}", self._generate_timeline(delivery_context))
    content = content.replace("{{CHECKLIST}}", self._generate_checklist(delivery_context))
    content = content.replace("{{CORE_DOCUMENTS}}", self._generate_core_documents())
    content = content.replace("{{DELIVERABLE_ASSETS}}", self._generate_deliverable_instructions(delivery_context))
    content = content.replace("{{EVIDENCE_SECTION}}", self._generate_evidence_section(delivery_context))
else:
    # Comportamiento legacy
    if manifest:
        content = content.replace("{{TOTAL_FILES}}", str(manifest.get("total_files", "N/A")))
        content = content.replace("{{TOTAL_SIZE}}", self._format_bytes(manifest.get("total_size_bytes", 0)))
    # Placeholders sin datos → vacíos
    for ph in ["{{PACKAGE_FILENAME}}", "{{PACKAGE_STRUCTURE}}", "{{CORE_DOCUMENTS}}",
                "{{DELIVERABLE_ASSETS}}", "{{PRESENT_IN_PRODUCTION_SECTION}}",
                "{{PRESENT_WITH_ISSUES_SECTION}}", "{{ESTIMATED_ASSETS_SECTION}}",
                "{{ADVISORY_GUIDES_SECTION}}", "{{EVIDENCE_SECTION}}", "{{TIMELINE}}", "{{CHECKLIST}}"]:
        content = content.replace(ph, "")
```

También actualizar la llamada en `package()` para construir y pasar el `DeliveryContext`:

```python
# Construir DeliveryContext si hay datos disponibles
delivery_context = None
if hasattr(self, '_asset_generation_data'):
    delivery_context = self._build_delivery_context(
        hotel_id, zip_filename, all_files
    )

self.create_readme(self.deliveries_dir, hotel_id, manifest, delivery_context=delivery_context)
```

## Criterios de Completitud

- [ ] La template NO contiene nombres de archivo hardcodeados (`boton_whatsapp.html`, `hotel-schema.json`, `geo_playbook.md`, `faq_page.md`).
- [ ] `Package Structure` se genera desde `files` reales (no desde una lista estática).
- [ ] Sección "Already Present" aparece si hay assets `PRESENT_IN_PRODUCTION`.
- [ ] Sección "Present but Requires Review" aparece si hay assets `PRESENT_WITH_ISSUES`.
- [ ] Sección "Estimated Assets" aparece si hay assets `ESTIMATED`.
- [ ] Sección "Advisory Guides" aparece si hay assets con `is_advisory=True`.
- [ ] Sin `DeliveryContext`, el comportamiento legacy se preserva (placeholders quedan vacíos).
- [ ] El nombre del ZIP en el README coincide con el filename real del archivo.
- [ ] 10 tests existentes del packager siguen pasando.

## Restricciones

- NO modificar `delivery_context.py` (FASE-A)
- NO modificar `create_manifest()` o `_create_zip()` (FASE-B)
- NO modificar `main.py`
- El comportamiento legacy debe preservarse: si no hay `DeliveryContext`, el README se genera sin errores (aunque con placeholders vacíos).

## Archivos involucrados

| Archivo | Tipo de cambio |
|---------|---------------|
| `templates/delivery_readme_template.md` | REESCRIBIR completamente (template modular) |
| `modules/delivery/delivery_packager.py` | MODIFICAR: `create_readme()`, AGREGAR: `_generate_package_structure()`, `_generate_present_*_section()`, `_generate_advisory_section()`, `_generate_timeline()`, `_generate_checklist()`, `_build_delivery_context()` |

## Post-Ejecución (OBLIGATORIO)

```bash
cd /mnt/c/Users/Jhond/Github/iah-cli
./venv/Scripts/python.exe scripts/log_phase_completion.py --fase FASE-C --desc "DT1_dynamic_README_state_sections_package_structure"
```
